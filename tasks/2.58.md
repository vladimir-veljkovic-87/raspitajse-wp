# Zadatak 2.58 — Validate original and redirected mail recipients and close the E2E gate

Status: READY
Baseline: ff42719f218e440b168b57690b70a2402ee66809
Previous task: 2.57
Target: staging
Production: FORBIDDEN
Time budget: 30 minutes
Mode: focused disposable-fixture mail validation; no application-code changes

## Required result

Resolve the sole incomplete assertion from Task 2.57 and complete the five enabled launch-critical transactional-message rows.

Task 2.57 proved:

- all five enabled messages rendered and were intercepted;
- PHPMailer, SMTP and external transport remained zero;
- every fixture was removed and T0/T1 state matched exactly;
- the only failed assertion compared the final redirected staging recipient with the original ledgered candidate address.

Read the exact report:

`reports/20260920T053629Z-zadatak-2_57.md`

Current source establishes two distinct recipient stages:

1. `raspitajse-staging-mail-safety.php` captures the original `wp_mail` payload at `wp_mail` priority `PHP_INT_MIN`;
2. the same safety layer rewrites `to` at priority `PHP_INT_MAX` to `RASPITAJSE_STAGING_MAIL_TO`.

Therefore the final `pre_wp_mail` payload is expected to contain the configured safe staging inbox, not the disposable fixture address. Validate intent and final safety destination separately.

PASS classifications:

`PASS: TRANSACTIONAL_MAIL_GATE_READY`

`PASS: LAUNCH_CRITICAL_E2E_READY`

No application change is authorized. A product defect, uncontrolled recipient, render/link defect, transport escape or cleanup failure is one precise BLOCKED result.

## Lean scope

Read only:

- `tasks/README.md`;
- this task;
- the exact Task 2.57 report above;
- `wp-content/mu-plugins/raspitajse-staging-mail-safety.php`;
- the currently active Raspitajse Communications mail transport;
- exact producers for the five enabled events.

Do not rerun authentication, quota, duplicate-application, authorization or scheduler suites. Do not deploy, edit source, investigate unrelated plugins or create persistent tooling.

Use one fresh private `/tmp/raspitajse-task-2.58.*` directory. Because `/tmp` is `noexec`, invoke validated temporary PHP/shell files explicitly through `php` or `bash`. Do not attempt direct execution first.

Do not use `shell_exec`, `exec`, `system`, `passthru` or another disabled PHP process function. Obtain Git/source facts in the shell preflight and pass only non-secret expected values into the harness.

For compact user snapshots use `get_users( array( 'fields' => 'ID' ) )` and normalize returned IDs as integers. Do not request `fields=all` or assume user objects. Use ID-only queries for other compact projections where possible.

Maximum one correction for a genuine temporary harness/bootstrap error. Syntax-check the complete harness and inspect its hook priorities and cleanup path before the first fixture run.

## Preconditions

Require:

- authorized Hostinger context;
- repository `/home/u601262303/repo/raspitajse-wp`;
- WordPress path `/home/u601262303/domains/raspitajse.com/public_html/public_html_stage`;
- local `staging`, `origin/staging` and live marker equal the declared baseline;
- clean Hostinger worktree;
- WordPress environment exactly `staging`;
- WordPress `7.1.1`, WPForms Lite `2.0.2`, child theme `superio-child`;
- Communications and Commerce loaded;
- staging mail-safety loaded and configured with a valid safe inbox;
- both runtime locks acquirable nonblocking;
- no overlapping owned-runner process.

Do not print the safe inbox or any fixture address. The unrelated client/Windows checkout remains untouched.

Any authoritative mismatch is BLOCKED before fixtures.

## Exact lock protocol

One persistent worker acquires nonblocking in this order:

1. descriptor 9: `/tmp/raspitajse-staging-owned-cron-runner.lock`;
2. descriptor 8: `/tmp/raspitajse-staging-runtime.lock`.

If either acquisition fails, release anything acquired by this worker and return `BLOCKED: STAGING_LOCK_BUSY`.

Hold both descriptors continuously from before T0 through message validation, exact cleanup and T1. Release descriptor 8 first and descriptor 9 second. Do not delete, rename or chmod lock files, kill holders, poll, disable host cron or run a scheduler callback.

## Recipient observation contract

Register one final `pre_wp_mail` interceptor after normal plugins and the MU safety layer are loaded. It must:

1. read the original payload from `raspitajse_staging_mail_safety_current_context()`;
2. receive the final filtered payload in the `pre_wp_mail` `$atts` argument;
3. copy only the values needed for in-memory assertions;
4. return `false` before PHPMailer;
5. pop the MU-plugin staging context exactly once for the intercepted call, because `phpmailer_init` will not run after a preempted message.

Do not modify or bypass the safety filters.

For each event prove separately:

- **intended recipient:** original context `to` normalizes to exactly the expected ledgered fixture address and role;
- **safe final recipient:** final filtered `to` normalizes to exactly the result of `raspitajse_staging_mail_safety_recipient()`;
- original and final recipient values are different for the disposable fixture;
- final recipient-bearing `To`, `Cc` and `Bcc` headers are absent;
- final subject has exactly one `[STAGING]` prefix;
- interception occurs before PHPMailer.

Compare canonicalized mailbox values in memory. Accept scalar or array forms and display-name syntax only through a deterministic mailbox parser. Do not compare raw strings when equivalent address representations differ.

Never print, hash with a reusable unsalted digest, or persist an address. Report only expected role plus boolean results. A task-private keyed/HMAC correlation may be used in memory if necessary, but the key and digest must not appear in evidence or reports.

## Safety guards and fixtures

Before triggering mail:

- instrument PHPMailer and SMTP entry and require zero;
- block payment/gateway/refund execution;
- block outbound WordPress HTTP before transport;
- block broad WP-Cron, Action Scheduler and owned scheduler callbacks;
- create an exact ledger.

Create only:

- one disposable employer with reciprocal profile;
- one disposable candidate with reciprocal profile;
- one job and one application only as required for application notifications.

Use reserved non-deliverable domains, random credentials held only in memory and normal WordPress/WP Job Board Pro/Raspitajse APIs. No raw SQL for creation/deletion and no CV/file upload.

Password reset targets only the disposable candidate. Do not print or persist passwords, reset tokens, cookies, nonces, addresses, raw subjects or bodies.

## Required event table

Trigger and fully validate:

1. candidate registration/welcome;
2. employer registration/welcome;
3. candidate password-reset request/render;
4. employer application notification;
5. candidate application confirmation.

Confirm job submission/publication notification remains intentionally disabled through the current setting and record `DISABLED_CONFIRMED`; do not enable or trigger it.

For each of the five enabled rows require:

- original intended recipient role PASS;
- final redirected safe recipient PASS;
- recipient headers stripped PASS;
- non-empty subject/body PASS;
- no unresolved placeholder PASS;
- required application/action link present when applicable;
- internal action links use `stage.raspitajse.com`;
- no production host `raspitajse.com` or `www.raspitajse.com`;
- no pricing/package/cart/checkout/payment link under the free-launch policy;
- no external credential/action URL;
- `pre_wp_mail` interception PASS;
- PHPMailer/SMTP zero.

External presentation/template URLs are not failures merely because their hosts are external. Record normalized hostnames and classification only. `plus.google.com` and `www.w3.org` are allowed presentation/template hosts when they contain no credential, fixture or action data. Do not request or follow any URL.

CAPTCHA remains `MANUAL_BROWSER_CHECK_REQUIRED`; do not automate it.

## Cleanup and equality

Always clean in dependency-safe order using only exact ledgered IDs and normal APIs:

1. application;
2. job;
3. profiles;
4. users.

Require:

- T0/T1 equality for users, employer profiles, candidate profiles, jobs and applications;
- no residue by ledgered ID;
- compact non-content business hash unchanged;
- active plugin/theme hash unchanged;
- owned scheduler contract and cron fingerprints unchanged;
- Action Scheduler item 32733 status/attempts unchanged;
- source/live marker unchanged;
- clean worktree.

At most one bounded cleanup correction may use exact ledgered IDs. Never broadly delete.

## Zero-side-effect requirements

- PHPMailer: 0;
- SMTP: 0;
- actual outbound WordPress HTTP transport: 0;
- payment/gateway/refund execution: 0;
- broad WP-Cron: 0;
- Action Scheduler execution: 0;
- owned scheduler callbacks: 0;
- source/deploy mutation: 0;
- production access: 0.

## Result

PASS requires five complete enabled-event rows, one proven disabled row, exact cleanup/T1 equality, unchanged baseline and every prohibited counter at zero.

Return:

`PASS: TRANSACTIONAL_MAIL_GATE_READY`

`PASS: LAUNCH_CRITICAL_E2E_READY`

Otherwise return one precise BLOCKED reason after cleanup. Do not alter application code or repeat broader E2E work.

## Final report

Publish one concise Zadatak 2.58 report with:

- result and both PASS classifications when earned;
- baseline/final HEAD and live marker;
- five enabled-event rows and one disabled-event row;
- original intended-role and final safe-recipient booleans kept distinct;
- stripped recipient-header and single staging-prefix assertions;
- render, placeholder and link assertions;
- normalized URL-host classification without paths/query strings;
- PHPMailer/SMTP and other zero-side-effect counters;
- T0/T1 equality and exact cleanup proof;
- lock acquisition/ownership/reverse release/final free state;
- `MANUAL_BROWSER_CHECK_REQUIRED`;
- production touched: NO.

Release runtime locks after T1, then publish and verify the remote report. STOP. Do not begin hardening, legal, UI or production work automatically.
