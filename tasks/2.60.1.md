# Zadatak 2.60.1 — Permit safe new-file patches in the namespace-free Git helper

Status: READY
Baseline: ff42719f218e440b168b57690b70a2402ee66809
Previous task: 2.60
Target: staging
Production: FORBIDDEN
Time budget: 25 minutes
Risk class: AUTONOMOUS_SAFE
Mutation class: repository-only tooling correction; no deployment or WordPress/runtime mutation

## Objective

Remove the exact tooling blocker reported by Zadatak 2.60: allow `tools/codex-git-apply.sh` to accept the canonical `--- /dev/null` old-path header of a Git new-file patch, while preserving every existing target-path, branch, cleanliness and patch-validation guard.

Required result:

`PASS: NAMESPACE_FREE_NEW_FILE_PATCH_READY`

This task corrects the helper only. It must not implement staging search isolation or touch the WordPress runtime. After this task passes, a separately numbered follow-up will repeat the Zadatak 2.60 outcome against the new baseline.

## Authoritative inputs

Read only:

- `tasks/README.md` and this task from `origin/codex-tasks`;
- current `AGENTS.md`, `CODEX_WORKFLOW.md` and `CODEX_HOST_RESILIENCE.md` from `origin/staging`;
- `reports/20260924T070155Z-zadatak-2_60.md` from `origin/codex-reports`;
- `tools/codex-git-apply.sh` from the declared baseline.

Do not inspect or change application, vendor, theme, plugin, deployment or runtime files.

## Preconditions

Verify:

1. authorized repository `/home/u601262303/repo/raspitajse-wp`;
2. clean worktree;
3. local `staging` and `origin/staging` exactly equal the declared baseline;
4. no active Git operation;
5. Task 2.60 stopped before mutation and its latest report contains `PARTIAL: NAMESPACE_FREE_NEW_FILE_PATCH_BLOCKED`;
6. the current helper rejects the canonical safe new-file patch because it treats exact `--- /dev/null` metadata as a forbidden absolute path.

The sixth check must be performed in a fresh task-private temporary Git repository or with a non-mutating parse/check reproduction. Do not dirty the project worktree merely to reconfirm the known failure.

If the baseline differs, the worktree is dirty, or the observed blocker is materially different, publish one precise `BLOCKED` result and stop.

## Branch and exact change scope

Create `feature/task-2.60.1-new-file-patch-helper` from the exact baseline.

Allowed tracked change:

- `tools/codex-git-apply.sh` only.

Make the smallest auditable change to the patch-metadata guard so that:

- exact line `--- /dev/null` is accepted only as the canonical old path used by Git for creation of a new file;
- the destination path remains subject to all current absolute-path, traversal, `.git`, `wp-config.php` and `public_html` prohibitions;
- `+++ /dev/null` remains rejected, so this correction does not silently authorize deletion patches;
- variants such as `/dev/null/...`, another absolute path, or `/dev/null` in any other metadata role remain rejected;
- `git apply --check --whitespace=error-all` remains mandatory before mutation;
- feature-branch-only, clean-worktree, nonempty-patch and post-apply `git diff --check` behavior remains unchanged.

Do not generalize the helper, loosen another rule, add an editor, or change the approved namespace-free workflow.

## Safe edit path

Attempt the normal patch mechanism at most once if it is available.

If it fails with the known namespace/bubblewrap `ENOSPC` signature, classify `HOST_NAMESPACE_PRESSURE` and immediately use the existing helper to apply the one-file patch to the existing tracked helper. This is valid because modifying an existing file does not require `/dev/null` metadata. Do not retry the failing namespace mechanism or use an unapproved editor.

After either edit path, run:

- `bash -n tools/codex-git-apply.sh`;
- `git diff --check`;
- complete diff inspection proving that only the allowed helper changed.

## Required isolated regression tests

Run the helper from a fresh task-private temporary Git repository on a `feature/*` branch. Do not use the live project worktree as the application target of these tests.

### Positive cases

1. A canonical Git new-file patch containing `new file mode`, exact `--- /dev/null`, and safe `+++ b/<relative-path>` passes `--check-only`.
2. The same class of patch applies successfully and creates exactly the intended safe relative file with the expected content.
3. A safe existing-file modification still passes and applies.

### Negative guard cases

Prove nonzero rejection, with no unintended file created or deleted, for at least:

1. an absolute destination path;
2. a traversal destination;
3. a `.git` destination;
4. a `wp-config.php` destination;
5. a path containing `public_html`;
6. a deletion patch using `+++ /dev/null`;
7. a malformed `/dev/null/...` old path;
8. execution outside a `feature/*` branch;
9. execution with an already dirty worktree.

Use deterministic local fixtures only. Do not include secrets, production-like absolute targets that are actually written, or external network access. Clean up the task-private test repository after recording concise results.

## Integration

If and only if syntax, diff and every required positive/negative test passes:

1. commit the single allowed helper change on the feature branch;
2. push the feature branch without force;
3. fast-forward `staging` to that exact commit and push without force;
4. verify local `staging` and `origin/staging` equal the new commit;
5. verify the project worktree is clean.

No staging deployment is needed or allowed: this helper is repository tooling and the WordPress live marker must remain at `ff42719f218e440b168b57690b70a2402ee66809`. Do not update `blog_public`, create the search-isolation MU plugin, or make HTTP evidence requests.

If any test fails, do not integrate. Restore/leave the project branch clean as appropriate, publish `PARTIAL` with the exact failed guard, and stop. Do not weaken the expected rejection set to obtain PASS.

## Result handling

### PASS

Return:

`PASS: NAMESPACE_FREE_NEW_FILE_PATCH_READY`

The report must include:

- feature and final `staging` commit SHA;
- exact one-file diff summary;
- namespace/apply mechanism used;
- `bash -n` and `git diff --check` results;
- all three positive and nine negative isolated test results;
- live deploy marker unchanged at the declared baseline;
- deployment, WordPress option, database and HTTP mutation/request counts = 0;
- fixtures/business-state/mail/scheduler/payment/cache mutations = 0;
- `Production touched: NO`.

### PARTIAL / BLOCKED / FAIL

Publish one precise result with the failed checkpoint, branch/HEAD, changed-file state, integration state and rollback/cleanup state. Do not implement search isolation or begin another numbered task.

## Final report

Publish exactly one Zadatak 2.60.1 report through the existing `codex-reports` workflow, verify the remote report, and STOP.

