# Zadatak 2.62.1 — Verify official WordPress 7.1.2 against staging runtime

Status: READY
Baseline: f6093c11d71ddc175ebf83fc69453c20aaca5239
Previous task: 2.62
Continues outcome of: 2.62
Target: staging
Production: FORBIDDEN
Time budget: 25 minutes
Risk class: AUTONOMOUS_SAFE
Mutation class: TASK_PRIVATE_DOWNLOAD_AND_READ_ONLY_COMPARISON

## Objective

Resolve the official-reference gate from Task 2.62 by downloading one exact WordPress 7.1.2 archive from WordPress.org into a fresh task-private temporary directory, verifying its published digests, and comparing its canonical core scope byte-for-byte with staging runtime and canonical Git 7.1.1.

Required result: `PASS: OFFICIAL_WORDPRESS_7_1_2_RUNTIME_PARITY_PROVEN`.

This task must not change repository source, staging runtime, deploy state, database, cache or configuration. It does not authorize Task 2.63 reconciliation.

## Fixed official reference

Use only:

- archive: `https://wordpress.org/wordpress-7.1.2.zip`;
- published MD5: `c332b9a5bcab2dea2a263b649bdb2a1c`;
- published SHA-1: `fab6ce3a905a1f4085ffff7fd9920bc693f5dd94`.

These values come from the official WordPress release archive. Do not substitute latest.zip, another locale, a mirror, GitHub, a nightly build, an update transient or runtime files as the reference.

## Authoritative inputs

Read `tasks/README.md`, this task, current operating/safety documents, Task 2.62, `deployment/deploy-staging.sh`, exact `origin/staging`, and only the canonical staging core scope.

Do not read or display `wp-config.php`, secrets, credentials, private data, production paths/database, unrelated logs, `wp-content`, uploads or cache bodies.

## Preconditions

Prove clean local/remote `staging` and live marker at the declared baseline, exact `staging` environment, source version 7.1.1, runtime version 7.1.2, active mail/cron/Action Scheduler guards and no competing Git/rsync/deploy/core-update process.

Reconfirm the Task 2.62 runtime aggregate manifest hash is `8b4f6e4a20b87010eeb3730a533fd5d16119ed49c6342f7f33f5a47f5b26e2a2`. If it changed, publish `BLOCKED: WORDPRESS_RUNTIME_CHANGED_SINCE_2_62` and stop before network transport.

## Network contract

Create one fresh non-symlink task-private directory matching `/tmp/raspitajse-task-2.62.1-*`.

Perform exactly one unauthenticated HTTPS GET for the fixed archive URL with bounded connect/overall timeouts, no cookies, authorization, request body, referrer or site data. Allow at most two redirects and only when every hop remains under `wordpress.org` or `downloads.wordpress.org`. Record sanitized status, final host, redirect count and byte count.

Any other host, TLS failure, non-200 final response, unexpected content type, archive size outside a reasonable WordPress-package bound, or additional request is a hard stop. Do not retry with a mirror or alternate URL.

Required transport counters: approved attempts `1`; actual external requests including redirect hops reported exactly; unexpected requests `0`.

## Verification

1. Require a regular non-symlink downloaded file.
2. Compute MD5, SHA-1 and SHA-256 locally.
3. Require MD5 and SHA-1 to equal the fixed official values before extraction.
4. List the ZIP safely before extraction. Reject absolute paths, `..` traversal, symlinks, devices or entries outside one `wordpress/` prefix.
5. Extract only inside the task-private directory.
6. Prove package `wp-includes/version.php` declares WordPress 7.1.2 and DB version 61833 without reporting unrelated file contents.
7. Build the exact Task 2.62 manifest for official `wp-admin/**`, `wp-includes/**`, and the same 15 canonical top-level files from the deploy script.
8. Compare official 7.1.2 to staging runtime. PASS requires 0 official-only, 0 runtime-only, 0 type differences and 0 content differences, with equal aggregate manifest hashes.
9. Compare official 7.1.2 to canonical Git 7.1.1. Require exactly the six Task 2.62 paths and no others: `license.txt`, `wp-admin/about.php`, `wp-includes/images/crystal/license.txt`, `wp-includes/js/codemirror/csslint.js`, `wp-includes/template.php`, and `wp-includes/version.php`.
10. Confirm no canonical scope symlink/non-regular entry.

If official/runtime differ, return `BLOCKED: STAGING_7_1_2_IS_NOT_OFFICIAL_PACKAGE_PARITY` with exact path/count groups and do not prepare reconciliation. If official/Git differs outside the six paths, return `BLOCKED: WORDPRESS_7_1_2_EXPECTED_DIFF_MISMATCH`.

## Cleanup

After all evidence hashes/counts are recorded, remove only the exact task-private directory and prove it absent. Do not retain the archive, extracted package or manifest files on the server. Cleanup failure is `PARTIAL` and must name the remaining task-private path without broad deletion.

## Zero-mutation contract

Required counters: repository/runtime changes `0`; Git commits/branches/merges/pushes `0`; deploy/snapshot/restore `0`; WordPress/database/cache mutations `0`; fixtures `0`; scheduler/mail/payment execution `0`; production access `0`.

Do not acquire mutation locks because no staging mutation is authorized.

## Follow-up boundary

On PASS, confirm the Task 2.62 decision becomes `ADOPT_7_1_2_IN_CANONICAL_SOURCE` and restate the smallest executable Task 2.63 contract, including the occupied fixed snapshot-slot blocker and the requirement to preserve both old and fresh rollback material. Do not create a branch, import files, alter deployment tooling, snapshot, deploy or integrate.

## Report

Publish exactly one Zadatak 2.62.1 report through `codex-reports`, verify it remotely, and STOP.

Report archive URL/final host, published and computed digests, sanitized transport counters, ZIP safety result, official/runtime/Git manifest hashes and delta counts/paths, cleanup, follow-up decision, final source/marker/runtime/guard state, retained HCDN and WP Private Message blockers, and `Production touched: NO`.

