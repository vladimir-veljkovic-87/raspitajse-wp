# Zadatak 2.57 — Close transactional mail evidence and final launch-critical E2E gate

Status: READY
Baseline: ff42719f218e440b168b57690b70a2402ee66809
Previous task: 2.56.1
Target: staging
Production: FORBIDDEN
Time budget: 30 minutes
Mode: focused disposable-fixture mail-render validation; no application-code changes

## Required result

Close the only incomplete gate from Task 2.56.1: per-message validation of launch-critical transactional email renders while transport remains blocked.

Task 2.56.1 already proved and exactly cleaned:

- authentication and role boundaries;
- three-active-job quota and slot reuse;
- candidate application and duplicate prevention;
- employer isolation;
- scheduler continuity;
- source/live/deploy alignment;
- exact T0/T1 business-state equality and zero prohibited side effects.

Do not repeat those journeys. Read the exact report:

`reports/20260920T052423Z-zadatak-2_56.md`

The reported blocker was evidence incompleteness in a temporary validator. The first registration message contained a valid staging application link plus an external `plus.google.com` template/social link. The validator incorrectly required every HTTP host to equal the staging host. No product defect was established.

PASS classifications:

`PASS: TRANSACTIONAL_MAIL_GATE_READY`

and, by combining this result with unchanged Task 2.56.1 evidence:

`PASS: LAUNCH_CRITICAL_E2E_READY`

Do not modify application code to obtain PASS. A real render defect, recipient defect, production link, unresolved placeholder, transport escape or cleanup failure is one precise BLOCKED result.

## Lean execution rules

Read only:

- `tasks/README.md`;
- this task;
- the exact Task 2.56.1 report above;
- current mail-safety and Raspitajse Communications code;
- the exact registration, password-reset, job-publication and application message producers needed by this test.

Do not reread broad historical reports, rerun the quota/application/scheduler suites, inspect unrelated plugins, deploy, or create persistent test tooling.

Use one fresh private directory matching `/tmp/raspitajse-task-2.57.*`. The server `/tmp` mount is known to be `noexec`; invoke validated PHP/shell files explicitly through their interpreter. This is expected environment behavior and is not a correction attempt.

Maximum one correction for a genuine temporary harness/bootstrap error. If a command produces no useful output for 10 minutes, stop it safely, clean exact fixtures and report the checkpoint.

## Preconditions

Before fixtures, require:

- authorized Hostinger execution context;
- repository `/home/u601262303/repo/raspitajse-wp`;
- WordPress path `/home/u601262303/domains/raspitajse.com/public_html/public_html_stage`;
- local `staging`, `origin/staging` and live deploy marker all equal the declared baseline;
- clean Hostinger worktree;
- WordPress environment exactly `staging`;
- WordPress `7.1.1`, WPForms Lite `2.0.2`, active child theme `superio-child`;
- Raspitajse Communications and Commerce loaded;
- staging mail-safety loaded and fail-closed;
- no source/deploy drift since Task 2.56.1;
- both lock files currently acquirable nonblocking;
- no running owned scheduler process overlaps the test.

The unrelated Windows/client checkout is outside this runtime precondition and must remain untouched.

If any authoritative precondition differs, STOP before mutation and report the exact mismatch.

## Exact lock protocol

One persistent worker performs the complete fixture phase.

Acquire nonblocking in this exact order:

1. `/tmp/raspitajse-staging-owned-cron-runner.lock`;
2. `/tmp/raspitajse-staging-runtime.lock`.

The equivalent descriptor protocol is:

```bash
exec 9>/tmp/raspitajse-staging-owned-cron-runner.lock
flock -n 9 || exit with BLOCKED: STAGING_LOCK_BUSY

exec 8>/tmp/raspitajse-staging-runtime.lock
flock -n 8 || {
  flock -u 9
  exit with BLOCKED: STAGING_LOCK_BUSY
}
```

The same worker must retain both descriptors from before fixture T0 through mail rendering, exact cleanup and T1 verification.

Release in reverse order:

1. runtime/mutation descriptor 8;
2. owned-runner descriptor 9.

Never delete, rename or chmod either lock file. Never kill another holder, wait in a polling loop, disable the Hostinger cron entry or run the scheduler after release.

## Safety setup and fixtures

Before any message-producing callback:

- install a final-priority `pre_wp_mail` interceptor that captures the structured message in process memory and returns false before PHPMailer;
- instrument PHPMailer/SMTP entry and require zero calls;
- block payment, refund and gateway execution;
- block outbound WordPress HTTP transport before transport and count attempts/intercepts separately;
- block broad WP-Cron, Action Scheduler and owned scheduler callback execution;
- create an exact ledger before each object is used.

Create only the minimum collision-resistant disposable staging fixtures needed to reach the enabled producers:

- one synthetic employer and reciprocal employer profile;
- one synthetic candidate and reciprocal candidate profile;
- one valid synthetic job only when required for job/application messages;
- one application only when required for application messages.

Use reserved non-deliverable addresses and credentials held only in memory. Use normal WordPress/WP Job Board Pro/Raspitajse APIs. Do not use raw SQL for fixture creation or deletion. Do not upload a CV or other file.

Password reset may target only the ledgered disposable candidate. Never print or persist a password, cookie, nonce, reset key/token, address, raw subject or raw message body.

## Message coverage

Trigger the minimum normal producer path for each currently enabled launch-critical event:

1. candidate registration/welcome;
2. employer registration/welcome;
3. password-reset request/render for the disposable candidate;
4. employer notification of the candidate application;
5. candidate application confirmation or status notification, when enabled;
6. job submission/publication notification, when enabled.

An event may be recorded as `DISABLED_CONFIRMED` only when the current hook/setting proves it is intentionally disabled. Do not enable a feature for this test.

Do not use browser CAPTCHA automation. Registration identities may be created through the normal server-side API path already accepted for disposable staging fixtures. Record browser CAPTCHA as `MANUAL_BROWSER_CHECK_REQUIRED`.

For every intercepted message, validate and report:

- event label;
- intended recipient role;
- actual recipient belongs only to the expected ledgered fixture role;
- non-empty subject;
- non-empty body;
- no unresolved template placeholder;
- required application/action link exists when the template contract requires one;
- every internal application/action link uses `stage.raspitajse.com`;
- no link uses production hosts `raspitajse.com` or `www.raspitajse.com`;
- no pricing, package, cart, checkout or payment link appears unless explicitly required by the current free-launch policy;
- interception occurred at `pre_wp_mail`;
- PHPMailer and SMTP were not entered.

Report role labels, booleans, URL hostnames and hashes only. Do not report recipients, paths containing reset tokens, query strings, raw subjects or bodies.

## URL classification

Do not require every URL hostname in an email template to equal the staging hostname.

Classify URLs as follows:

- internal application/action URL: must use `stage.raspitajse.com`;
- production application URL: any URL using exactly `raspitajse.com` or `www.raspitajse.com` is BLOCKED;
- external presentation/social/template URL: may be present and is not a failure solely because its hostname is external;
- external action URL, credential-bearing URL, or link containing fixture/private data: BLOCKED unless the current product contract explicitly requires and safely explains it.

Do not request or follow any rendered URL. Record only normalized hostnames and the classification. In particular, the previously observed `plus.google.com` link is an external presentation/social/template link, not an application-host failure.

## Cleanup and equality

Cleanup is required even when a render assertion fails.

Delete only ledgered objects through normal APIs in dependency-safe order:

1. application;
2. job;
3. employer/candidate profiles;
4. temporary users.

After cleanup require:

- exact T0/T1 counts for users, employer profiles, candidate profiles, jobs and applications;
- no residue by any ledgered ID;
- compact non-content business-state hash unchanged;
- plugin/theme set unchanged;
- owned cron contracts unchanged;
- Action Scheduler item 32733 status/attempts unchanged;
- source HEAD and live deploy marker unchanged;
- worktree clean.

If exact cleanup fails, make at most one bounded correction using only ledgered IDs. Never use broad deletion.

## Required zero-side-effect state

- real PHPMailer/SMTP transport: 0;
- actual outbound WordPress HTTP transport: 0;
- payment/gateway/refund execution: 0;
- broad WP-Cron execution: 0;
- Action Scheduler execution: 0;
- owned scheduler callback execution: 0;
- source/deploy mutation: 0;
- production access: 0.

Inbound Git/report traffic is outside the WordPress outbound counter. No rendered link may be requested.

## Result handling

### PASS

Return both:

`PASS: TRANSACTIONAL_MAIL_GATE_READY`

`PASS: LAUNCH_CRITICAL_E2E_READY`

only when every enabled event has an individual PASS row, every disabled event is proven disabled, cleanup/T1 equality is exact, the baseline is unchanged and all prohibited side-effect counters are zero.

### BLOCKED

Return one precise blocker, clean all exact fixtures, prove final state and STOP. Do not repair application code or broaden the test.

## Final report

Publish one concise Zadatak 2.57 report containing:

- result and both classifications when PASS;
- baseline/final HEAD and live marker;
- exact execution context;
- per-event table including `PASS` or `DISABLED_CONFIRMED`;
- per-message recipient-role/render/link/interception assertions;
- normalized URL hostname/classification inventory;
- explicit treatment of `plus.google.com`;
- enabled/disabled event evidence;
- T0/T1 counts and compact equality hashes;
- exact cleanup proof;
- lock acquisition, continuous ownership, reverse-order release and final free state;
- zero-side-effect counters;
- `MANUAL_BROWSER_CHECK_REQUIRED` for CAPTCHA;
- production touched: NO.

Write and verify the remote report after releasing the two runtime locks. STOP. Do not begin UI, legal, hardening or production work automatically.
