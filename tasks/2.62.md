# Zadatak 2.62 — Identify the WordPress runtime drift and reconciliation boundary

Status: READY
Baseline: f6093c11d71ddc175ebf83fc69453c20aaca5239
Previous task: 2.61
Advances readiness items: D5, D6, F1
Target: staging
Production: FORBIDDEN
Time budget: 30 minutes
Risk class: AUTONOMOUS_SAFE
Mutation class: READ_ONLY_DIAGNOSIS

## Objective

Explain exactly why the marker-aligned staging runtime reports WordPress `7.1.2` while canonical `origin/staging` contains WordPress `7.1.1`, and define the smallest safe reconciliation contract without changing either source or runtime.

Required result: `PASS: WORDPRESS_RUNTIME_DRIFT_BOUNDARY_IDENTIFIED`.

This task diagnoses reproducibility only. Do not update, downgrade, copy, delete, deploy, snapshot, restore or commit WordPress files.

## Authoritative inputs

Read `tasks/README.md`, this task, current operating/safety/DoD documents, Task 2.53, Task 2.61, `deployment/deploy-staging.sh`, exact current `origin/staging`, deploy-state metadata and the staging WordPress core paths.

Do not read or display `wp-config.php`, credentials, salts, database connection values, private/user data, production files, production database, unrelated logs or cache bodies. External network requests and WordPress checksum downloads are forbidden in this task; use local Git/runtime evidence only.

## Preconditions

Prove the authorized repository, clean local/remote `staging` at the declared baseline, live marker equal to it, exact `staging` environment, active mail/cron/Action Scheduler guards and no competing Git/rsync/deploy/update process.

Confirm, read-only, canonical source version and runtime-reported version. If source/runtime no longer show the 7.1.1/7.1.2 split, report the exact new state and stop without repair.

## Required diagnosis

### 1. Exact source/live core manifest

Compare only the canonical core scope managed by `deployment/deploy-staging.sh`:

- `wp-admin/**`;
- `wp-includes/**`;
- the exact canonical top-level WordPress files listed by the deploy script.

Produce deterministic source and runtime manifests using relative paths, file type, size and SHA-256. Do not include file contents.

Report:

- source-only, runtime-only and content-different counts;
- exact changed-path list, grouped by `wp-admin`, `wp-includes` and root files;
- whether any symlink or non-regular file exists in canonical scope;
- whether runtime files consistently identify one WordPress version or a mixed package;
- source and runtime aggregate manifest hashes.

Do not recursively inspect `wp-content`, uploads, cache, configuration, production or unrelated document-root paths.

### 2. Deployment-marker truth boundary

Establish whether the deploy marker records only the last successful repository deploy or can prove current core byte parity. Trace the exact marker-write behavior and package reconciliation conditions in `deployment/deploy-staging.sh`.

Determine whether the current marker can remain `f6093c11...` after an out-of-band WordPress core update and identify the exact false assurance this creates for later tasks.

### 3. Drift origin classification

Using safe local metadata and configuration-state booleans only, classify exactly one:

- `WORDPRESS_AUTOMATIC_CORE_UPDATE`;
- `HOSTINGER_MANAGED_CORE_UPDATE`;
- `MANUAL_OR_OTHER_OUT_OF_BAND_UPDATE`;
- `PARTIAL_OR_FAILED_PACKAGE_RECONCILIATION`;
- `UNRESOLVED`.

Allowed evidence: safe WordPress version/update state, file mtimes summarized as ranges, package/deploy history, update-related metadata that contains no credentials/private data, and relevant non-secret WordPress constants reported only as booleans/enums.

Do not trigger update checks, cron, HTTP, package downloads, cache purge, plugin/theme updates or database writes. Do not claim an origin from timestamps alone; state confidence and competing explanations.

### 4. Safe reconciliation decision

Select and justify one evidence-based target:

- `ADOPT_7_1_2_IN_CANONICAL_SOURCE`;
- `RESTORE_RUNTIME_TO_CANONICAL_7_1_1`;
- `BLOCKED_PENDING_OFFICIAL_PACKAGE_REFERENCE`.

The decision must account for current runtime stability, Task 2.53 rollback material, exact canonical package availability, plugin/theme compatibility evidence, deployment behavior and reversibility. Do not execute it.

Define the smallest follow-up task contract with exact source/reference required, feature-branch file scope, package snapshot/rollback, lock order if runtime mutation is later authorized, syntax/static checks, deploy command, focused smoke, final source/runtime/marker parity and stop conditions.

## Zero-mutation contract

Required counters: source/runtime files changed `0`; Git commits/branches/merges/pushes `0`; deploy/snapshot/restore operations `0`; WordPress/database/cache mutations `0`; update checks/downloads `0`; fixtures `0`; HTTP requests `0`; scheduler/mail/payment execution `0`; production access `0`.

Do not acquire mutation locks. If exact comparison would require mutation, network transport, production access or secret disclosure, stop and report the missing fact.

## Acceptance

PASS requires complete canonical-scope manifests, exact path/count deltas, marker semantics, one drift-origin classification with confidence, one reconciliation decision, and an executable but unperformed follow-up contract.

Use `BLOCKED: WORDPRESS_RUNTIME_DRIFT_BOUNDARY_NOT_PROVABLE` only when the canonical scope or source/live manifests cannot be safely established.

## Report

Publish exactly one Zadatak 2.62 report through `codex-reports`, verify it remotely, and STOP.

Report all manifest hashes/counts/path groups, version consistency, marker truth boundary, origin classification, reconciliation decision, follow-up contract, zero-mutation counters, final source/marker/runtime/guard state, retained HCDN blocker from 2.60.5, retained WP Private Message D1 finding from 2.61, and `Production touched: NO`.

Do not reconcile core, modify vendor code, deploy or begin another task.

