# Zadatak 2.60.5 — Identify the exact robots cache owner and narrow eviction path

Status: READY
Baseline: f6093c11d71ddc175ebf83fc69453c20aaca5239
Previous task: 2.60.4
Continues outcome of: 2.60
Target: staging
Production: FORBIDDEN
Time budget: 30 minutes
Risk class: AUTONOMOUS_SAFE
Mutation class: READ_ONLY_DIAGNOSIS

## Objective

Identify, without mutating cache or runtime state, the exact component that continued to serve the cached empty staging `/robots.txt` response in Task 2.60.4 and the smallest technically valid staging-only mechanism that can evict only that URL.

Required PASS result:

`PASS: ROBOTS_CACHE_OWNER_AND_NARROW_EVICTION_PATH_IDENTIFIED`

This task is diagnosis and contract design only. Do not retry the isolation deployment, remove `robots.txt`, change `blog_public`, invoke a purge action, issue an HTTP request, or integrate any source.

## Authoritative evidence and reads

Read only:

- `tasks/README.md` and this task from `origin/codex-tasks`;
- `AGENTS.md`, `CODEX_HOST_RESILIENCE.md`, `STAGING_MUTATION_POLICY.md`, `STAGING_DEFINITION_OF_DONE.md`, and directly relevant sections of `CODEX_WORKFLOW.md` from the exact baseline;
- the latest Task 2.60.4 report and, only where it supplies a directly referenced fact, Task 2.60.3;
- the active staging LiteSpeed Cache plugin's directly relevant purge, URL-normalization, header-emission and WP-CLI integration source files;
- staging-only web/cache configuration and file metadata needed to locate the owner of the exact `/robots.txt` response.

Do not read `wp-config.php`, credentials, secrets, production configuration, production cache state, unrelated cache bodies, private/user-specific cached content, or broad logs. Do not recursively dump cache directories. Record only sanitized class/function names, relevant configuration flags, paths relative to the staging/cache root, hashes, timestamps and conclusions.

## Preconditions

Prove before diagnosis:

- authorized repository is `/home/u601262303/repo/raspitajse-wp` and staging root is `/home/u601262303/domains/raspitajse.com/public_html/public_html_stage`;
- local `staging` and `origin/staging` equal the declared baseline and the worktree is clean;
- live deploy marker equals the baseline;
- WordPress environment is exactly `staging`;
- `blog_public=1`, the isolation MU plugin is absent, and the physical staging-root `robots.txt` is the restored regular non-symlink empty file with SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`;
- mail-safety, owned-cron recovery and Action Scheduler drop protection remain active;
- no active Git, rsync or deployment process.

If any source/runtime precondition differs, publish one precise `BLOCKED` result and stop. Do not repair or reconcile it.

## Required diagnosis

### 1. Trace the failed purge path

Using source inspection and safe reflection only, establish the complete call chain for:

`do_action( 'litespeed_purge_url', <canonical staging robots URL> )`

Record:

- registered callback and defining file;
- exact URL normalization and cache-key/tag derivation;
- whether the callback deletes a cache object directly or merely queues/emits a LiteSpeed response header/directive;
- the execution phase/context in which the LiteSpeed web server consumes that directive;
- whether invoking it from WP-CLI can transmit the required directive to the serving web server;
- why Task 2.60.4 could observe the PHP callback while the public cache entry remained intact.

Do not invoke the action, its callback, a CLI purge command, or any equivalent purge method.

### 2. Classify the serving cache owner

Use the recorded Task 2.60.4 public response metadata plus read-only staging evidence to classify exactly one owner:

- `LITESPEED_STAGING_VHOST_PAGE_CACHE`;
- `HOSTINGER_OR_UPSTREAM_EDGE_CACHE`;
- `OTHER_EXACTLY_NAMED_OWNER`;
- `UNRESOLVED`.

For LiteSpeed/local ownership, identify the staging-only vhost/cache namespace and, if safely derivable without reading unrelated cache content, the exact key or relative artifact family for `/robots.txt`. For upstream ownership, identify the exact layer and available URL-scoped API/control path without accessing production or credentials.

Do not infer ownership solely from the plugin being active. Tie the conclusion to the response metadata, call-chain semantics and staging-only configuration/cache evidence.

### 3. Select one narrow eviction mechanism

Identify one mechanism that is all of the following:

- exact to `https://stage.raspitajse.com/robots.txt`;
- limited to the staging vhost/cache namespace;
- does not purge home, tags, posts, object cache, the whole vhost, the whole domain or production;
- can be bounded to one invocation and audited;
- requires no credential disclosure and no control-panel mutation;
- does not create a reusable public purge endpoint;
- has a clear success signal and a rollback/failure boundary.

Prefer a vendor-supported URL-specific mechanism. If the mechanism requires a normal staging HTTP execution context so that a LiteSpeed purge response directive reaches the web server, specify a one-shot, task-owned, collision-resistant, loopback-only or otherwise non-publicly-reusable trigger contract; specify authentication, request count, allowed host, redirect policy, cleanup and proof that the directive targets only `/robots.txt`. Do not implement or call it in this task.

Direct deletion of a cache file is acceptable only as a proposed mechanism if source/config evidence proves a unique staging-only artifact for exactly this URL and proves deletion cannot select another URL, vhost or private cache object. Otherwise reject direct deletion.

If no exact mechanism can be proven, return:

`BLOCKED: NARROW_ROBOTS_CACHE_EVICTION_PATH_NOT_PROVEN`

Name the missing fact or required human/Hostinger capability. Do not substitute purge-all, cache flush, crawler/warm, wildcard invalidation, repeated GETs or control-panel experimentation.

## Zero-mutation and network contract

Allowed operations are read-only Git, source, reflection, configuration and narrowly scoped file-metadata inspection plus report publication.

Required counters:

- public/local HTTP requests: `0`;
- LiteSpeed purge/invalidation action calls: `0`;
- cache files changed/deleted: `0`;
- WordPress options/database rows changed: `0`;
- deploys: `0`;
- source commits/merges/pushes: `0`;
- production access: `0`.

Do not acquire mutation locks because no staging mutation is authorized. If diagnosis unexpectedly requires mutation or network transport, stop and report the exact boundary instead of broadening scope.

## Required follow-up contract

On PASS, define the smallest executable Task 2.60.6 contract, but do not execute it. It must state:

- exact owner and exact invalidation mechanism;
- the standard nonblocking lock order: owned-cron lock on fd 9, then runtime lock on fd 8; release fd 8, then fd 9;
- exact temporary deployment/removal/`blog_public` sequence already proven in 2.60.4;
- exact purge-trigger installation, single invocation, success evidence and mandatory removal;
- maximum HTTP request counts separated into purge trigger and final acceptance;
- exactly one normal public home GET and one normal public robots GET for acceptance, with no redirects, query cache-busting or request no-cache headers;
- integration only after both acceptance responses pass;
- rollback to baseline, restored empty `robots.txt`, `blog_public=1`, plugin absent and exact cleanup if any checkpoint fails;
- prohibition of broad purge/flush, production, business state, mail, scheduler and payment mutation.

Do not authorize Task 2.60.6 from this report; only record its evidence-based contract for the task author.

## Report

Publish exactly one Zadatak 2.60.5 report through `codex-reports`, verify it remotely, and STOP.

For PASS report the exact owner classification, callback/call chain, why WP-CLI did not evict the public entry, selected URL-specific mechanism, evidence that it is staging-only and exact, proposed 2.60.6 request/mutation/cleanup bounds, all zero-mutation counters, final baseline/marker/runtime/guard state, and `Production touched: NO`.

For BLOCKED/PARTIAL report the exact unresolved ownership or invalidation fact, all zero-mutation counters, unchanged final state, and the smallest external fact/capability needed. Do not begin another task.
