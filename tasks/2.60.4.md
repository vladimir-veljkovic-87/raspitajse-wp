# Zadatak 2.60.4 — Remove the staging robots bypass and refresh exact isolation URLs

Status: READY
Baseline: f6093c11d71ddc175ebf83fc69453c20aaca5239
Previous task: 2.60.3
Completes outcome of: 2.60
Target: staging
Production: FORBIDDEN
Time budget: 35 minutes
Risk class: AUTONOMOUS_SAFE
Mutation class: bounded staging technical configuration, one exact static-file removal, and two URL-scoped cache invalidations

## Objective

Close Definition-of-Done criterion E1 using the two exact response owners proven by Task 2.60.3:

1. remove only the confirmed zero-byte physical staging `robots.txt` so WordPress owns `/robots.txt`;
2. invalidate only the LiteSpeed cache entries for the staging home URL and staging robots URL;
3. validate the already-tested isolation feature with exactly two normal public GET requests;
4. integrate the feature only after all public acceptance checks pass.

Required result:

`PASS: STAGING_SEARCH_ENGINE_ISOLATION_READY`

## Authoritative evidence and source

Read `tasks/README.md`, this task, current safety/workflow/mutation/DoD documents, the latest Task 2.60.3 report, `deployment/deploy-staging.sh`, LiteSpeed's directly relevant `litespeed_purge_url` implementation, and exact feature commit `783cc6b3425985c1e27de8e823fca020d0d7b869` only.

Task 2.60.3 established:

- WordPress hooks and the feature implementation are correct;
- normal home is a stale local LiteSpeed hit;
- a no-cache diagnostic home request exposes the correct header/meta contract;
- physical staging-root `robots.txt` is a regular non-symlink zero-byte file with the empty-file SHA-256 and bypasses WordPress;
- local/public static probes proved the same staging document root;
- no source correction is required.

Do not modify or amend the existing feature commit.

## Preconditions

Verify all of the following before mutation:

- authorized Hostinger repository `/home/u601262303/repo/raspitajse-wp` and staging root `/home/u601262303/domains/raspitajse.com/public_html/public_html_stage`;
- clean local/remote `staging` at the declared baseline and live marker equal to it;
- WordPress environment exactly `staging`, `blog_public=1`, and isolation MU plugin absent;
- `origin/feature/task-2.60.2-staging-search-isolation` exactly equals `783cc6b3425985c1e27de8e823fca020d0d7b869`, is a direct child of the baseline, and changes only the isolation MU plugin;
- exact staging-root `robots.txt` exists, is a regular non-symlink zero-byte file, and has the SHA-256 of an empty file;
- LiteSpeed Cache is active and the `litespeed_purge_url` action is registered to the active plugin implementation;
- mail-safety, cron-recovery and Action Scheduler guards are active;
- no active Git, rsync or deploy process.

If any fact differs, publish one precise `BLOCKED` result and stop. Never delete, overwrite or rename a robots file whose type, size or hash differs.

## Allowed mutations

Allowed only:

- deploy the exact existing feature commit through `deployment/deploy-staging.sh changed feature/task-2.60.2-staging-search-isolation`;
- set staging `blog_public` from its captured value to `0`;
- copy the exact empty robots file into a fresh task-private `/tmp/raspitajse-task-2.60.4-*` rollback directory while preserving metadata, then remove only the exact original file;
- call WordPress action `litespeed_purge_url` once for the exact canonical staging home URL and once for the exact canonical staging `/robots.txt` URL;
- perform exactly two normal public acceptance GETs;
- on PASS, fast-forward staging to the exact feature commit;
- on any failure, perform the exact rollback below.

Forbidden: production; source edits/new commits; purge-all, flush-all, wildcard/tag/post/type/front-page/broad purge actions; object-cache flush; crawler/warm/optimization; control-panel, web-server, `.htaccess` or `wp-config.php` changes; any other file/option/database mutation; business fixtures/state; mail, cron, Action Scheduler or payment execution; redirects; cache-busting queries; request no-cache headers; any other host.

## Lock procedure and mutation

Acquire nonblocking in this exact order:

1. `/tmp/raspitajse-staging-owned-cron-runner.lock` on fd 9;
2. `/tmp/raspitajse-staging-runtime.lock` on fd 8.

If either is unavailable, release any acquired lock and return `BLOCKED: STAGING_MUTATION_LOCK_UNAVAILABLE`.

While both locks are continuously held:

1. re-prove every mutation precondition;
2. record exact `blog_public`, robots path/type/mode/owner/group/mtime/size/hash, marker and guard state;
3. create a private rollback directory and preserve the exact robots file plus its metadata; verify backup hash before removal;
4. deploy the exact feature branch and prove exactly its MU plugin was synced;
5. set only `blog_public=0` and verify effective value;
6. re-check the original robots file is still the exact expected empty non-symlink, then remove only that file and prove the path absent;
7. call `do_action( 'litespeed_purge_url', <exact canonical home URL> )` once;
8. call `do_action( 'litespeed_purge_url', <exact canonical robots URL> )` once;
9. prove no broad LiteSpeed purge/flush action was invoked and guards/environment remain valid;
10. release fd 8, then fd 9.

Do not hold locks while making the two acceptance GETs or formatting evidence. The two URL-scoped invalidations are intentional bounded technical cache mutations; no other cache operation is authorized.

## Final public acceptance

Make exactly two unauthenticated normal GET requests:

1. `https://stage.raspitajse.com/`;
2. `https://stage.raspitajse.com/robots.txt`.

Use bounded timeouts, no cookies/auth/user data, zero redirects, no query strings, and no `Cache-Control`/`Pragma` request override.

PASS requires:

- both return successful non-redirect staging responses;
- home `X-Robots-Tag` contains `noindex` and `nofollow`;
- rendered home robots meta contains `noindex`;
- robots response `X-Robots-Tag` contains `noindex` and `nofollow`;
- robots body contains global `User-agent: *` and `Disallow: /`, with no sitemap;
- effective `blog_public=0`;
- physical staging-root robots file remains absent;
- deployed plugin hash matches the exact feature commit;
- guards remain active and no prohibited side effect occurred.

Record relevant cache response headers, but do not perform a third request. Do not weaken acceptance because a cache or static layer still wins.

## Integration

Only after full acceptance:

1. fast-forward local `staging` to `783cc6b3425985c1e27de8e823fca020d0d7b869`;
2. push `staging` without force;
3. verify local `staging`, `origin/staging` and live marker all equal that commit;
4. verify clean worktree, staging environment, `blog_public=0`, plugin parity, robots file absent and guards active;
5. remove the task-private empty-file backup only after remote integration and final state are proven.

The intended PASS final state includes deliberate removal of the zero-byte staging-only `robots.txt`; WordPress now owns the endpoint. This file contained no data and is recoverable from its captured metadata or as an empty file.

## Mandatory rollback on any failure

If acceptance or integration fails, reacquire locks in the same order and:

1. restore the exact empty robots file atomically with its captured mode/owner/group/mtime and hash;
2. restore the exact prior `blog_public` value;
3. deploy `changed staging` from the baseline to remove the isolation plugin and restore marker;
4. invoke only the same two exact URL-scoped `litespeed_purge_url` actions so failed diagnostic content cannot remain cached;
5. verify baseline marker/source, plugin absent, robots metadata/hash restored, option restored, environment/guards valid and clean worktree;
6. release fd 8, then fd 9.

If exact rollback fails, return `FAIL` and state the precise remaining staging-only technical state. Do not fix forward or integrate.

## Report

For PASS report feature/final/deploy SHA, exact file removal evidence, pre/post option state, the two exact URL-scoped invalidations, zero broad cache actions, exactly two GETs/zero redirects/one allowed host, header/meta/robots results, final marker/guards, zero fixtures/business/mail/scheduler/payment mutations, rollback state, and `Production touched: NO`.

For non-PASS report the failed checkpoint and exact restoration state. Publish exactly one Zadatak 2.60.4 report through `codex-reports`, verify it remotely, and STOP. Do not start another task.

