# Zadatak 2.60 — Enforce and verify staging search-engine isolation

Status: READY
Baseline: ff42719f218e440b168b57690b70a2402ee66809
Previous task: 2.59
Target: staging
Production: FORBIDDEN
Time budget: 40 minutes
Risk class: AUTONOMOUS_SAFE
Mutation class: bounded staging technical configuration/code mutation; no protected business-state mutation

## Objective

Close Definition-of-Done criterion E1 by making the staging site unambiguously non-indexable at both WordPress and HTTP levels.

Task 2.59 proved that the current staging candidate is source/live aligned but unsafe for search isolation:

- effective `blog_public=1`;
- the staging home response did not contain a `noindex` signal;
- `/robots.txt` did not disallow crawling.

Required result:

`PASS: STAGING_SEARCH_ENGINE_ISOLATION_READY`

Production must not be accessed or changed.

## Authoritative inputs

Read only:

- `tasks/README.md` and this task from `origin/codex-tasks`;
- current `AGENTS.md`, `CODEX_WORKFLOW.md`, `CODEX_HOST_RESILIENCE.md`, `STAGING_MUTATION_POLICY.md` and `STAGING_DEFINITION_OF_DONE.md` from `origin/staging`;
- `reports/20260924T062335Z-zadatak-2_59.md` from `origin/codex-reports` for the E1 finding;
- the current deploy script and directly relevant owned MU-plugin patterns.

Do not reopen unrelated readiness gaps or rerun earlier E2E, mail, scheduler, quota or application tests.

## Preconditions

Before any change, verify:

1. authorized Hostinger context;
2. repository `/home/u601262303/repo/raspitajse-wp`;
3. clean worktree;
4. `origin/staging` exactly equals the declared baseline;
5. live deploy marker exactly equals the baseline;
6. WordPress path `/home/u601262303/domains/raspitajse.com/public_html/public_html_stage` reports environment type exactly `staging`;
7. no active Git, rsync or `deployment/deploy-staging.sh` process;
8. staging mail-safety and protected scheduler guards remain present;
9. the current effective `blog_public` value and the two bounded HTTP observations agree with, or safely supersede, Task 2.59.

If source/live alignment differs, the environment is not exactly staging, the worktree is dirty, a relevant deploy is active, or a safety guard is missing, return one precise `BLOCKED` result. Do not repair, rebase or broaden scope.

## Branch and file scope

Create `feature/task-2.60-staging-search-isolation` from the exact baseline.

The intended source change is one small Raspitajse-owned MU plugin:

`wp-content/mu-plugins/raspitajse-staging-search-isolation.php`

It must:

- do nothing unless `wp_get_environment_type()` is exactly `staging`;
- add a deterministic `X-Robots-Tag` response value containing at least `noindex` and `nofollow` for public staging responses;
- force WordPress robots metadata to contain at least `noindex` and `nofollow`;
- make the dynamic staging `robots.txt` response contain a global `User-agent: *` plus `Disallow: /` rule without advertising a sitemap;
- coexist with WordPress core and active SEO/cache plugins without modifying their files;
- contain no hostname, recipient, credential, secret or production behavior other than the staging environment gate.

Use the smallest relevant WordPress hooks. Do not edit WordPress core, Superio, WP Job Board Pro, All in One SEO, LiteSpeed Cache or any other vendor tree. Do not add a general SEO subsystem. One directly relevant lightweight test file is allowed only if needed to prove hook output before deployment.

Do not implement search isolation only through an untracked runtime file or control-panel setting. The HTTP enforcement must be versioned and reproducible.

## Allowed mutations

Allowed:

- the scoped feature-branch source change above;
- one staging option mutation: set only `blog_public` to `0`, after proving the environment is staging and recording its exact previous value;
- changed-file deployment of the feature commit to the exact staging WordPress path through `deployment/deploy-staging.sh`;
- fast-forward integration of the validated feature commit into `staging` and push without force;
- the normal deployment-marker update performed by the approved deploy script;
- task-private `/tmp/raspitajse-*` evidence files and the normal report publication.

Forbidden:

- production access, production HTTP requests, deployment or database use;
- any database mutation except the single `blog_public` option update;
- users, jobs, applications, messages, orders, payments or other business fixtures;
- mail, WP-Cron, Action Scheduler or payment execution;
- cache purge, flush, warm, crawler or optimization execution;
- `.htaccess`, `wp-config.php`, web-server/control-panel, vendor or core edits;
- following redirects or contacting any host other than `stage.raspitajse.com` for HTTP evidence;
- force push, history rewrite, broad cleanup or recursive production-adjacent filesystem commands.

## Static validation

Before deployment:

1. run `php -l` on every changed PHP file;
2. inspect the complete diff and `git diff --check`;
3. prove the non-staging path registers no search-isolation behavior with the smallest safe static/unit check practical;
4. prove the staging path produces the required robots directives/body/header behavior without loading or modifying live WordPress when practical;
5. confirm the feature diff contains only the allowed file set and no secrets, generated files or production paths;
6. commit and push the feature branch without force.

If the normal patch helper hits the known namespace `ENOSPC` signature, follow `CODEX_HOST_RESILIENCE.md`: classify `HOST_NAMESPACE_PRESSURE`, attempt it at most once, then use the approved namespace-free repository patch helper. Rerun all syntax/diff checks after the fallback. If equivalence cannot be preserved, report `PARTIAL` and stop before runtime mutation.

## Locked staging mutation

Acquire both locks nonblocking in this exact order:

1. `/tmp/raspitajse-staging-owned-cron-runner.lock` on fd 9;
2. `/tmp/raspitajse-staging-runtime.lock` on fd 8.

If either lock is unavailable, release any acquired lock and return:

`BLOCKED: STAGING_MUTATION_LOCK_UNAVAILABLE`

While both locks are held:

1. re-prove the exact staging environment, baseline marker and safety guards;
2. record the exact previous `blog_public` value without reading credential/config contents;
3. deploy only the feature commit using `deployment/deploy-staging.sh changed feature/task-2.60-staging-search-isolation`;
4. verify the deployed marker equals the feature commit and the MU plugin exists at the exact staging path;
5. run the single bounded staging configuration mutation `wp option update blog_public 0` with the exact staging `--path`;
6. verify the effective WordPress value is exactly `0` and the environment remains exactly `staging`;
7. release fd 8, then fd 9.

Do not hold the locks during HTTP smoke checks, Git integration or report formatting.

## Bounded HTTP verification

Make exactly two unauthenticated `GET` requests:

1. `https://stage.raspitajse.com/`;
2. `https://stage.raspitajse.com/robots.txt`.

Constraints:

- do not send cookies, authorization, user data or custom identifying payloads;
- do not follow redirects; any redirect is a stop condition;
- use bounded timeouts and capture only status, relevant response headers and the minimum body fragments needed for evidence;
- do not contact production or any external host;
- do not use cache-busting queries, purge/flush/warm commands or crawler execution.

PASS requires all of:

- both requests return a successful staging response without redirect;
- the home response has an `X-Robots-Tag` containing `noindex` and `nofollow`;
- the rendered home HTML has a robots meta directive containing `noindex`;
- `/robots.txt` contains a global `User-agent: *` section with `Disallow: /` and does not advertise a sitemap;
- `/robots.txt` also receives the staging `X-Robots-Tag` isolation header;
- WordPress reports effective `blog_public=0`;
- mail/scheduler guards remain loaded, with no mail, cron or scheduled action executed;
- no cache mutation or business-state mutation occurred.

Do not weaken the acceptance criteria merely because a cache or SEO plugin wins a hook. That is a precise `PARTIAL` finding requiring a new bounded contract.

## Integration and rollback

Only after every static, runtime and HTTP acceptance check passes:

1. fast-forward `staging` to the exact validated feature commit;
2. push `staging` without force;
3. verify `origin/staging`, local `staging` and the live deploy marker all equal that commit;
4. verify the worktree is clean.

If any check fails after runtime mutation, reacquire the two locks in the same order, restore the exact recorded `blog_public` value, and deploy `changed staging` from the still-unmodified baseline branch to remove the feature change from live staging. Verify the marker returns to the baseline, release fd 8 then fd 9, publish `PARTIAL`, and stop. Do not integrate the feature branch.

If rollback cannot be completed exactly, use `FAIL` and state the precise remaining staging-only technical state. Never fix forward by broadening paths or changing another subsystem.

## Result handling

### PASS

Return:

`PASS: STAGING_SEARCH_ENGINE_ISOLATION_READY`

The report must include:

- feature commit and final `staging`/deploy SHA;
- exact changed files;
- static-test results;
- pre/post `blog_public` values;
- concise home and robots evidence for status, `X-Robots-Tag`, meta and `Disallow: /`;
- HTTP requests attempted = 2, redirects followed = 0, contacted host = `stage.raspitajse.com` only;
- cache purge/flush/warm = 0;
- fixtures = 0;
- mail/scheduler/payment execution = 0;
- protected business-state mutation = 0;
- rollback state;
- `Production touched: NO`.

### PARTIAL / BLOCKED / FAIL

Publish one precise result with the failed checkpoint, source/live/option state, whether rollback completed, and the next smallest corrective fact. Do not begin another numbered task.

## Final report

Publish exactly one Zadatak 2.60 report through the existing `codex-reports` workflow, verify the remote report, and STOP.

