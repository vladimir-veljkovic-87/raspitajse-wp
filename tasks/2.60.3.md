# Zadatak 2.60.3 — Identify the public response layer bypassing staging isolation

Status: READY
Baseline: f6093c11d71ddc175ebf83fc69453c20aaca5239
Previous task: 2.60.2
Target: staging
Production: FORBIDDEN
Time budget: 35 minutes
Risk class: AUTONOMOUS_SAFE
Mutation class: temporary bounded staging diagnostics with mandatory exact rollback

## Objective

Determine which response layer served the old public home and `robots.txt` payload while the Task 2.60.2 WordPress runtime, deployed MU plugin and `blog_public=0` state were verified internally.

Required result:

`PASS: STAGING_SEARCH_RESPONSE_LAYER_IDENTIFIED`

PASS means the responsible layer is evidence-backed and the smallest corrective contract is defined. It does not close E1 and does not authorize cache purge, integration or production work.

## Sources

Read `tasks/README.md`, this task, current safety/workflow/mutation/DoD documents, the latest 2.60.2 report, `deployment/deploy-staging.sh`, the exact feature commit `783cc6b3425985c1e27de8e823fca020d0d7b869`, and directly relevant LiteSpeed/WordPress routing code or safe configuration metadata only.

Do not read secrets, `wp-config.php` contents, arbitrary cache bodies, unrelated reports, business data or production configuration.

## Preconditions

Verify:

- authorized Hostinger repository and exact staging WordPress path;
- clean local `staging` and `origin/staging` at the declared baseline;
- live marker equals the baseline, environment is exactly `staging`, and `blog_public=1` after the completed rollback;
- feature branch `origin/feature/task-2.60.2-staging-search-isolation` still equals `783cc6b3425985c1e27de8e823fca020d0d7b869`, is a direct child of the baseline, and changes only `wp-content/mu-plugins/raspitajse-staging-search-isolation.php`;
- runtime plugin is absent before the diagnostic;
- mail-safety and scheduler guards are active;
- no active Git, rsync or deploy process.

If any fact differs, publish one precise blocker and stop. Do not recreate or amend the feature commit.

## Diagnostic contract

This task may temporarily reproduce the already-safe 2.60.2 state, but must not integrate code. It may:

- deploy the exact existing feature commit with `deployment/deploy-staging.sh changed feature/task-2.60.2-staging-search-isolation`;
- set only staging `blog_public` from its recorded value to `0`;
- create one collision-resistant non-secret text probe at exactly `wp-content/mu-plugins/raspitajse-task-2.60.3-origin-probe-<nonce>.txt` inside the staging runtime, containing only the same random probe token;
- issue the bounded origin/public requests below;
- remove that exact probe, restore the option, and deploy baseline staging before reporting.

Forbidden: source edits or commits; staging integration; production; cache purge/flush/warm/crawler/optimization; web-server/CDN/control-panel mutation; `.htaccess` or `wp-config.php` reads/changes; business fixtures/state; mail, cron, Action Scheduler, payments; authentication/cookies/PII; redirects; another host.

## Locks and temporary reproduction

Acquire nonblocking in this exact order:

1. `/tmp/raspitajse-staging-owned-cron-runner.lock` on fd 9;
2. `/tmp/raspitajse-staging-runtime.lock` on fd 8.

If unavailable, release any acquired lock and return `BLOCKED: STAGING_MUTATION_LOCK_UNAVAILABLE`.

While held:

1. re-prove staging, baseline/marker and guards;
2. record exact `blog_public` pre-value;
3. deploy the exact feature branch and prove exactly the one MU plugin was synced;
4. set only `blog_public=0` and prove effective value;
5. create the exact random probe file without symlinks and record its SHA-256;
6. release fd 8, then fd 9.

## Evidence matrix

Capture and compare these layers without printing IP addresses, tokens, cookies, credentials or full bodies:

1. **WordPress bootstrap** — prove environment, `blog_public`, MU-plugin/hash, registered filters, resulting robots directives/text/header values.
2. **Local web origin** — at most three GETs to the local web server using SNI/Host `stage.raspitajse.com`: `/`, `/robots.txt`, and the exact probe path. Do not follow redirects. If a safe local vhost endpoint cannot be established without privileged configuration, record it as unavailable; do not guess ports or scan.
3. **Public normal path** — exactly three GETs to `https://stage.raspitajse.com`: `/`, `/robots.txt`, and the exact probe path, without query strings or cache-control overrides.
4. **Public diagnostic bypass variants** — at most two additional GETs, one for home and one for robots, using one collision-resistant query parameter plus request headers `Cache-Control: no-cache` and `Pragma: no-cache`. These are diagnostic only and are not final E1 acceptance evidence.

For HTTP evidence record only status, redirect/location presence, robots headers/meta/body facts, exact probe-hash match, and relevant routing/cache headers when present: `Age`, `Cache-Control`, `CF-Cache-Status`, `X-Cache`, `X-LiteSpeed-Cache`, `Via`, `Vary`, `ETag`, `Last-Modified`, `Date`, `Server`. Do not assert a missing header.

Read-only DNS/local-interface comparison may establish match/no-match, but report only that boolean relationship, never raw addresses. Do not contact resolved production/external hosts separately.

Normal GETs may naturally cause ordinary request-side cache bookkeeping; record observed evidence. No explicit cache mutation command is authorized.

## Classification

Classify exactly one outcome only when supported:

- `UPSTREAM_OR_PAGE_CACHE` — origin/bootstrap is fresh while normal public is stale, and a diagnostic bypass or cache header identifies the boundary;
- `LOCAL_WEB_CACHE_OR_STATIC_LAYER` — WordPress bootstrap is fresh but the local web-origin response is stale;
- `PUBLIC_ROUTING_OR_DOCROOT_MISMATCH` — local origin/probe maps correctly while public responses/probe demonstrably map elsewhere;
- `APPLICATION_HOOK_CONTRACT` — the actual origin request reaches PHP but the implemented hooks do not affect the required response;
- `UNRESOLVED_RESPONSE_LAYER` — evidence cannot distinguish the boundary.

Do not infer cache solely from stale output. `UNRESOLVED_RESPONSE_LAYER` is `PARTIAL`, not FAIL.

## Mandatory rollback

Rollback is required regardless of diagnosis. Reacquire locks in the same order, then:

1. remove only the exact probe file and prove it absent;
2. restore exact prior `blog_public`;
3. run `deployment/deploy-staging.sh changed staging` from the baseline;
4. prove only the isolation MU plugin was removed;
5. verify marker/local/origin staging equal the baseline, plugin absent, option restored, environment staging and guards active;
6. release fd 8, then fd 9.

If exact rollback fails, return `FAIL` with the precise remaining staging-only state. Do not fix forward.

## Report

PASS requires an evidence-backed layer classification, complete rollback, and a smallest next corrective contract stating exact owner/config/path, allowed mutation, rollback and final two-request acceptance plan.

Report request counts by local/public/diagnostic class, redirects followed = 0, cache mutation commands = 0, exact final state, fixtures/business/mail/scheduler/payment mutations = 0, and `Production touched: NO`.

Publish exactly one Zadatak 2.60.3 report through `codex-reports`, verify it remotely, and STOP. Do not execute the corrective task.

