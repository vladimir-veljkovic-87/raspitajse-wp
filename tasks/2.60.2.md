# Zadatak 2.60.2 — Enforce and verify staging search-engine isolation

Status: READY
Baseline: f6093c11d71ddc175ebf83fc69453c20aaca5239
Previous task: 2.60.1
Retries outcome of: 2.60
Target: staging
Production: FORBIDDEN
Time budget: 40 minutes
Risk class: AUTONOMOUS_SAFE
Mutation class: bounded staging technical configuration/code mutation; no protected business-state mutation

## Objective

Close Definition-of-Done criterion E1 by making staging unambiguously non-indexable at WordPress and HTTP levels.

Task 2.60 stopped before mutation because the namespace-free helper could not create a new file. Task 2.60.1 corrected and regression-tested that helper. Required result:

`PASS: STAGING_SEARCH_ENGINE_ISOLATION_READY`

Production must not be accessed or changed.

## Sources and scope

Read `tasks/README.md`, this task, current `AGENTS.md`, `CODEX_WORKFLOW.md`, `CODEX_HOST_RESILIENCE.md`, `STAGING_MUTATION_POLICY.md`, `STAGING_DEFINITION_OF_DONE.md`, reports 2.59/2.60/latest 2.60.1, the deploy script, corrected helper, and directly relevant owned MU-plugin patterns only.

Do not reopen unrelated readiness gaps or rerun earlier E2E, mail, scheduler, quota or application tests.

## Expected marker lag

At task start:

- `origin/staging` must equal the declared baseline;
- live marker is expected to be `ff42719f218e440b168b57690b70a2402ee66809` because 2.60.1 changed only `tools/codex-git-apply.sh` and deployed nothing;
- that marker must be an ancestor of the baseline;
- their complete diff must be exactly the accepted three-line helper correction;
- no deploy-allowlisted path may differ between marker and baseline.

Accept only this explained lag. Any extra path, non-ancestor relationship or runtime drift is `BLOCKED`; do not repair or rebase.

## Preconditions

Verify authorized Hostinger context, repository `/home/u601262303/repo/raspitajse-wp`, clean worktree, local/remote staging at baseline, exact marker relationship above, staging WordPress environment, no active Git/rsync/deploy process, and active mail-safety plus scheduler guards. Confirm effective pre-state `blog_public=1`. In a fresh task-private feature-branch fixture, confirm the corrected helper accepts a minimal canonical new-file patch with `--check-only`.

If any precondition fails, publish one precise blocker and stop.

## Branch and allowed source change

Create `feature/task-2.60.2-staging-search-isolation` from the exact baseline.

Create one small owned file:

`wp-content/mu-plugins/raspitajse-staging-search-isolation.php`

It must:

- do nothing unless `wp_get_environment_type()` is exactly `staging`;
- add deterministic `X-Robots-Tag` containing at least `noindex` and `nofollow` to public staging responses;
- force WordPress robots metadata to contain at least `noindex` and `nofollow`;
- make dynamic `/robots.txt` contain global `User-agent: *` and `Disallow: /`, without a sitemap advertisement;
- coexist with core and active SEO/cache plugins without editing them;
- contain no hostname, credential, secret or production behavior beyond the staging environment gate.

Do not edit core, vendor, theme, `.htaccess`, `wp-config.php`, web-server/control-panel or cache configuration. One small directly relevant test file is allowed only if needed for pre-deploy hook verification.

## Allowed and forbidden mutations

Allowed: the scoped feature change; set only staging option `blog_public` to `0`; changed-file deployment through `deployment/deploy-staging.sh`; validated fast-forward integration into staging; normal marker update; task-private files and report publication.

Forbidden: production; any other database change; business fixtures/state; mail, cron, Action Scheduler or payment execution; cache purge/flush/warm/crawler/optimization; redirects or HTTP contact outside `stage.raspitajse.com`; force push, history rewrite or broad cleanup.

## Static validation

Use the corrected namespace-free helper immediately if the normal patch path hits the known namespace `ENOSPC` circuit breaker. Do not retry that failure.

Before deployment:

1. run `php -l` on changed PHP;
2. run `git diff --check` and inspect the full diff/file set;
3. prove the non-staging path registers no isolation behavior;
4. prove staging hook outputs as far as safely practical without live mutation;
5. confirm no secrets, generated files or production paths;
6. commit and push the feature branch without force.

If safe new-file creation still fails, publish `PARTIAL` and stop before runtime mutation.

## Locked mutation

Acquire nonblocking in this exact order:

1. `/tmp/raspitajse-staging-owned-cron-runner.lock` on fd 9;
2. `/tmp/raspitajse-staging-runtime.lock` on fd 8.

If unavailable, release any acquired lock and return `BLOCKED: STAGING_MUTATION_LOCK_UNAVAILABLE`.

While both are held:

1. re-prove staging, marker relationship and guards;
2. record exact previous `blog_public`;
3. run `deployment/deploy-staging.sh changed feature/task-2.60.2-staging-search-isolation`;
4. prove only the new allowlisted MU plugin was synced and marker equals feature commit; the helper must not be copied to WordPress;
5. run only `wp option update blog_public 0` with the exact staging `--path`;
6. verify effective value `0` and environment still `staging`;
7. release fd 8, then fd 9.

Do not hold locks during HTTP, Git integration or report formatting.

## HTTP verification

Make exactly two unauthenticated GETs, with bounded timeouts and no cookies/auth/data/cache-busting, and follow zero redirects:

1. `https://stage.raspitajse.com/`;
2. `https://stage.raspitajse.com/robots.txt`.

PASS requires:

- both are successful staging responses without redirect;
- home header has `X-Robots-Tag: noindex, nofollow` semantics;
- home HTML robots meta contains `noindex`;
- robots response has global `User-agent: *`, `Disallow: /`, no sitemap, and the isolation header;
- effective `blog_public=0`;
- mail/scheduler guards remain active;
- no mail, cron, scheduled action, cache or business-state mutation occurred.

Do not weaken acceptance if cache/SEO behavior wins a hook; return precise `PARTIAL`.

## Integration and rollback

Only after every check passes, fast-forward staging to the validated feature commit, push without force, and prove local staging, `origin/staging` and live marker all equal it with a clean worktree.

If any post-mutation check fails, reacquire locks in the same order, restore the exact prior `blog_public`, and deploy `changed staging` from the still-unmodified baseline. This must remove only the new MU plugin and set marker to `f6093c11d71ddc175ebf83fc69453c20aaca5239`. Verify rollback, release fd 8 then fd 9, publish `PARTIAL`, and do not integrate. If exact rollback fails, use `FAIL`.

## Report

For PASS return `PASS: STAGING_SEARCH_ENGINE_ISOLATION_READY` and report feature/final/deploy SHA, changed/deployed files, static checks, edit mechanism, pre/post `blog_public`, concise HTTP header/meta/robots evidence, two requests/zero redirects/one allowed host, zero cache operations/fixtures/mail/scheduler/payment/business-state mutation, rollback state, and `Production touched: NO`.

For any non-PASS, state the exact checkpoint and source/live/option/rollback state. Publish exactly one Zadatak 2.60.2 report through `codex-reports`, verify it remotely, and STOP. Do not begin another task.

