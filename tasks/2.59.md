# Zadatak 2.59 — Build the final staging technical-readiness evidence matrix

Status: READY
Baseline: ff42719f218e440b168b57690b70a2402ee66809
Previous task: 2.58
Target: staging
Production: FORBIDDEN
Time budget: 35 minutes
Mode: read-only evidence audit; no source, deploy or database mutation

## Objective

Create the authoritative gap map between the current staging release candidate and every criterion in `STAGING_DEFINITION_OF_DONE.md`.

Task 2.58 established:

- `PASS: TRANSACTIONAL_MAIL_GATE_READY`;
- `PASS: LAUNCH_CRITICAL_E2E_READY`;
- exact fixture cleanup;
- unchanged source/live marker;
- zero prohibited transport, scheduler and payment side effects.

Do not repeat E2E, mail, quota, application or scheduler execution.

Required result:

`PASS: TECHNICAL_READINESS_GAP_MAP_COMPLETE`

This task does not itself declare `TECHNICALLY_READY` unless every Definition-of-Done criterion is supported by current, reproducible evidence and no manual/release blocker remains.

## Sources

Read:

- `tasks/README.md`;
- this task;
- `STAGING_DEFINITION_OF_DONE.md`;
- current `AGENTS.md`, `CODEX_WORKFLOW.md`, `STAGING_MUTATION_POLICY.md` and `CODEX_PROJECT_CONTEXT.md`;
- the exact reports:
  - `reports/20260914T181530Z-zadatak-2_47.md`;
  - `reports/20260915T055214Z-zadatak-2_48.md`;
  - `reports/20260915T063651Z-zadatak-2_50.md`;
  - the latest successful job-alert/communications deployment report from the 2.51 series;
  - `reports/20260918T201218Z-zadatak-2_53.md`;
  - `reports/20260919T191142Z-zadatak-2_55.md`;
  - `reports/20260920T055214Z-zadatak-2_58.md`.

Read an additional historical report only when one exact criterion cannot otherwise be classified. Name the report and the missing fact before reading it.

Inspect only current source/config/documentation directly relevant to a criterion. Do not perform a broad repository audit or regenerate prior test suites.

## Preconditions

Verify read-only:

- authorized Hostinger context;
- repository `/home/u601262303/repo/raspitajse-wp`;
- branch `staging`;
- local HEAD and `origin/staging` equal the declared baseline;
- clean worktree;
- live deploy marker equals the baseline;
- WordPress environment is `staging`;
- WordPress `7.1.1`, WPForms Lite `2.0.2`, child theme `superio-child`;
- no active source/deploy operation.

If source/live alignment differs, return one precise BLOCKED result. Do not repair or rebase.

No staging mutation lock is required because this task is read-only. Do not create users, posts, orders, applications, sessions or scheduler state.

## Classification model

Classify every criterion as exactly one of:

- `SATISFIED` — current evidence directly proves the criterion;
- `PARTIAL` — meaningful evidence exists but one defined proof is missing;
- `BLOCKER` — required launch/release behavior is missing or unsafe;
- `MANUAL_PRE_RELEASE` — requires a human/browser/provider/control-panel action before release;
- `OUT_OF_SCOPE_APPROVED_POLICY` — excluded by the current approved free-launch policy and not required for this release;
- `NOT_APPLICABLE` — structurally irrelevant, with a precise reason.

Do not mark a criterion `SATISFIED` using plans, old assumptions or absence of observed errors. Cite an exact current report, source path, runtime fact or repository document.

Do not treat optional cosmetic cleanup, perfect performance scores or obsolete paid-flow parity as blockers unless the active launch policy or current code still depends on them.

## Required evidence matrix

Produce one row for every item below.

### A. Scheduler and background processing

1. backlog recovery controlled and understood;
2. owned ongoing trigger operational;
3. lock/overlap behavior proven;
4. exactly intended owned recurring callbacks active;
5. legacy competing callbacks absent;
6. temporary recovery controls have a documented final state;
7. no broad runner is required for normal operation.

### B. Communications

1. Raspitajse-owned transport authoritative;
2. fail-closed staging recipient safety;
3. welcome, password reset, application and enabled launch templates validated;
4. owned job-alert and expiry paths covered;
5. legacy transport gated/removed according to policy;
6. sender policy and environment-aware links;
7. real transport/provider readiness evidence required before production.

### C. Critical business behavior and ownership

1. candidate/employer identity and role boundaries;
2. administrator access;
3. free-launch three-active-job policy;
4. job lifecycle and quota concurrency;
5. application creation, duplicate prevention and employer isolation;
6. private messaging behavior required by current launch scope;
7. packages/orders/paid entitlement behavior under the approved free-launch policy;
8. browser CAPTCHA/manual UX checks.

### D. Vendor/update safety

1. WordPress and WPForms current accepted versions;
2. Superio/WP Job Board Pro/WooCommerce and other critical dependency inventory;
3. direct vendor modifications inventoried;
4. remaining vendor diffs mapped to KEEP/CHANGE/DROP;
5. critical owned behavior no longer depends on undocumented vendor edits;
6. reproducible update/deploy procedure.

### E. Production-critical hardening

1. staging `noindex`/search-engine isolation;
2. environment-aware URLs and recipients;
3. filesystem/role/permission baseline;
4. PHP/WordPress error logging policy;
5. operational monitoring/alerting plan;
6. LiteSpeed/object-cache sanity for critical flows;
7. code rollback procedure;
8. backup procedure;
9. credible restore procedure or restore rehearsal evidence;
10. scheduler and mail operating procedures;
11. secrets remain external to repository and unexposed.

### F. Release/data strategy

1. exact release-candidate SHA reproducible;
2. code-first production strategy documented;
3. required database migrations identified or explicitly none;
4. rollback boundary defined;
5. production deployment remains a separate human-approved task;
6. manual browser/CAPTCHA checks listed;
7. go/no-go evidence package can be assembled without rerunning completed tests.

## Bounded read-only runtime checks

Run only checks needed to classify missing hardening facts:

- WordPress environment/version and active theme/plugin versions;
- `blog_public` and relevant noindex/robots behavior;
- one unauthenticated GET or HEAD to the staging robots response and one to the staging home page only when needed to confirm indexing headers/meta;
- presence and status of current scheduler/mail guards and owned callbacks without executing them;
- LiteSpeed/object-cache status and bounded read-only health indicators;
- logging configuration state through safe WordPress APIs or non-secret constants only;
- existence/readability of repository backup, rollback, deployment and operations documentation;
- filesystem permission metadata for exact critical owned paths using `stat`, without recursive traversal and without reading credential/config contents.

Do not:

- access production;
- read `wp-config.php` contents;
- print secrets, email addresses, usernames or PII;
- execute backup, restore, deploy, cron, Action Scheduler, mail or payment paths;
- warm/flush cache;
- change robots, options, permissions, files or database rows;
- follow redirects to production or external hosts.

## Active launch-policy interpretation

The current release is a free-launch journey with the three-active-job policy. Determine from current code and accepted reports whether packages, checkout and paid entitlements are:

- safely dormant/backward-compatible and outside current launch scope; or
- still reachable/required and therefore need regression evidence.

Do not assume either answer. Record exact hook/UI/runtime evidence. If safely excluded, classify the corresponding paid-flow criterion `OUT_OF_SCOPE_APPROVED_POLICY`, not BLOCKER.

Apply the same rule to private messaging: determine whether it is part of the actual launch journey or deferred. If required, identify the missing evidence as a concrete gap; do not run a new message fixture in this audit.

## Output requirements

The report must contain:

1. current source/live/runtime alignment;
2. the complete criterion matrix with classification and exact evidence;
3. a short list of remaining `BLOCKER`, `PARTIAL` and `MANUAL_PRE_RELEASE` items;
4. explicit approved-scope exclusions;
5. a dependency graph/order for the remaining work;
6. the smallest next numbered task, with:
   - objective;
   - exact scope;
   - mutation/risk class;
   - prerequisite evidence;
   - expected PASS classification;
7. whether a final `TECHNICALLY_READY` declaration task can follow directly after the remaining work.

Prefer at most seven remaining-work items. Combine related documentation-only gaps into one bounded closeout task where practical. Do not create cosmetic follow-ups.

## Result handling

### PASS

Return:

`PASS: TECHNICAL_READINESS_GAP_MAP_COMPLETE`

when every Definition-of-Done criterion has an evidence-backed classification and the remaining work is ordered and bounded.

PASS does not mean the site is already technically ready.

### BLOCKED

Return one precise blocker only if the evidence matrix itself cannot be completed safely or authoritative source/runtime alignment is inconsistent.

## Final report

Publish one concise Zadatak 2.59 report containing the complete matrix and next-task recommendation.

Required safety statements:

- source/deploy/database mutation: 0;
- fixtures created: 0;
- scheduler/mail/payment execution: 0;
- production touched: NO.

Verify the remote report and STOP. Do not start the recommended follow-up automatically.
