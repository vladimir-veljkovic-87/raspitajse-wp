# Codex Execution Report

- Task: Zadatak 1.89 — Owned candidate-job alert evaluator, query integration and active cutover
- Task ID: 1.89
- Result: PASS
- Recorded at (UTC): 2026-09-01T10:40:07Z
- Source branch: staging
- Source HEAD: 0b00f2c5a8d191dcf7b6bc78b82e1bff8f680807
- Source working tree clean: YES
- Staging deploy marker: 0b00f2c5a8d191dcf7b6bc78b82e1bff8f680807
- Staging environment: staging

## Task report

## 1. Outcome and baseline/final SHA

- Result: PASS.
- Baseline source/origin/staging/deploy SHA: `49026a7ef51c283ae957cc7a3c07df15094ff6e7`.
- Feature branch: `feature/z1-89-owned-candidate-job-evaluator`.
- Feature/integration commit: `0b00f2c5a8d191dcf7b6bc78b82e1bff8f680807` — `feat: activate owned candidate job alert evaluator`.
- Final local staging/origin/staging/deploy SHA: `0b00f2c5a8d191dcf7b6bc78b82e1bff8f680807`.
- Integration: PASS, fast-forward only; force update: NO.
- Final source working tree: clean.

## 2. Exact changed files

- Added: `wp-content/plugins/raspitajse-communications/includes/class-candidate-job-alert-integration.php`.
- Modified: `wp-content/plugins/raspitajse-communications/includes/class-candidate-job-alert-delivery.php`.
- Modified: `wp-content/plugins/raspitajse-communications/raspitajse-communications.php`.
- Diff: three owned plugin files, 638 insertions, 11 deletions.
- No WPJBP vendor, theme, MU plugin, WooCommerce, payment, Action Scheduler, deployment, or production file changed.

## 3. Evaluator component

- Added `Raspitajse_Communications_Candidate_Job_Alert_Evaluator`.
- Batch size: maximum 20 service calls per invocation.
- Deterministic keyset order: queryable due GMT ascending, then alert ID ascending.
- No offset pagination over a mutable due set.
- Per-alert `Throwable`/`WP_Error` isolation; one broken alert does not stop later alerts.
- One `Delivery_Service::process()` call per alert per invocation; the service/claim contract permits at most one mail attempt for that alert.
- New work stops at the smaller of 20 seconds or 50% of the PHP request execution limit.
- Operational observation payload contains only alert IDs, sanitized outcomes, counts, and continuation state.

## 4. Dedicated cron hook

- Canonical recurring hook: `raspitajse_candidate_job_alert_evaluator`.
- Owned continuation hook: `raspitajse_candidate_job_alert_evaluator_continue`.
- No Action Scheduler action was introduced.
- No WPJBP daily-notice execution was introduced.

## 5. Schedule cadence

- Recurring cadence: WordPress `hourly`.
- First schedule is placed five minutes after a missing-event bootstrap.
- If a bounded batch leaves work, one owned single continuation may be scheduled for +5 minutes with a due/ID keyset cursor.

## 6. Duplicate-schedule prevention

- Update-safe `init` bootstrap scans the cron array for an existing exact owned hook before scheduling.
- Activation hook uses the same idempotent method; deactivation clears only the two owned hooks.
- Two consecutive post-deploy WordPress bootstraps produced the same single recurring event timestamp/fingerprint.
- Fixture produced zero continuation events.

## 7. Before/after daily-hook callback graph

Before:

- priority 10: owned `Candidate_Job_Alert_Bridge::send_job_alert_notice`;
- priority 10: two job-expiry callbacks;
- priority 10: two candidate-expiry callbacks;
- priority 10: employer-to-candidate `Candidate_Alert::send_candidate_alert_notice`.

After:

- priority 10: the same two job-expiry callbacks;
- priority 10: the same two candidate-expiry callbacks;
- priority 10: the same employer-to-candidate candidate-alert callback;
- owned bridge absent;
- vendor direct candidate-to-job callback absent.

The existing WPJBP daily cron event remains because the other five callbacks still own it.

## 8. Final daily callback count

- Exact final callback count: 5.
- Every remaining callback is still priority 10.
- Owned bridge priority: absent.
- Vendor `WP_Job_Board_Pro_Job_Alert::send_job_alert_notice`: absent.
- Daily hook executed during task: NO.

## 9. Owned hourly event

- Exact final event count: 1.
- Hook: `raspitajse_candidate_job_alert_evaluator`.
- Schedule: `hourly`.
- Args: empty.
- Event was inspected only; cron event run was not invoked.

## 10. Query adapter implementation

- Added `Raspitajse_Communications_Candidate_Job_Alert_Query_Adapter`.
- The adapter accepts only the delivery service context, normalizes the saved query, reads the owned activation cutoff, and returns IDs only.
- Invalid adapter context/query/state or a query exception returns adapter failure, not a false empty-success result.

## 11. WPJBP matching mechanisms reused

- Reuses `WP_Job_Board_Pro_Query::get_posts()`.
- That path continues to invoke WPJBP `Job_Filter::get_query_var_filter()`, `get_meta_filter()`, and `get_tax_filter()` behavior.
- Runtime fixture proved two real saved dimensions: `filter-category` and `filter-location`.
- Delivery removes ranking authority from `filter-orderby` by overriding only final delivery order; matching filters remain WPJBP-owned.

## 12. Publication/window filters

- Post type: `job_listing`.
- Status: `publish`.
- Exact machine boundary: `post_date_gmt >= activation_cutoff_gmt` and `post_date_gmt < window_end_gmt`.
- Publish/future/draft state is enforced by WordPress post status.
- Active check also excludes WPJBP filled jobs and fails closed on malformed expiry dates.
- Canonical WPJBP expiry date behavior is preserved: a site-local `_job_expiry_date` before today is inactive.

## 13. Delivered-job exclusion

- Adapter excludes `alert_id + job_id` entries already present in `_raspitajse_cja_delivered_job`.
- Delivery service revalidates post type, status, cutoff/end, and ledger before snapshot.
- Config changes do not erase the semantic sent ledger.

## 14. Result scan/cap behavior

- Query page size: 50 IDs.
- Hard scan bound: 10 pages / 500 matching DB rows.
- Ledgered/inactive early rows are skipped until up to five deliverable results are found.
- Final service cap: five.
- Delivery order: `post_date_gmt ASC, ID ASC` regardless of menu order, featured, urgent, or random saved ordering.

## 15. Renderer integration

- Added `Raspitajse_Communications_Candidate_Job_Alert_Mailer`.
- Reuses WPJBP `job_alert_notice` subject/content options, `render_email_vars()`, current `job_entry_template`, jobs-page URL, and canonical job meta fields.
- Preserves legacy UX split: newest selected job uses `newest_*`; older selected jobs use `job_data` entries.
- Recipient is resolved at send time from current `job_alert.post_author`.
- Recipient/body are not persisted or operationally logged.

## 16. Subject/body placeholder regression

- Sanitized staging template contract: subject/content/entry hashes captured without printing template content.
- Active content placeholders used on staging: `newest_job_*`, `newest_employer_name`, `newest_location`, and `job_data`.
- Entry placeholders: job URL/title/employer/location/publish date.
- Five-result and two-result intercepted payloads contained every selected title.
- HTML wrapper present; unresolved placeholder count: 0.
- Canonical original recipient matched current `post_author`; fixture explicitly used the preexisting mail-safety original-context stack rather than the redirected staging inbox.

## 17. candidate_alerts transport proof

- Delivery service scopes the mail adapter through `CHANNEL_CANDIDATE_ALERTS` only.
- Mail adapter rejects any other channel.
- Runtime SenderPolicy assertion confirmed `candidate_alerts` resolves to the candidate sender mapping with exact output keys `from_email`, `from_name`, `reply_to`.
- No hardcoded sender identity was added to query/renderer/evaluator code.

## 18. First-send real-query proof

- Alert created at T0.
- Matching job before T0: excluded and never ledgered.
- Matching jobs after T0: eligible.
- First owned invocation selected/rendered/delivered exactly the oldest five eligible new jobs.
- First-send cutoff equals alert creation for never-attempted alerts.

## 19. Matching-filter assertions

- Matching category + matching location: included.
- Nonmatching category + matching location: excluded.
- Matching category + nonmatching location: excluded.
- The fixture used the exact active WPJBP taxonomy filter keys.

## 20. Five-plus-two overflow proof

- Seven matching undelivered jobs entered the first logical eligibility range.
- First evaluation: exactly five selected, five rendered, five ledgered after accepted handoff.
- Remaining two: not rendered, not ledgered, still eligible.
- Next due window: exactly those two selected/rendered; one mail; semantic ledger then contained all seven exactly once.

## 21. Same-job no-resend proof

- A later due evaluation with all seven still matching produced zero mail.
- Outcome: `empty`.
- Ledger remained seven exact semantic IDs; no delivered job was selected again.

## 22. Unpublished/expired exclusion

- Matching published active job: eligible.
- Matching draft job: excluded.
- Matching published job with expired canonical expiry date: excluded.
- Filled-job predicate is also enforced in source through the WPJBP canonical helper.

## 23. Retry evaluator integration

- Controlled first mail result: false.
- Outcome: `retryable_failed`.
- Retry instant: +1 hour.
- Queryable `_raspitajse_cja_next_due_gmt` is now atomically aligned to the exact retry instant.
- Evaluator at +30 minutes: processed 0, mail 0.
- Evaluator after retry instant: one second controlled attempt; accepted; ledger written only after success.

## 24. Empty-result behavior

- Real matching adapter returned zero for a valid alert with no jobs.
- Renderer/Transport/wp_mail calls: 0.
- Outcome: `empty`.
- Next site-calendar due advanced normally.

## 25. Broken-alert isolation

- Same bounded batch contained malformed saved-query alert, unknown-state alert, and valid due alert.
- Malformed/unknown alerts: sanitized `WP_Error`, zero mail.
- Valid alert: processed and delivered normally.
- Batch error count: two; valid processing continued.

## 26. Concurrency/claim integration

- Worker A acquired the exact alert option claim.
- Worker B evaluator returned `already_claimed`, sent zero for that alert, did not alter the claim token, and did not ledger its jobs.
- An unrelated due alert in the same batch sent normally.
- Worker A released the exact token; final claim count zero.

## 27. Lazy legacy migration proof

- Valid legacy `_job_alert_send_email_time` initialized owned state with zero mail.
- Activation cutoff became the migration instant.
- Historical matching job was not blasted and no historical ledger was invented.
- A post-migration matching job was sent at the next calendar due time.
- Legacy send date remained unchanged.
- Malformed legacy date: sanitized failure, zero mail.

## 28. New frequency validation

Owned create endpoint fixture:

- daily: PASS;
- weekly: PASS;
- fortnightly: PASS;
- monthly: PASS;
- biannually: REJECT for new alert;
- annually: REJECT for new alert.

Manual legacy biannual/annual fixture records remained published with their original frequency/send-date fields until exact fixture cleanup.

## 29. Frontend frequency behavior/status

- Current WPJBP frontend getter still exposes six keys: daily, weekly, fortnightly, monthly, biannually, annually.
- Backend canonical new-alert allowlist exposes only the approved four.
- No safe create-only UI filter seam exists: the available getter filter is global to all job-alert displays and could degrade legacy record labels.
- Therefore 1.89 intentionally leaves a documented frontend/backend mismatch rather than globally changing legacy display behavior.
- Recommended next small task should address a scoped UI/template seam.

## 30. Legacy send_email_time status

- `_job_alert_send_email_time` is read only for lazy legacy initialization.
- Owned evaluator/service/mailer never updates or deletes it.
- No legacy outer frequency loop remains active for candidate-to-job delivery.

## 31. Zadatak 1.84 security regression

- All 12 mutation routes retain exactly one owned callback at priority 10.
- Corresponding vendor callbacks remain absent.
- `job_alert` and `candidate_alert` REST exposure remain disabled.
- Full destructive 40/40 matrix was not repeated because route ownership source was not otherwise redesigned; focused runtime callback regression passed.

## 32. Zadatak 1.85 SenderPolicy regression

- Four channels resolve: system, candidate_alerts, employer_alerts, job_expiry.
- Exact output keys unchanged.
- Unknown channel fails closed.
- Transport remains enabled on staging.
- Production contract source remains fail closed unless the existing configured production sender is valid.
- Credentials were neither read into reports nor added to source/state.

## 33. Zadatak 1.86 sender/bridge regression

- Candidate-to-job identity remains `candidate_alerts` through owned Transport.
- 1.86 bridge class remains in source for compatibility/history but is not registered.
- Direct vendor sender is not registered.
- Employer-to-candidate sender/callback remains untouched.

## 34. Zadatak 1.88 foundation regression

- Local pre-deploy contract harness: 14/14 PASS.
- Active fixture additionally passed calendar current-frequency validation, exact 14-day fortnightly behavior, claim atomicity, accepted ledger behavior, retry state, ledger idempotency, and unknown-state fail-closed.
- Foundation retry integration seam changed only to keep queryable due scalar equal to exact retry time; core 200-case destructive matrix was not repeated.

## 35. Controlled wp_mail attempts

- Accepted functional matrix: seven expected candidate-alert wp_mail API attempts.
- Breakdown: first five, overflow two, retry false, retry success, isolation valid, claim unrelated, lazy legacy new-result = seven total calls across the relevant phases (some phases replace zero-mail evaluations).
- Every call was short-circuited by `pre_wp_mail` before PHPMailer.
- Three diagnostic/corrected fixture iterations each kept all mail intercepted; no real recipient or SMTP path was reached.

## 36. PHPMailer/SMTP/network/payment counters

Final functional matrix:

- wp_mail API attempts: 7.
- PHPMailer init attempts: 0.
- SMTP attempts: 0.
- WP HTTP API attempts: 0.
- Guard-intercepted HTTP: 0.
- Actual external network requests: 0.
- Payment calls: 0.

## 37. Legacy candidate-alert fingerprint

- Baseline count: 4.
- Final count: 4.
- Task-local baseline fingerprint: `e1fcdb45fb5faa790e5f0eecff3507d45bac54cf4cbfe2e9b94f5e6d33628237`.
- Final fingerprint: identical.
- Employer-to-candidate candidate-alert data was not migrated or changed.

## 38. job_alert cleanup baseline

- Baseline global job_alert count: 0.
- Final global job_alert count: 0.
- Final empty fingerprint: `4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945`.
- Fixture alerts/state/ledger existed only on exact marker-owned IDs.

## 39. WPJBP daily cron fingerprint

- Baseline and final exact event: timestamp `1778854026`, schedule `wpie_daily`, empty args hash `4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945`.
- Event count remained one.
- Daily event was not unscheduled, rescheduled, or run.

## 40. Owned hourly cron state

- Final exact event count: 1.
- Timestamp: `1788258663`.
- Schedule: `hourly`.
- Empty args hash: `4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945`.
- Duplicate bootstrap check: PASS.
- Final continuation event count: 0.

## 41. Action Scheduler fingerprint

- Pending count before: 7.
- Pending count after: 7.
- Task-local fingerprint before/final: `96f257a094b3c649c9db3fb593fa189e49ea5021c39128fa0c13f5d9a16df3d3`.
- Runner/due actions executed: NO.

## 42. ID32733

- Hook: `action_scheduler/migration_hook`.
- Final status: pending.
- Final attempts: 0.
- Executed: NO.

## 43. Source/runtime hashes

- `raspitajse-communications.php`: source/runtime `4bb9ff67d6a51c4011b107310094d855eafa9aca0ce8bcfca52f4adb750ad747`.
- `class-candidate-job-alert-delivery.php`: source/runtime `ce94a8c7f80a10db034f3e3e7f99c167282a0bfcf8b95c09d83efdd4389bcd3b`.
- `class-candidate-job-alert-integration.php`: source/runtime `4cc7786c83a8e0574814966463be86a443a1fd00048254edf388148370da1461`.
- PHP lint: PASS for all source/runtime changed PHP files.
- `git diff --check`: PASS.
- Source/runtime parity: PASS.

## 44. Cleanup proof and fixture diagnostics

- Fixture marker: `codex-z1-89-20260901-a7c91f`.
- Each functional run created 35 disposable posts and one disposable candidate user.
- All fixture posts, terms, alert state, ledgers, and claims cleaned by exact captured IDs/marker.
- Final: fixture posts 0; fixture users 0; claims 0; job_alert 0; continuation events 0.
- The long single-process WP-CLI harness consistently left its exact user row when calling `wp_delete_user()` in-process, while the same exact WordPress API call in a fresh staging process succeeded. This was a fixture lifecycle/cache issue, not an application-source defect.
- Therefore no fictitious 130/130 claim is made: corrected functional/runtime assertions were 129/129 PASS, and cleanup was separately executed and verified PASS in a fresh process.
- Initial recipient assertion also correctly identified that staging mail safety had already redirected `to`; corrected proof used the mail-safety original-context stack and passed without source changes.
- LiteSpeed cache purge messages during exact fixture post deletion were expected bounded technical housekeeping.
- HOST_NAMESPACE_PRESSURE remained active. Source/report work used the proven namespace-free direct Git/filesystem workflow; no hung helper was reused.

## 45. Safety

- Production touched: NO.
- Production database accessed: NO.
- Protected non-fixture staging business state mutated: NO.
- Real mail sent: NO.
- Actual SMTP: 0.
- Actual external network: 0.
- Payment calls: 0.
- Broad WP-Cron: NO.
- `wp cron event run`: NO.
- WPJBP daily-notice hook: NO.
- Action Scheduler runner/due actions: NO.
- ID32733: pending/0.
- Employer-to-candidate subsystem changed: NO.
- Matching product/AI roadmap changed: NO.
- SenderPolicy semantics changed: NO.

## Proposed next small task

`Zadatak 1.90 — Candidate-job alert scoped frequency UI and post-cutover observation`

Bounded scope only:

- add a create-form-only UI seam exposing the approved four new frequencies without globally breaking legacy biannual/annual labels;
- observe the single hourly event/daily five-callback graph and sanitized evaluator outcomes without running cron broadly;
- remove no compatibility class and redesign no matching/template/employer-alert subsystem.
