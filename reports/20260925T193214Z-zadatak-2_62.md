# Codex Execution Report

- Task: Zadatak 2.62 — Identify the WordPress runtime drift and reconciliation boundary
- Task ID: 2.62
- Result: PASS
- Recorded at (UTC): 2026-09-25T19:32:14Z
- Source branch: staging
- Source HEAD: f6093c11d71ddc175ebf83fc69453c20aaca5239
- Source working tree clean: YES
- Staging deploy marker: f6093c11d71ddc175ebf83fc69453c20aaca5239
- Staging environment: staging

## Task report

## Result

`PASS: WORDPRESS_RUNTIME_DRIFT_BOUNDARY_IDENTIFIED`

This is a read-only diagnosis. No source or runtime reconciliation was performed.

## Preconditions and scope

- Authorized repository: `/home/u601262303/repo/raspitajse-wp`.
- Clean local `staging` = `origin/staging` = declared baseline `f6093c11d71ddc175ebf83fc69453c20aaca5239`.
- Live deploy marker: `f6093c11d71ddc175ebf83fc69453c20aaca5239`.
- Environment: exact `staging`.
- Guards: mail safety active; cron recovery guard active; Action Scheduler drop guard active.
- No competing Git, rsync, deploy, `wp core update` or `wp core download` process was present.
- Canonical Git source reports WordPress `7.1.1`; runtime reports `7.1.2`. The required split still exists.
- Canonical scope only: `wp-admin/**`, `wp-includes/**`, and the 15 top-level files named by `deployment/deploy-staging.sh`. No `wp-content`, configuration, uploads, cache body, production path or private data entered the manifest.

## Deterministic canonical-core manifests

Manifest format was one bytewise path-sorted record per regular file:

`relative-path|file-type|size-bytes|sha256`

The aggregate hash is SHA-256 over the complete newline-terminated manifest. Symlinks and other non-regular/non-directory entries were enumerated separately. Source was the clean working tree proven equal to `origin/staging`; runtime was the exact staging root.

| Measure | Source 7.1.1 | Runtime 7.1.2 |
|---|---:|---:|
| Total regular files | 3,337 | 3,337 |
| `wp-admin` files | 593 | 593 |
| `wp-includes` files | 2,729 | 2,729 |
| canonical root files | 15 | 15 |
| Aggregate manifest SHA-256 | `2691c4852170892af2c6ba1ad2a8227c0a5b124cd43d59bf0f84a6940cc3369a` | `8b4f6e4a20b87010eeb3730a533fd5d16119ed49c6342f7f33f5a47f5b26e2a2` |
| Symlink/non-regular entries | 0 | 0 |

Delta counts:

- Source-only: `0`.
- Runtime-only: `0`.
- Content-different: `6`.
- Type-different: `0`.
- Missing/unsafe canonical root files: `0`.

### Exact changed paths and records

Root files — 1:

- `license.txt`
  - source: `f|19519|dec703cdb49042ca367f71c3982dee4552ffef395225f1fe9f14d29a4a0cb0ff`
  - runtime: `f|19903|49e97f629ce763abb2f8f1f13da74cb5a8a9a72a0e72a7477d1540c98cef5141`

`wp-admin` — 1:

- `wp-admin/about.php`
  - source: `f|17445|2cee6d4819b7854631d066b40227fffdc45637993fd67afc9cd526476eedbb6f`
  - runtime: `f|17994|048ef42465b3399bc24bfa953970917a839ea47249df7caca92fea23e31c9cb8`

`wp-includes` — 4:

- `wp-includes/images/crystal/license.txt`
  - source: `f|141|665c0494c1fca1c4b6d7421042a9dc51c0aa1e703403df6237070fe5ae58eb75`
  - runtime: `f|149|a41e19d4262122efd12bb47d29c2b9bec030f91bf60f7088e74a6a946735a615`
- `wp-includes/js/codemirror/csslint.js`
  - source: `f|366769|4b4d3dc9bdc969498ef6a163ed81d8092023e84d17c6c72de228e843de58c870`
  - runtime: `f|374004|b4384e34ca9091f8da24f9f40071567d8ed7b213a230f6d7313e0e1d4ade2c2b`
- `wp-includes/template.php`
  - source: `f|36824|578e56453cdf93c67c8f968b7fa53bf9a6f138ad8792e4e36d3bf567ca427be5`
  - runtime: `f|38826|63db56469e69056315eb75d4d96daaf2f5d40551595e5666a7cdb5bb71d36693`
- `wp-includes/version.php`
  - source: `f|1103|2061f926822154f17c421cf1499341b58b82cc3c6db7ebd1e1d4f57bd58b9def`
  - runtime: `f|1103|c822935799e6f3b83129882554ccff48596cf98b4a5eb82b83d82fe15455894e`

## Version consistency

Safe package fields:

| Field | Source | Runtime |
|---|---|---|
| WordPress version | 7.1.1 | 7.1.2 |
| DB version | 61833 | 61833 |
| TinyMCE package version | 49110-20250317 | 49110-20250317 |
| Required PHP | 7.4 | 7.4 |
| Required MySQL | 5.5.5 | 5.5.5 |

`wp-includes/version.php` is the sole package version declaration and identifies runtime as 7.1.2. The 7.1.2 identifiers in runtime `about.php`, `template.php`, and `version.php` are consistent with that version. Literal 7.1.1 strings remaining in unchanged files are API/source annotations, not competing package declarations. The equal path sets, six coherent content replacements, unchanged DB/TinyMCE requirements, and uniform changed-file mtime support a single patch-package transition, not a mixed or structurally partial tree. Exact upstream pristine parity cannot be claimed without an official 7.1.2 reference.

All six runtime-different files have the exact mtime `2026-09-24T21:03:52Z`.

## Deployment-marker truth boundary

The marker records the repository commit of the last successful invocation of the approved deploy script. It does not continuously attest current core bytes.

Exact script behavior:

1. Canonical package roots are `wp-admin`, `wp-includes`, WPForms Lite, and the 15 listed root files.
2. A `full` deploy reconciles those trees when the complete package source is available.
3. A `changed` deploy computes Git changes only between the previous marker and target HEAD. It sets `package_changed=1` only if that Git diff contains a canonical package path.
4. Full package reconciliation runs only when `package_changed=1`.
5. After the selected deploy mode, the script proves only Raspitajse Communications source/runtime parity, writes its manifest, and then unconditionally replaces the deploy marker with HEAD.

The six runtime core files changed at `2026-09-24T21:03:52Z`, outside the Task 2.53 deploy that had proven exact 7.1.1 parity. The later marker/communications metadata were written at `2026-09-25T14:02:20Z`. Current commit `f6093c11...` changes only `tools/codex-git-apply.sh`, so its changed deploy had no canonical package path, did not call `reconcile_package_trees`, and still wrote the marker.

Therefore the marker can validly remain/evolve to `f6093c11...` after an out-of-band core update. The false assurance is: `marker == origin/staging` proves only the last successful repository deploy identity, not current core byte parity or even the same reported WordPress patch version. Later tasks must not infer core reproducibility from marker equality alone.

## Drift-origin classification

Classification: `UNRESOLVED`.

Confidence:

- High confidence that this was a coherent out-of-band core patch replacement after Task 2.53 and before the later marker write.
- Low confidence in the actor/mechanism; local allowed evidence cannot distinguish Hostinger control-plane replacement from a manual/other out-of-band package operation.

Evidence:

- Task 2.53 proved exact source/live 7.1.1 parity with deletion detection on 2026-09-18.
- Git has no later canonical core change.
- All six different runtime files have one exact timestamp and no path-set/type drift.
- WordPress file modifications permit automatic updates: `WP_AUTO_UPDATE_CORE=minor`, `AUTOMATIC_UPDATER_DISABLED` undefined, `DISALLOW_FILE_MODS` undefined; stored dev/minor/major policies all map to enabled.
- However, WordPress built-in `WP_Automatic_Updater::after_core_update()` records a success through `auto_core_update_notified` before attempting notification delivery. The current sanitized record is stale (`success`, version 6.6.7, 2026-08-22), and there is no `auto_core_update_failed` record. This does not positively support a built-in 7.1.2 automatic update.
- The Hostinger plugin/runtime context exists, and a Hostinger-managed control-plane update is the strongest operational hypothesis, but no permitted local Hostinger event record proves it.
- Manual/other out-of-band replacement remains a competing explanation.
- `PARTIAL_OR_FAILED_PACKAGE_RECONCILIATION` is not supported: Task 2.53 proved parity; the approved script would checksum-reconcile the entire canonical package when triggered; the later tools-only deploy did not trigger it; the current path sets are identical.

No timestamp alone was used to assign an actor. Selecting `WORDPRESS_AUTOMATIC_CORE_UPDATE`, `HOSTINGER_MANAGED_CORE_UPDATE`, or `MANUAL_OR_OTHER_OUT_OF_BAND_UPDATE` would overstate the available evidence.

## Safe reconciliation decision

Decision: `BLOCKED_PENDING_OFFICIAL_PACKAGE_REFERENCE`.

Rationale:

- Current runtime boots as WordPress 7.1.2, retains DB schema 61833, and loads all three staging guards. No runtime failure was observed by the permitted read-only checks.
- Canonical 7.1.1 is complete in Git and was accepted under Task 2.53, but restoring it would knowingly downgrade a running patch version without a current pre-mutation 7.1.2 rollback snapshot or evidence that downgrade is preferable.
- Existing package rollback material is present but is WordPress 7.1, DB 61833, metadata commit `8d774c1e831d754c7e3f7e57842f61471a43f811`; it is not an exact rollback for the current 7.1.2 runtime. Its fixed directory also prevents the current script from creating a new snapshot until it receives an explicit safe disposition.
- No official WordPress 7.1.2 archive/checksum reference is locally available. Task 2.61 already classified core provenance as `UPSTREAM_REFERENCE_MISSING`.
- Without that reference, adopting live bytes could canonize an unverified package; restoring 7.1.1 could discard a security/maintenance patch and leaves no exact 7.1.2 rollback.
- Task 2.53 provides compatibility evidence for 7.1.1, not full 7.1.2 regression evidence. Current 7.1.2 bootstrap is encouraging but not a replacement for package provenance and focused smoke.

## Smallest safe unperformed follow-up contract

1. **Reference gate:** obtain an official exact WordPress 7.1.2 archive plus authoritative checksum/signature evidence in a separately authorized network-capable task. Stop if authenticity cannot be proven.
2. **Repository-only comparison:** regenerate the same canonical manifest for official 7.1.2, current Git 7.1.1, and runtime. Require the official/runtime comparison to have `0` source-only, `0` runtime-only and `0` content differences. Require official 7.1.2 versus Git to differ only in the six paths listed above. Any mismatch stops the task.
3. **Target after reference gate:** if and only if runtime equals official 7.1.2, choose `ADOPT_7_1_2_IN_CANONICAL_SOURCE`; do not downgrade. Create `feature/task-2.63-wordpress-7.1.2-reconcile` from exact `origin/staging` baseline.
4. **Feature scope:** import only official `wp-admin/**`, `wp-includes/**`, and the exact 15 canonical top-level files. `wp-content`, configuration and server-control files remain forbidden. Expected Git diff is exactly the six listed paths; any extra path stops the task.
5. **Static checks:** inspect the complete diff; prove no symlink/non-regular entry; run `php -l` on changed PHP files (`wp-admin/about.php`, `wp-includes/template.php`, `wp-includes/version.php`); run an appropriate syntax/static parse for changed JavaScript; verify version fields and DB version 61833.
6. **Rollback gate:** preserve the existing 7.1/Commit-A snapshot; do not overwrite or delete it implicitly. Obtain explicit approval for its bounded archival/disposition or update the deployment tooling in a separately reviewed change to support immutable per-commit snapshots. Before deploy, create and verify a fresh package snapshot of the exact current 7.1.2 runtime, including WPForms, tied to the feature commit. If the fixed snapshot slot remains occupied or the new snapshot cannot be proven exact, stop.
7. **Lock order for later runtime mutation:** one persistent worker must nonblocking-lock `/tmp/raspitajse-staging-owned-cron-runner.lock`, then nonblocking-lock `/tmp/raspitajse-staging-runtime.lock`. If either is busy, release any lock acquired by that worker and stop with `BLOCKED: STAGING_LOCK_BUSY`. Hold both continuously through snapshot, deploy, validation and any rollback; release runtime lock first, owned-runner lock second. Never alter lock files or holders.
8. **Deploy:** after all gates, use only `deployment/deploy-staging.sh changed feature/task-2.63-wordpress-7.1.2-reconcile`. The six package-path changes must trigger full canonical package reconciliation with deletion detection.
9. **Focused smoke:** prove exact environment `staging`; source/runtime canonical manifest equality; WordPress 7.1.2 and DB 61833; normal WordPress/admin/plugin bootstrap; WPForms Lite active; Raspitajse Communications, Commerce and active child theme load; guards remain active; one bounded HTTP check each for home, login/register and jobs only if that follow-up explicitly authorizes HTTP. No mail, cron, Action Scheduler, fixtures or payment execution.
10. **Failure handling:** on deploy/parity/bootstrap/smoke failure, reacquire/retain the same locks, restore the verified fresh snapshot through the approved package restore path, prove the pre-task manifest/version/marker state, publish failure, and stop. Never fall back to the older 7.1 snapshot without an explicit separate contract.
11. **Integration:** only after all gates pass, fast-forward/push `staging` to the exact feature commit without force. Final required equality: local `staging` = `origin/staging` = live marker; source manifest = runtime manifest; runtime version = canonical 7.1.2. Preserve the feature/package rollback until the task's explicit cleanup point.
12. **Stop conditions:** missing/unverified official reference; unexpected package path/hash; dirty or shifted baseline; production path; secret/config need; unsafe file type; occupied/unresolved snapshot slot; lock contention; DB-version change; guard loss; smoke failure; rollback uncertainty.

No part of this contract was executed in Zadatak 2.62.

## Zero-mutation counters

- Source/runtime files changed: `0`.
- Source-branch Git commits/branches/merges/pushes: `0/0/0/0`.
- Deploy/snapshot/restore operations: `0/0/0`.
- WordPress/database/cache mutations: `0`.
- Update checks/downloads: `0/0`.
- Fixtures: `0`.
- HTTP requests: `0`.
- Scheduler/mail/payment execution: `0/0/0`.
- Mutation locks acquired: `0`.
- Production access: `0`.
- Task-private `/tmp` manifest files were removed after their hashes/counts/deltas were recorded; this did not change source or runtime.
- The single required report-only commit/push is isolated to `codex-reports` and is not an application/source mutation.

## Retained findings and final state

- Source worktree: clean `staging` at `f6093c11d71ddc175ebf83fc69453c20aaca5239`.
- `origin/staging`: `f6093c11d71ddc175ebf83fc69453c20aaca5239`.
- Live marker: `f6093c11d71ddc175ebf83fc69453c20aaca5239`.
- Runtime: `staging`, WordPress 7.1.2; canonical source 7.1.1.
- Mail, cron and Action Scheduler guards: active.
- HCDN blocker retained from Task 2.60.5: no proven URL-scoped Hostinger HCDN eviction path for staging `/robots.txt`.
- WP Private Message D1 retained from Task 2.61: two vendor lines change initial/reply notification From identity; `PROVEN_DIRECT_DELTA`, classification `UNRESOLVED`, pending official 1.0.7 reference and fixture-backed mail regression.
- Production touched: NO.
