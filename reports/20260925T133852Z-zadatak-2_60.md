# Codex Execution Report

- Task: Zadatak 2.60.3 — Identify the staging search response layer
- Task ID: 2.60
- Result: PASS
- Recorded at (UTC): 2026-09-25T13:38:52Z
- Source branch: staging
- Source HEAD: f6093c11d71ddc175ebf83fc69453c20aaca5239
- Source working tree clean: YES
- Staging deploy marker: f6093c11d71ddc175ebf83fc69453c20aaca5239
- Staging environment: staging

## Task report

## Outcome
- PASS: STAGING_SEARCH_RESPONSE_LAYER_IDENTIFIED
- Exact classification: `LOCAL_WEB_CACHE_OR_STATIC_LAYER`.
- The WordPress hook contract was active and correct, while two local web-serving mechanisms bypassed the fresh WordPress response: LiteSpeed page cache for the home page and a physical zero-byte `robots.txt` in the staging document root.
- No source change, commit, merge, or integration was performed.

## Baseline and scope
- Source branch before and after: `staging`.
- Source HEAD and `origin/staging`: `f6093c11d71ddc175ebf83fc69453c20aaca5239`.
- Exact temporary feature inspected and deployed: `origin/feature/task-2.60.2-staging-search-isolation` at `783cc6b3425985c1e27de8e823fca020d0d7b869`.
- The feature is a direct child of the baseline and its only diff is `wp-content/mu-plugins/raspitajse-staging-search-isolation.php`.
- Initial and final staging deploy marker: `f6093c11d71ddc175ebf83fc69453c20aaca5239`.
- Staging environment was proven as exactly `staging`; production was not accessed or changed.

## Controlled reproduction
- Both required nonblocking staging locks were acquired in the prescribed order and held across the temporary deployment, option change, probe creation, evidence capture, and rollback-sensitive work; they were released in reverse order.
- Temporarily deployed the exact feature and changed only `blog_public` from `1` to `0`.
- Created exactly one random, task-owned, non-symlink static probe below the staging MU-plugin directory. Its content was only the random nonce; its path and nonce are intentionally omitted.
- The probe SHA-256 was captured before requests and matched both the local-origin and public responses.
- No business fixture, user, job, application, order, message, mail, or scheduler state was changed.

## WordPress bootstrap evidence
- `blog_public=0`; the isolation MU plugin existed and matched the feature copy.
- All four owned hooks were registered at `PHP_INT_MAX`: `wp_headers`, `wp_robots`, `aioseo_robots_meta`, and `robots_txt`.
- Direct filter evaluation produced `X-Robots-Tag: noindex, nofollow`, core robots noindex/nofollow, AIOSEO noindex/nofollow, and global `User-agent: *` plus `Disallow: /` without a sitemap.
- Staging mail safety, owned-cron recovery protection, and Action Scheduler drop protection remained active.
- LiteSpeed Cache and the external object cache were active.

## HTTP evidence matrix
- Local web origin, with staging SNI/Host: exactly 3 GETs and 0 redirects. Home returned `200` without the owned header or meta; `robots.txt` returned `200` without the owned rules; the static probe returned `200` with an exact content-hash match.
- Public normal path: exactly 3 GETs and 0 redirects. Home returned a stale LiteSpeed cache hit with a nonzero multi-day `Age` and lacked the owned header/meta. `robots.txt` returned `200` without the owned rules. The static probe returned `200` with the same exact hash, proving the public host and local origin used the same staging document root.
- Public diagnostic path: exactly 2 GETs and 0 redirects, using collision-resistant query parameters and request no-cache directives. Home returned a LiteSpeed cache miss with the required owned header and noindex meta. `robots.txt` still returned `200` without the owned rules.
- Total diagnostic HTTP requests: 8 GETs; redirects followed: 0.
- DNS comparison showed the public address was not a local interface address; no raw addresses were recorded.
- Cache mutation, purge, flush, warm, crawler, or broad invalidation commands executed: 0.

## Decisive filesystem evidence
- The staging document root contains an exact physical `robots.txt` that is a regular non-symlink file, size 0 bytes, with the SHA-256 of an empty file.
- It contains no user-agent, disallow, or sitemap directives.
- This static file explains why both normal and diagnostic `robots.txt` requests bypassed WordPress.

## Classification rationale
- `APPLICATION_HOOK_CONTRACT_FAILURE` is excluded because direct bootstrap/filter evaluation passed completely.
- `ROUTING_OR_DOCROOT_MISMATCH` is excluded because the same task-owned probe was served with an exact hash match through local origin and the public host.
- `UPSTREAM_PROXY_OR_CDN_CACHE` is not the exact outcome because the stale behavior was already present at the local web origin.
- The home-page split between a normal LiteSpeed hit and a diagnostic miss, together with the physical static `robots.txt`, establishes exactly `LOCAL_WEB_CACHE_OR_STATIC_LAYER`.

## Smallest follow-up corrective contract
- Owner 1: the LiteSpeed local page-cache entry for the staging home URL.
- Owner 2: the exact zero-byte, non-symlink staging document-root `robots.txt`.
- Under the same two locks, a follow-up may reproduce the exact feature state, preserve and verify the exact static-file metadata/hash, temporarily remove only that one file so WordPress owns `/robots.txt`, and use LiteSpeed's URL-scoped `litespeed_purge_url` action only for the exact staging home and robots URLs.
- Forbidden in that follow-up: purge-all, cache flush-all, crawler/warm operations, wildcard invalidation, broad file operations, production access, and any business-state mutation.
- Acceptance must use exactly one normal public GET for `/` and one for `/robots.txt`, with no cache-busting query or request no-cache headers. Both must satisfy the feature's required header/body contract before any integration.
- On any failed gate, restore the exact saved static file, restore `blog_public=1`, deploy the baseline, remove the feature plugin, and use only the same two URL-scoped invalidations needed to prevent diagnostic content from remaining cached. Verify hashes, marker, guards, environment, and clean Git alignment.

## Rollback and final state
- Verified the task-owned probe hash, removed exactly that probe, and confirmed it absent.
- Restored `blog_public=1`.
- Redeployed `staging`; the isolation MU plugin is absent and the deployment marker is restored to the baseline SHA.
- Preserved the pre-existing zero-byte staging `robots.txt` unchanged.
- Final branch is `staging`; local HEAD equals `origin/staging`; working tree is clean.
- Final runtime: environment `staging`, `blog_public=1`, mail safety active, owned-cron recovery protection active, and Action Scheduler drop protection active.
- Rollback: PASS.

## Warnings / errors
- One known Hostinger namespace-limit `ENOSPC` signature occurred during a final read-only source check after rollback. It was classified as `HOST_NAMESPACE_PRESSURE`; the sandbox attempt was not retried, and the mandated namespace-free path was used. It did not affect runtime evidence, rollback, Git state, or the result.

## Safety
- Protected staging business state changed: NO.
- Production touched: NO.
