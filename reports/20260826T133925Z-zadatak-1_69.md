# Codex Execution Report

- Task: Zadatak 1.69 — Audit remaining package UI and legacy overrides
- Task ID: 1.69
- Result: PASS
- Recorded at (UTC): 2026-08-26T13:39:25Z
- Source branch: staging
- Source HEAD: 8fb70d72271aa29d289254df1a3b2067d38adeba
- Source working tree clean: YES
- Staging deploy marker: 8fb70d72271aa29d289254df1a3b2067d38adeba
- Staging environment: staging

## Task report

## Summary
- Strictly read-only audit completed for the active employer `job_package` UI/runtime.
- No source, staging data, package/product, order, entitlement, job, user, cron, Action Scheduler, payment, mail, vendor, parent-theme, or deployment state was changed.
- Canonical entitlement authorization remains server-side in `Raspitajse_Commerce_Job_Package_Policy`; the active selection UI consumes the same status decision.
- No active runtime read/write of `_wjbp_package_expiration_*` remains. The only code residue is an overridden, dormant Superio parent template; staging contains `0` matching usermeta rows.
- Audit result is `PASS` for discovery/classification, with presentation debt and one independent candidate-access business decision explicitly deferred.

## Runtime template resolution map

| Concern | Requested template/widget | Active resolved file | Child override? | Parent fallback? | Vendor fallback? |
|---|---|---|---:|---:|---:|
| Submit flow shell / package purchase selection | `choose-package-form` | `wp-content/themes/superio-child/template-paid-listings/choose-package-form.php` | YES | dormant | dormant |
| Existing entitlement selection | `user-packages` | `wp-content/themes/superio-child/template-paid-listings/user-packages.php` | YES | dormant | dormant |
| Start/Pro/Partner cards inside submit/dashboard forms | `packages` | `wp-content/themes/superio/template-paid-listings/packages.php` | NO | YES, active | dormant |
| Employer dashboard packages | Elementor `apus_element_jobs_user_packages` | `wp-content/themes/superio-child/inc/vendors/elementor/wc-paid-listings-widgets/user_packages.php` | YES | dormant | N/A |
| Dashboard package status | same dashboard widget | same child Elementor file, lines 238-254 | YES | dormant | N/A |
| Dashboard package expiry | same dashboard widget | same child Elementor file, lines 207-220 | YES | dormant | N/A |
| Dashboard quota/duration | same dashboard widget | same child Elementor file, lines 200-220 | YES | dormant | N/A |
| Submit status/expiry/quota | `user-packages` | child `template-paid-listings/user-packages.php`, lines 25-105 | YES | dormant | dormant |
| Pricing-page package grid | Elementor `apus_element_jobs_packages` | `wp-content/themes/superio/inc/vendors/elementor/wc-paid-listings-widgets/packages.php` | NO | YES, active | N/A |
| Home single package card | Elementor `apus_element_jobs_single_package` | `wp-content/themes/superio/inc/vendors/elementor/wc-paid-listings-widgets/package.php` | NO | YES, active | N/A |
| Start/Pro/Partner CTA in parent template | `packages` | Superio parent `template-paid-listings/packages.php`, lines 21-33 and 91-99 | NO | YES, active | dormant |

Runtime proof:
- Staging environment: `staging`; stylesheet `superio-child`; parent `superio`.
- Template loader resolved `choose-package-form` and `user-packages` to the child, and `packages` to the parent.
- Reflection resolved `Superio_Elementor_Jobs_User_Packages` to the child file.
- Published Elementor placements: dashboard page uses `apus_element_jobs_user_packages`; pricing page uses `apus_element_jobs_packages`; one published home page uses `apus_element_jobs_single_package`.
- Dormant vendor/parent files were not treated as active behavior.

## Canonical policy consumer inventory

| Location | Consumer | Data source | Policy call | Own business calculation? | Classification |
|---|---|---|---|---:|---|
| `raspitajse-commerce/...job-package-policy.php:240-387` | UI-facing entitlement query/status/date API | canonical entitlement post/meta | `get_packages_by_user`, `get_status`, `get_valid_until`, `is_usable` | YES, canonical owner | KEEP |
| same file, `choose_package_handler` and `process_package_handler` | server authorization before selection and immediately before mutation/quota use | canonical policy | `is_usable` | NO | KEEP |
| child `choose-package-form.php:12-20` | entitlement inventory for submit UI | canonical query, with dormant vendor fallback | `get_packages_by_user` | fallback duplicates old source | REDESIGN |
| child `user-packages.php:25-77` | available/disabled/checked/radio state | canonical status | `get_status` | NO | KEEP |
| child `user-packages.php:35-65,88-105` | used quota, limit, job duration, expiry and local labels | entitlement snapshots plus canonical expiry | `get_valid_until`; local meta/label map | presentation arithmetic/duplication, not authorization | REDESIGN |
| child Elementor widget `:84-86,238-254` | dashboard inventory/status | canonical query/status, with dormant fallback | `get_packages_by_user`, `get_status` | NO in canonical path | KEEP |
| child Elementor widget `:200-220` | dashboard feature/quota/duration/date | entitlement snapshots plus canonical expiry | `get_valid_until` | presentation arithmetic/duplication | REDESIGN |
| active parent `packages.php` and parent Elementor product widgets | purchasable catalog cards | published Woo `job_package` products | none | no entitlement decision; catalog-only | KEEP |
| child JS `preselectActivePackage` | selection convenience | server-emitted `disabled` state | none | selects first enabled radio | DROP |
| child completed-order body classes | inferred access/presentation state | completed orders + product names | none | YES, invalid parallel model | DROP — DUPLICATED/INVALID AUTHORIZATION |

Target dependency is `raspitajse-commerce canonical policy -> small view model -> presentation`; only server handlers authorize.

## Remaining legacy-expiry residue
- Active runtime reads: `0`.
- Active runtime writes: `0`.
- Active child template references: `0`.
- Staging `_wjbp_package_expiration_*` usermeta rows: `0`.
- Dormant/dead code: `wp-content/themes/superio/template-paid-listings/user-packages.php:37-73` reads the old key, derives expiry from individual `job_duration`, writes usermeta during rendering, logs values, and sets `package-expired`. It is overridden by the child template and is not runtime source of truth.
- Documentation/comment residue: comments and debug log strings are confined to that dormant parent file.
- CSS/JS: no remaining `.package-expired` selector or JS hide rule. `.action.expired` is an active canonical status presentation class, not legacy expiry state.
- Vendor templates do not read `_wjbp_package_expiration_*`.

## Completed-order / product-name / body-class residue
- `superio-child/functions.php:64-101` actively runs `wc_get_orders(status=completed)`, derives product-name body classes, and logs user/product state on frontend requests.
- `superio/page-dashboard.php:18` calls the same query again and discards the result; the body-class filter already calls it, so dashboard requests duplicate the work/logging.
- `superio-child/functions.php:104-117` `user_has_package()` has no caller and is a `DROP CANDIDATE`.
- `superio-child/style.css:81-83` hides the whole candidate archive for `.purchased-nothing`. This is access behavior encoded in CSS from completed-order/product-name history, not canonical entitlement state: `DROP — DUPLICATED/INVALID AUTHORIZATION` for the current mechanism.
- Whether candidate archive access should exist for a separate product/business purpose is outside employer `job_package` scope and is `NEEDS BUSINESS DECISION`; do not silently replace it with the job-package policy.
- `get_user_subscription_status()` at child `functions.php:40-61` is explicitly marked not in use and has no caller: `DROP CANDIDATE`.

## Start / Pro / Partner UI map

| Package | Purchase card | Self-service CTA | Quota shown | 30-day package validity shown | Job duration shown | Canonical policy used? |
|---|---|---|---:|---:|---:|---|
| Start | active in submit/dashboard parent cards and Woo/Elementor catalog | YES, `Počni besplatno` / Woo add-to-cart | 2 | NO | YES, 15 days | Not on purchase card; canonical after entitlement activation |
| Pro | active in submit/dashboard parent cards and Woo/Elementor catalog | YES, `Ubrzaj zapošljavanje` / Woo add-to-cart | 3 | NO | YES, 30 days; main copy `(30 dana)` is ambiguous, short copy identifies ad duration | Not on purchase card; canonical after entitlement activation |
| Partner | active in submit/dashboard parent cards and Woo/Elementor catalog | YES, `Reši zapošljavanje` / Woo add-to-cart | 10 | NO | YES, 30 days in detailed copy | Not on purchase card; canonical after entitlement activation |

- All three published products are `job_package`, purchasable, and self-service. The active parent template submits all three, including Partner.
- The vendor `packages.php` branch that turns Partner into `type=button` is dormant and not runtime behavior.
- No different hidden authorization model was found among Start/Pro/Partner.
- `PRODUCT COPY / POLICY MISMATCH`: none of the cards discloses the separate 30-day entitlement window from first activation. Start correctly labels its 15-day individual job duration. Pro's compact `(30 dana)` is ambiguous without the detailed copy. Partner's detailed copy describes individual ads as 30 days.

## Dashboard UX state
Current active dashboard widget shows title, internal entitlement ID/type, used count, limit, individual job duration, canonical valid-until date, and canonical status. It does not show remaining quota directly and has no distinct Historical state. Revoked/cancelled entitlements are included and shown as `Opozvan`; the runtime currently has no published entitlement and 10 cancelled historical records in aggregate.

Minimal target states:
- Available: title, `remaining / total`, `valid until`, usable status.
- Exhausted: title, `0 remaining`, expiration context, disabled/no-use action.
- Expired: title, retained remaining quota if any, expired date, disabled/no-use action.
- Revoked: title and revoked status, no use action.
- Historical: collapsed/archive presentation for old entitlements with no current business significance; the current canonical API does not define this separate state, so criteria need a design decision before implementation.
- Remove internal ID/type columns from the employer-facing default view unless support has a documented need.

## Job submission parity matrix

| Fixture state | Server policy | Active UI state | Evidence |
|---|---|---|---|
| active + quota | usable | visible, enabled, first usable selectable | same `get_status`; server rechecks `is_usable` twice |
| exhausted | unusable | visible, `Potrošen`, disabled/not selectable | same `get_status` |
| expired + quota | unusable | visible, `Istekao`, valid-until shown, disabled/not selectable | same `get_status` |
| revoked | unusable | visible, `Opozvan`, disabled/not selectable | cancelled included by canonical query |
| wrong owner | unusable | not visible | canonical query owner meta filter; server owner recheck |
| non-publish | unusable | not visible, except explicit cancelled/revoked history | canonical query allows only publish/cancelled; status rejects other states |

- Static flow proves server/UI use the same canonical result. Zadatak 1.68 fixture evidence already covered active/exhausted/expired/revoked rendering; 1.69 created no fixture.
- UI cannot authorize a disabled/stale selection because both server phases re-check canonical `is_usable`.
- Remaining mismatch is informational: UI says `Iskorišćena kvota` rather than showing remaining quota; dashboard similarly shows used and limit separately.
- A publish entitlement with missing canonical activation metadata would be visible as `Nedostupan` and disabled. This is fail-closed, not an authorization mismatch.

## CSS / JavaScript package rules

| Location / selector or function | Purpose | Source state | Render only? | Changes business decision? | Classification |
|---|---|---|---:|---:|---|
| JS `.action` Active/Expired translation, lines 148-158, called by 800ms global poll | translate old English status labels | server now emits Serbian canonical labels | YES | NO | DROP, redundant package branch |
| JS `.package-more-toggle`, lines 284-294 | custom details toggle | `data-target` contains `#`, but `getElementById` receives it unchanged, so handler is a no-op; Bootstrap collapse remains | intended presentation | NO | DROP |
| JS `preselectActivePackage`, lines 296-323 | auto-select first enabled radio | server already checks first canonical available radio | UX | NO; it respects `disabled` | DROP |
| JS package click handler, lines 327-387 | AJAX add-to-cart, update review, hard-coded `/checkout/` redirect | Woo product/catalog state | transport UX | NO entitlement decision | REDESIGN to localized Woo endpoints/native form flow |
| CSS `.package-partner`, `.package-pro`, `.package-start`, `.subwoo-inner`, feature/more selectors, lines 229-337 | card presentation | product slug/server markup | YES | NO | KEEP; fix brittle selector during redesign |
| CSS `.action.expired`, lines 273-275 | canonical expired status color | server status class | YES | NO | KEEP |
| CSS `.woocommerce-shop .product.post-1728/1729/1730`, lines 523-550 | card sizing/color by fixed product IDs | product IDs | YES | NO | REDESIGN to semantic slug/class |
| CSS `.package-pro.button-action`, line 325 | intended Pro button spacing | selector does not match descendant markup | YES, but dead | NO | DROP CANDIDATE |
| CSS `.post-type-archive-candidate.purchased-nothing`, lines 81-83 | hide candidate archive | completed orders/product names | NO, creates access result | YES | DROP current rule; underlying access decision required |
| CSS order-confirmation second paragraph, lines 604-610 | presentation for package activation message | order-page markup | YES | NO | KEEP |

Target rule is satisfied for canonical entitlement authorization: JS never enables a server-disabled entitlement. Redundant JS still increases UI fragility and should be removed in a bounded cleanup.

## Child-theme ownership classification
- `KEEP IN CHILD THEME`: visual package/card/status CSS, localized labels, layout-only markup, and safe escaped rendering.
- `MOVE / REDESIGN`: the duplicated status maps and direct quota/date assembly in two child renderers should consume a small Raspitajse-owned view model; the 306-line Elementor override should shrink to a renderer/hook; AJAX purchase navigation should use Woo-localized/native URLs.
- `DROP`: completed-order/product-name package inference, uncalled helpers, broken/redundant package JS, dormant non-job branches in the job-only dashboard query, and vendor fallbacks inside canonical child consumers.

## Vendor / parent drift risks
- Child Elementor `user_packages.php`: 306 lines versus parent 342; diff is 234 insertions / 270 deletions. It copies the whole widget and registration contract. Elementor API changes, parent column/markup changes, or package-type changes can break it. `HIGH OVERRIDE DRIFT RISK`. Replace with a small owned view model plus minimal hook/renderer.
- Child `user-packages.php`: 120 lines versus parent 163; diff 87 insertions / 130 deletions. Full selection markup is justified short-term for canonical disabled state, but remains coupled to Paid Listings field names and submit flow. `HIGH OVERRIDE DRIFT RISK`; move data decisions to view model and keep only a narrow presentation partial.
- Child `choose-package-form.php`: 44 lines versus parent 39; diff 32 insertions / 28 deletions. Nearly full copy for one query difference. Prefer a filter/query seam or a smaller wrapper. Medium/high drift.
- Active Superio parent `packages.php`: 109 lines versus vendor 75; diff 61 insertions / 27 deletions. It is tightly coupled to vendor POST names and Woo product internals, contains a malformed heading attribute (`id="available-packages class="widget-title"`), and can drift on vendor updates. `HIGH OVERRIDE DRIFT RISK` even though it is not child-owned.
- Parent Elementor `packages.php` and `package.php` are active presentation fallbacks and depend on `woocommerce_after_shop_loop_item`; no entitlement authorization is present.

## Dead code / DROP candidates
- Child `functions.php:40-61`: unused subscription-status helper.
- Child `functions.php:104-117`: uncalled `user_has_package`.
- Parent dashboard `page-dashboard.php:18`: duplicate discarded completed-order lookup.
- Child Elementor `user_packages.php:134-199`: CV/contact/candidate/resume switch branches are unreachable because the active canonical query returns only `package_type=job_package`.
- Child JS `:284-294`: broken no-op details handler.
- Child JS `:296-323`: redundant first-radio preselection.
- Child JS `:148-158`: package-status translation residue under continuous polling.
- Child CSS `.package-pro.button-action`: non-matching selector.
- Dormant parent `template-paid-listings/user-packages.php`: old expiration computation/write/debug implementation.
- Dormant vendor `templates/packages.php`: obsolete Partner non-submit experiment relative to confirmed self-service policy.
- Child vendor-query fallbacks in `choose-package-form.php:17-20` and dashboard widget line 86: inactive while canonical service is loaded and undesirable as a second source in failure mode.

## Classification table

| # | Location / rule | Current purpose | Runtime active? | Source of truth | Classification | Proposed target |
|---:|---|---|---:|---|---|---|
| 1 | Commerce policy UI API | status/query/date | YES | canonical | KEEP | expose view model in addition |
| 2 | Child choose template | submit shell/query | YES | canonical + fallback | REDESIGN | minimal query seam/wrapper |
| 3 | Child selection radio state | selectable/disabled | YES | canonical | KEEP | presentation over view model |
| 4 | Child selection info | quota/date/duration/labels | YES | mixed presentation meta | REDESIGN | shared view model |
| 5 | Child selection CTA anchor | jump to cards | YES, broken target | markup | REDESIGN | valid semantic target/button |
| 6 | Dashboard query/status/date | package states | YES | canonical | KEEP | retain through view model |
| 7 | Dashboard quota/history UX | package information | YES | mixed | REDESIGN | remaining + five-state UX |
| 8 | Dashboard non-job switch branches | copied other package types | NO in current query | vendor-era | DROP | remove from job-only renderer |
| 9 | Parent purchase cards | product catalog/CTA | YES | Woo catalog | KEEP | presentation only |
| 10 | Parent Elementor catalog/single widgets | pricing/home cards | YES | Woo catalog | KEEP | presentation only |
| 11 | JS preselection | first enabled radio | YES | server DOM | DROP | server default only |
| 12 | JS custom more-toggle | details | registered but no-op | DOM | DROP | Bootstrap/native disclosure |
| 13 | JS AJAX purchase | add-to-cart/checkout | YES | Woo catalog/session | REDESIGN | localized native Woo flow |
| 14 | JS action translation poll | old status translation | YES but redundant | rendered text | DROP | server localization |
| 15 | Semantic package CSS | card/status visuals | YES | server markup | KEEP | retain |
| 16 | Fixed-ID/typo CSS | product visuals | partly | product IDs | REDESIGN | semantic slug classes |
| 17 | Completed-order body-class pipeline | infer products/access | YES | completed orders/names | DROP | no job-package authorization use |
| 18 | `user_has_package` | inferred package helper | NO caller | completed orders/names | DROP | remove |
| 19 | subscription helper | debug/experiment | NO caller | subscriptions | DROP | remove |
| 20 | candidate archive hide | access via CSS | YES | body class | NEEDS BUSINESS DECISION | decide independent candidate-access policy; current rule must not authorize |
| 21 | Parent legacy-expiry template | expiry/write/render | NO, overridden | old usermeta/job duration | DROP | leave dormant until safe parent update; never reactivate |
| 22 | Vendor Partner no-submit branch | restrict Partner CTA | NO | product slug | DROP | confirmed self-service behavior |
| 23 | Child vendor query fallbacks | plugin-missing fallback | NO normal runtime | vendor query | DROP | fail closed / owned API only |
| 24 | Product copy | quota/duration/validity | YES | product content | REDESIGN | disclose separate 30-day validity |
| 25 | gettext package labels | localization | YES | translations | KEEP | retain/centralize |
| 26 | Order-confirmation package CSS | presentation | YES | order markup | KEEP | retain |
| 27 | Full child Elementor override | dashboard renderer | YES | mixed | REDESIGN | narrow renderer/view model |

Totals: `KEEP 8`, `REDESIGN 8`, `DROP 10`, `NEEDS BUSINESS DECISION 1` (27 classified findings).

## Package-related lines/files removable without package-policy change
High-confidence, source-owned cleanup candidates:
- Child `functions.php:40-61` (22 lines): unused subscription helper.
- Child `functions.php:104-117` (14 lines): uncalled completed-order name helper.
- Child JS `:284-294` (11 lines): no-op duplicate collapse handler.
- Child JS `:296-323` (28 lines): redundant radio preselection; server already emits canonical checked/disabled state.
- Child Elementor `:134-199` (66 lines): unreachable non-job package-type branches in the canonical job-only query, subject to a focused render regression check.
- Package-specific JS status translation `:148-158` (11 lines) is authorization-neutral but may change localization in unrelated `.action` widgets; remove only after a page-level presentation check.
- Fallback branches are removable without changing normal canonical runtime, but should be handled deliberately as fail-closed behavior rather than bundled into dead-code deletion.

Not safe to remove without a separate decision/test:
- completed-order body-class pipeline and candidate archive CSS, because current candidate visibility would change;
- active full dashboard/selection overrides;
- parent/vendor files, which must not be edited as part of Raspitajse-owned cleanup;
- fixed product-ID CSS if exact current shop appearance must remain.

## Minimal proposed target UI architecture
1. `Raspitajse_Commerce_Job_Package_Policy` remains the only authorization and lifecycle owner.
2. Add one read-only owned entitlement view model returning: ID, title, status, usable, used, limit, remaining, valid-until, individual-job-duration, feature flags, and an explicitly designed historical flag.
3. Server submit wrappers continue to call `is_usable` immediately before both selection and mutation.
4. Child renderers consume only the view model; no expiry/quota/status calculation, completed-order lookup, or product-name inference.
5. Product purchase cards remain a separate Woo catalog presentation. Their copy clearly distinguishes `package usable for 30 days from activation` from `each job visible for 15/30 days`.
6. JS may enhance disclosure/navigation but must never derive, enable, hide, or authorize entitlement state.
7. Reduce full template/widget copies to minimal hooks/partials where upstream seams permit; keep vendor/parent files untouched.

## Recommended Zadatak 1.70
**Zadatak 1.70 — Remove proven dead package UI helpers and redundant selection JavaScript**

Small bounded implementation only:
- remove uncalled child `get_user_subscription_status()` and `user_has_package()`;
- remove the broken custom package-details handler and redundant first-enabled radio preselection;
- retain the same server-rendered canonical checked/disabled behavior;
- run static checks plus a disposable/UI-only render regression for zero/one/multiple available entitlements, without changing package policy;
- do not yet rewrite the dashboard widget, product copy, completed-order candidate visibility, or parent/vendor templates.

The remaining dashboard view-model/UX work should first receive a separate design contract because Historical semantics and candidate archive access require explicit decisions.

## Validation
- PASS: repository/source worktree clean before and after audit.
- PASS: runtime environment proved exactly `staging`.
- PASS: active template paths and Elementor class file proved at runtime.
- PASS: package product catalog inspected read-only; 3 published purchasable job-package products (Start/Pro/Partner).
- PASS: no active legacy-expiry source-of-truth reads/writes; staging legacy-expiry usermeta count `0`.
- PASS: server/UI policy call-site parity inspected statically; no fixture created in 1.69.
- PASS: no mail, external HTTP transport, cron, Action Scheduler, payment, order, entitlement, job, user, package, or product action executed.
- Note: the first read-only product inspection called a non-existent product accessor and exited with a WP-CLI fatal before any mutation; the corrected read-only inspection used the actual product meta contract and passed.

## Runtime state
- Source branch: `staging`.
- Source HEAD: `8fb70d72271aa29d289254df1a3b2067d38adeba`.
- Local `origin/staging`: `8fb70d72271aa29d289254df1a3b2067d38adeba`.
- Staging branch SHA: `8fb70d72271aa29d289254df1a3b2067d38adeba`.
- Staging deployed SHA: `8fb70d72271aa29d289254df1a3b2067d38adeba`.
- Deploy performed: NO.
- Production touched: NO.

## Warnings / errors
- Active parent `packages.php` has malformed heading markup, so the child `#available-packages` CTA target does not resolve correctly.
- Current dashboard and submit UI display used quota, not the requested remaining quota.
- Current product cards omit the separate canonical 30-day package validity; Pro compact copy is ambiguous.
- The active completed-order/product-name body-class pipeline remains outside canonical entitlement policy and performs duplicated queries/logging on dashboard pages.
- No cleanup was implemented; all findings are recommendations only.

## Safety
- Read-only task; no staging mutation and no deployment.
- Production touched: NO.
