# Codex Execution Report

- Task: Zadatak 2.12 — Lightweight steady-state runner redesign (Stage A)
- Task ID: 2.12
- Result: PARTIAL
- Recorded at (UTC): 2026-09-05T13:52:19Z
- Source branch: staging
- Source HEAD: 77d3a1019e0248a2abacd607fd508ed6868da70b
- Source working tree clean: YES
- Staging deploy marker: 77d3a1019e0248a2abacd607fd508ed6868da70b
- Staging environment: staging

## Task report

## Result
- PARTIAL — READY_FOR_HUMAN_NATURAL_FIRE_OBSERVATION.
- Stage A implementation, integration, deployment, and guarded T0 acceptance PASS.
- Whole-task PASS remains intentionally pending the first complete natural Hostinger provider fire after T0.

## Baseline and revisions
- Required baseline: `43ac33e6e96fdc0062233e6419cfb761633113c5` (verified fresh before work).
- Primary feature: `feature/z2-12-lightweight-cron`, feature SHA `a44bc11ad6ae6701a5746d16f14f1688b9c7fdfd`.
- Narrow schema correction: `feature/z2-12-progress-schema`, final SHA `77d3a1019e0248a2abacd607fd508ed6868da70b`.
- Final local `staging` = fresh `origin/staging` = deploy marker = manifest commit: `77d3a1019e0248a2abacd607fd508ed6868da70b`.
- Source worktree clean: YES.

## Files changed
- `tools/raspitajse-staging-owned-cron-runner.sh` — lightweight `2 + N` normal path, staging-only normal mode, manifest validation, targeted final attribution, stable additive telemetry.
- `tools/raspitajse-staging-owned-cron-guard.php` — separate lightweight snapshot, preserved deep snapshot, runner-context marker, sanitized aggregate progress capture.
- `deployment/deploy-staging.sh` — deterministic Communications tree fingerprint, post-sync source/runtime verification, atomic manifest-before-marker publication.
- `wp-content/plugins/raspitajse-communications/includes/class-candidate-job-alert-integration.php` — runner-context continuation suppression and explicit `has_more`; outside-runner continuation behavior unchanged.
- `wp-content/plugins/raspitajse-communications/raspitajse-communications.php` — sanitized result observations for the two expiry evaluators.
- No vendor/core/theme/business-rule files changed.

## Runner architecture
- Before: normal path used `5 + 2N` WordPress bootstraps with repeated deep global snapshots.
- After: normal path uses one light preflight + one exact-hook process per due hook + one light final, therefore `2 + N` bootstraps.
- Fixed literal hook order remains job expiry, employer expiry notice, candidate job alert.
- Normal mode remains zero-argument and now requires branch exactly `staging`; arbitrary args reject before WordPress. `--check-only` is the explicit deep/read-only diagnostic and executes no hook.
- Retained every-run controls: exact staging root/no symlink, clean tree, HEAD/marker/manifest parity, staging environment, `DISABLE_WP_CRON=true`, configured mail safety, exact callbacks/priority/accepted args, one hourly/3600 empty-args event per hook, due-only selection, exact-hook execution, nonblocking flock, 45-second hook timeout, 180-second watchdog, WP HTTP pre-transport block/attribution, mail/PHPMailer/SMTP and payment telemetry, continuation absence, fail-closed JSON output.
- Removed from the normal gate and retained in deep diagnostics: global/full and non-allowlisted cron fingerprints, Action Scheduler count/fingerprint and historical ID32733, global claims, due-business scan, protected business component fingerprints, candidate-expiry footprint, and retired legacy sender/callback evidence.
- No broad cron, Action Scheduler, continuation runner, daemon, web-cron, or second scheduler surface exists.

## Deploy integrity
- Manifest: `/home/u601262303/deploy-state/raspitajse-staging-communications.manifest`.
- Format is exactly three sanitized lines: format version, commit SHA, Communications SHA-256.
- Deploy computes deterministic source/runtime Communications tree hashes, requires equality, atomically publishes the manifest, then atomically advances the existing deploy marker.
- Normal mode strictly validates manifest shape, commit identity, and hash syntax without tree traversal; deep mode recomputes source/runtime trees and compares both to the manifest.
- Final source = runtime = manifest Communications tree fingerprint: `0c170e99a875005d347a7dd276d24071075ef958ebef517507db12aebf817ea2`.
- Both changed runtime PHP files match source byte-for-byte.

## Candidate continuation and progress
- Under exact `selective_runner_v1` guard context, candidate `has_more=true` does not schedule the continuation hook; backlog remains for later passes of the existing hourly owned hook.
- Outside that context, prior continuation behavior is unchanged.
- Existing bounded batch, ledger, claims, retry/idempotency, and matching behavior were not changed.
- Provider progress contains only supported aggregate fields (`selected`, `processed`, `succeeded`, `failed`, `has_more` as available); IDs, recipients, addresses, queries, bodies, and payloads are excluded.

## Static/private acceptance
- PASS, 78 assertions total.
- Runner mock matrix (25): zero/arbitrary args, 0/1/2/3 due hooks, fixed order, `2 + N`, deep no-execution, feature normal rejection/diagnostic allowance, missing/malformed manifest, deploy mismatch, missing guard, overlap lock, child error, timeout, not-due timestamp mismatch, contract drift, continuation rejection, schema fields.
- Guard matrix (13): WP HTTP interception/attribution, unexpected HTTP, wp_mail, PHPMailer, SMTP, payment blocking/telemetry, continuation blocking, exact runner context, sanitized progress/no identifiers.
- Light contract matrix (7): exact contract plus duplicate/missing event, changed args, wrong recurrence, wrong callback, continuation event.
- Static/source matrix (31): shell/PHP syntax, diff checks, fixed allowlist/order, staging-only mode, bounds, light/deep separation, no broad cron/AS path, deep diagnostic coverage, candidate continuation truth table, manifest order, bounded five-file scope.
- Stable progress-object correction (2): empty `{}` and populated object types.
- No real WordPress business state, external HTTP, mail, SMTP, payment, cron hook, broad cron, or Action Scheduler was used by private tests.

## Pre-integration protected snapshot
- Guard result PASS; runtime staging; `DISABLE_WP_CRON=true`; mail safety loaded/configured; `doing_cron` clear.
- Owned callback/event contracts all valid; continuation rows `0`; legacy daily/shared callbacks/events `0`; retired vendor sender callbacks `0`; candidate auto-expiry effective `0`; owned claims all `0`; due business total `0`.
- Owned timestamps: job `1788619186`; employer `1788616752`; candidate `1788618663`.
- Full cron fingerprint `9c1b6f863b1866f06d1bf4ee85d1ba0d9e929112e4c88e59e89878c74a7003d2`; non-allowlisted fingerprint `875972ba451addb368e78d86b4f3df94e7627037911ac617e620c2cdba6c1e71`.
- Action Scheduler: pending count `7`, pending fingerprint `1e06ffd60f1323db49bf194e8a3c8cdd7ea055c09a9554cda01c33aff1f32143`; ID32733 `pending`, attempts `0`; AS executions by task `0`.
- Protected business aggregate fingerprint `f3f7ce8a46e82e9164a85132f72eed50a1ca9daff026f939062197f0cf1537cc`.

## Stage A final T0
- T0 boundary recorded: `2026-09-05T13:51:24Z` UTC.
- T0 deep diagnostic: `result=NOOP`, `environment=staging`, `mode=check`, `reason=check_only`, `executed_hooks=0`, `execution_path=deep_health`, `wordpress_bootstraps=1`.
- T0 owned timestamps unchanged from pre-integration: job `1788619186`; employer `1788616752`; candidate `1788618663`; all three were not due.
- T0 callback/event, full cron, non-allowlisted cron, Action Scheduler, ID32733, claims, due-business, and protected business fingerprints exactly match pre-integration.
- Diagnostic safety: WP HTTP attempts/intercepted/unexpected `0/0/0`; wp_mail/PHPMailer/SMTP `0/0/0`; payment `0`; Action Scheduler execution `0`.
- The compatible `actual_external_network=0` field now carries `external_network_scope=wp_http_transport_preempted`; it is not presented as proof about arbitrary raw-socket mechanisms.

## Expected first natural provider JSON
- Return the first complete natural Hostinger View Output JSON strictly after T0 `2026-09-05T13:51:24Z`.
- It must retain `result`, `environment`, `mode`, `reason`, `duration_seconds`, `executed_hooks`, fixed-order `hooks`, and `safety`.
- Additive evidence must show `execution_path="lightweight"`, `wordpress_bootstraps=2+executed_hooks`, `progress` as an object, and `safety.external_network_scope="wp_http_transport_preempted"`.
- Do not change or manually trigger the scheduler. Return only its next natural complete provider output for Stage B reconciliation.

## Scheduler and safety
- Existing accepted human-owned scheduler architecture remains one `*/15 * * * *` zero-argument selective-runner entry.
- Scheduler mutations/manual triggers by task: `0`.
- Business mutations: `0`; mail/SMTP/payment/external WP HTTP side effects: `0`; Action Scheduler executions: `0`.
- Production touched: NO.

## Warnings / errors
- Known `HOST_NAMESPACE_PRESSURE` circuit breaker remained active; namespace-free isolated clone/scratch helpers were used. No safety gate was weakened.
- A post-deploy diagnostic exposed and then resolved the empty-progress array/object inconsistency before final T0. Final T0 is at the corrected SHA above.

## Cleanup
- Live staging worktree is clean on `staging`.
- Task-private harnesses remain only under bounded `/tmp`/Codex visualization scratch for audit during this session; they are not deployed or committed.

## Proposed next task
- After 2.12 Stage B PASS only: `Zadatak 2.13 — WP Job Board Pro security/upgrade readiness audit with fresh authoritative vulnerability/version verification` (proposed only; not created or started).
