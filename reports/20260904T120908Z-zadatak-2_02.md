# Codex Execution Report

- Task: Zadatak 2.02 — Implement owned employer job-expiry notification on the job-only lifecycle
- Task ID: 2.02
- Result: PASS
- Recorded at (UTC): 2026-09-04T12:09:08Z
- Source branch: staging
- Source HEAD: d09abafb154d1e4e004696361aa28c4c7f1920e7
- Source working tree clean: YES
- Staging deploy marker: d09abafb154d1e4e004696361aa28c4c7f1920e7
- Staging environment: staging

## Task report

## 1. Outcome
- **PASS** — the owned employer job-expiry notification slice is implemented, validated with an isolated no-real-mail staging fixture, fast-forwarded to `staging`, and source/runtime/deploy parity is exact.
- Protected business, scheduler, Action Scheduler, mail, network and production invariants held. All disposable fixture state was removed.

## 2. Baseline, branch and commits
- Declared and verified baseline: `862c5e5c47f4172807bb898ee6253c15b178a32d`.
- Read-only task control ref: `origin/codex-tasks` at `be7ef61cb49af1c5b2371d4bfbbb03d2e6c418bf`; it was not modified, merged or pushed.
- Feature branch: `feature/z2-02-owned-job-expiry-notice`.
- Feature commit: `d09abafb154d1e4e004696361aa28c4c7f1920e7` (`feat: add owned employer job expiry notice`).
- Final local `staging`: `d09abafb154d1e4e004696361aa28c4c7f1920e7`.
- Final `origin/staging`: `d09abafb154d1e4e004696361aa28c4c7f1920e7`.

## 3. Deploy and runtime parity
- Staging deployment from the feature branch completed successfully using `deployment/deploy-staging.sh changed feature/z2-02-owned-job-expiry-notice` before integration.
- Final deploy marker: `d09abafb154d1e4e004696361aa28c4c7f1920e7`.
- Source/runtime file SHA-256: `47fdd56f4bd26952771e4064f45dc08fa45f5be0a47756e5a80a1b0c35e0a9b3` on both sides.
- Final runtime: environment `staging`; staging mail-safety loaded; owned notification class loaded; communications Transport enabled.
- Feature was then fast-forwarded to `staging`; no redundant second deploy was performed because the deployed feature commit already equaled final `staging`.

## 4. Changed files and diffstat
- Changed file: `wp-content/plugins/raspitajse-communications/raspitajse-communications.php`.
- Diffstat: 1 file changed, 716 insertions, 1 deletion. The one deletion is the plugin version-line replacement (`0.8.0` to `0.9.0`).
- No files changed in WordPress core, WP Job Board Pro, WP Job Board Pro WooCommerce Paid Listings, WooCommerce, Superio parent/child themes, Raspitajse Commerce, MU plugins or deployment tooling.
- `php -l`: PASS for the changed PHP file.
- `git diff --check`: PASS.
- Static audit: no debug/PII logging, secrets, hard-coded user IDs/emails, production URLs, direct vendor mailer use, unmanaged `wp_mail()`, sender overrides, entitlement-expiry substitution or unrelated refactor introduced.

## 5. Owned notification hook and schedule
- Hook: `raspitajse_employer_job_expiry_notice_evaluator`.
- Callback: `Raspitajse_Communications_Employer_Job_Expiry_Notification::run`, priority `10`, final count exactly `1`.
- Recurrence: `hourly`, final event count exactly `1`.
- Callback fingerprint: `874fd2e8d6be76c327c836eaa103515d0a1c1b04518de441f3ea9c9e19235597`.
- Cron fingerprint: `82667f9965701ca91fb697e4fa3fc798a4da4baf3fbceba19b69147520f59a75`.
- `ensure_schedule()` runs on `init` priority 20, adds only a missing event, and repeated bootstrap left the count at one. Deactivation clears only this component's hook.
- The empty legacy daily hook, legacy shared expiry hook and Action Scheduler are not used.

## 6. Calendar due semantics
- Canonical source is only job meta `_job_expiry_date`.
- Initial due condition is an exact real `Y-m-d` date equal to tomorrow in the WordPress site calendar, for a `publish` `job_listing`.
- `current_datetime()`, `wp_timezone()` and immutable date parsing/formatting are used; raw PHP `date()` is not used.
- Missing, empty, malformed, today, past, more-than-one-day-away and non-published records are excluded.
- Changing the expiry date creates a distinct revision; the new revision becomes eligible only when its new date is tomorrow.

## 7. Bounded selection and stale-result protection
- Batch cap: `50`.
- Stable order: canonical expiry date, then post ID ascending.
- Query selects only published `job_listing` rows whose `_job_expiry_date` exactly equals tomorrow.
- Immediately before claim and again before delivery, the evaluator re-reads post type, status and exact current expiry date. A stale/unpublished/deleted/changed record cannot send.
- Per-record `Throwable` isolation prevents one bad record from blocking the remaining batch.

## 8. Canonical recipient resolution
- The job's authoritative `_job_employer_posted_by` employer profile must exist and be published.
- WPJBP's canonical employer profile-to-user mapping is resolved and mutually verified with the reverse user-to-profile mapping; the account must be an employer.
- A valid canonical employer profile email is preferred. The same verified account email is used only when the profile email is absent/invalid.
- `is_email()` validation is required. There is no admin, post-author, candidate or hard-coded fallback.
- Sanitized fixture proof: valid canonical mapping sent through the guarded application path; invalid canonical recipient produced zero mail attempt and a safe retryable failure state. No recipient value or fixture identity is reported.

## 9. Delivery state and revision identity
- Per-job state meta: `_raspitajse_job_expiry_notice_state`.
- Revision identity: job plus exact canonical expiry date.
- Bounded schema fields: schema version, expiry date, `in_flight|failed|delivered`, attempt count, last-attempt GMT, next-retry GMT, delivered-at GMT and sanitized failure code.
- Never-attempted is represented by absent state. Unknown/malformed state fails closed.
- A successful revision remains delivered and is skipped on all later evaluator runs. A changed expiry date is a new revision and does not inherit delivered status.
- Failed Transport results are never marked delivered.

## 10. Concurrency claim
- Atomic claim storage uses a non-autoloaded option with prefix `raspitajse_job_expiry_notice_claim_` and a key derived from the job/revision identity.
- Claim TTL: `600` seconds.
- Ownership token: collision-resistant 16-byte random token, with UUID fallback, and exact-token release.
- Atomic `add_option()` prevents overlapping acquisition; stale recovery uses compare-and-delete of the exact serialized snapshot before reacquisition.
- Fixture proof: a concurrent active claim blocked delivery; a stale claim recovered; final remaining claim count was `0`.

## 11. Retry contract
- Maximum attempts per revision: `3`.
- Deterministic backoff after transient failure: 15 minutes after attempt 1, 60 minutes after attempt 2; attempt 3 is terminal.
- No retry happens inside the same evaluator invocation and no infinite retry is possible.
- Every retry still requires the listing to be published, retain the exact same expiry date and remain in the tomorrow window.
- Fixture proof: first failure remained retryable; immediate repeat was skipped; a later eligible retry succeeded and became idempotent; terminal failure stopped after exactly three guarded attempts.

## 12. Owned rendering boundary
- Location: owned class methods `render()` and `render_message()` in `wp-content/plugins/raspitajse-communications/raspitajse-communications.php`; no WPJBP expiry template/option is used.
- Narrow view model: `job_title`, site-timezone-formatted `expiry_date`, and safe public `job_url`.
- Subject is sanitized; HTML output is escaped; strings use WordPress i18n. No candidate, entitlement, order/payment, internal-ID, admin-link, secret or debug field is rendered.
- Sanitized subject fingerprint: `7f1bb0f84e43002fbfb150be8da6a81b90c197f9bd0b153a8c9302ee7b983d30`.
- Sanitized body fingerprint: `f0ed00895ce71f8a6da171d76a69a1eb0d8ac4eef049af6ea500f1b1436d47a0`.

## 13. SenderPolicy and Transport boundary
- The only application send call is `Raspitajse_Communications_Transport::send(Raspitajse_Communications_Sender_Policy::CHANNEL_JOB_EXPIRY, ...)`.
- No direct vendor `WP_Job_Board_Pro_Email::wp_mail()` or direct unmanaged `wp_mail()` exists in the feature.
- Caller supplies only the HTML content-type header; it supplies no From or Reply-To override. SenderPolicy remains authoritative and resolved correctly on staging.
- Existing fail-closed behavior for missing/unsupported production channel configuration was not changed.

## 14. Isolated no-real-mail fixture matrix
- Final acceptance fixture result: PASS.
- Disposable matrix created 13 task-private job rows plus one task-private employer/profile identity and removed all of them in `finally` cleanup.
- Proved: tomorrow success; same-revision second-run idempotency; exclusions for today/future/past/malformed/missing/empty/non-published; changed future revision not yet due; transient failure/backoff/retry/success; active overlap blocking; stale claim recovery; three-attempt terminal state; invalid-recipient zero send; SenderPolicy headers on every guarded application send.
- Final acceptance fixture intercepted `wp_mail` attempts: `7`, all originating through the new Transport path.
- PHPMailer attempts: `0`; WP HTTP API attempts: `0`; guard-intercepted HTTP: `0`; actual external network requests: `0`.

## 15. Idempotency and failure assertions
- Successful delivery marks only the current job/exact-expiry revision delivered.
- A second evaluator run generated no additional send for that revision.
- A changed future expiry revision was recognized as distinct but not prematurely sent.
- Simulated Transport failure incremented the bounded attempt state without marking delivered; immediate tight-loop retry was blocked; the later eligible retry delivered once, then remained idempotent.
- Missing/invalid canonical recipient generated zero mail attempt and a retryable safe state.

## 16. Preserved 2.01 job-status lifecycle
- Separate hook `raspitajse_job_listing_expiry_evaluator` remains registered exactly once at priority 10; callback fingerprint stayed `dd3817786200c96e8ceb67f11a12517c1bcaf27b867bb2bf88730a901365abce`.
- Its recurring event remains exactly one `hourly`; cron fingerprint stayed `edbb361775d0f71568abf365c8c44152a5efa72bc9208f59b56f8cd8be434074`.
- Its batch size remains `50` and global claim TTL remains `600`.
- Candidate automatic expiry remains disabled by the owned late filter; effective candidate-expiry count remained `0`.
- Legacy daily expiry callbacks remained `0`; legacy shared expiry callbacks/events remained `0`.
- The status evaluator was not executed and no employer mail was sent from it.
- The vendor/Paid Listings `_job_expiry_date` writer path was not changed; status expiry and notification delivery remain separate callbacks and clocks.

## 17. Package-entitlement separation
- Raspitajse Commerce policy was unchanged; `VALIDITY_DAYS` remains 30 calendar days and policy-file SHA-256 remains `d4f0f9463a26c30831980a8e86cdcc1923491e99a4dba5c8f7c52d7bdf988da5`.
- Notification selection reads `_job_expiry_date`; it never reads or substitutes package valid-until state.
- Package expiry does not trigger this email, and unused entitlement validity remains independent of a published job's listing lifetime.
- No entitlement recalculation, migration, payment or package mutation occurred.

## 18. Protected alert and business-state fingerprints
Before and after fixture cleanup were identical:
- Candidate profiles: count `2` (`pending=1`, `publish=1`), fingerprint `51c76bed93079ef1eba756fe6950507c3bce610453295d6ac80986da9dfcd65d`.
- Historical `candidate_alert`: count `4`, all published, fingerprint `1d29516db8de9e92d1fa497293300c62ab670dd9beee0d0b03a01d726898deaa`.
- Published `job_alert`: count `0`, fingerprint `4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945`.
- Existing employers: count `1`, fingerprint `f6227cc86308f867aa30395c607712f8ece33137d2b1b63b2a8450098f17fe95`.
- Existing employer users: count `1`, fingerprint `6e8e6548de73179039c71b964731e6e55cc0650f63effe51c612afdae8dd986f`.
- Existing jobs: count `0`, empty fingerprint unchanged after cleanup.
- Job packages: count `10`, all cancelled, fingerprint `39cb6d0d5d0fc59d6b4ab5605c788f6d464c4aaabc8b430b2fd581e51a6caaf6`.
- Employer-to-candidate vendor delivery callback remains `0`; new candidate-alert creation remains retired/fail-closed.
- Candidate-to-job vendor sender remains `0`; owned candidate-to-job callback/event remain exactly `1/hourly`; continuation event remains `0`; neither alert subsystem was executed.

## 19. Legacy and Action Scheduler protection
- Orphan legacy daily event remained exactly one, callback count `0`, cron fingerprint `e37b37c804749fba0fe24ef4c39ab0c50f6552dff63e4e6ada90a739d785d9a1`.
- Legacy shared expiry callback/event counts remained `0`.
- Pending Action Scheduler count remained `7`; fingerprint remained `f692768d5db7a32111989b71d2b89576b9b98917d584ab6cac8a7c404f4a3af4`.
- ID32733 remained `pending/0`.
- No Action Scheduler cleanup, runner or due action was executed.

## 20. Mail/network/payment/scheduler counters
Final acceptance fixture:
- Intercepted `wp_mail` attempts through the new Transport path: `7`.
- Real external email deliveries: `0`.
- PHPMailer/SMTP attempts: `0`.
- WP HTTP API attempts: `0`.
- Actual external network requests: `0`; unexpected external network requests: `0`.
- Payment calls: `0`.
- Broad WP-Cron runs: `0`.
- Action Scheduler runner/due-action runs: `0`.
- 2.01 job-status expiry evaluator runs: `0`.
- Candidate-to-job evaluator runs: `0`.
- Legacy expiry sender runs: `0`.

## 21. Fixture cleanup
- Final matrix: jobs created/deleted `13/13`; task-private employer deleted; task-private user deleted; remaining task markers `0`; remaining notice claims `0`.
- Final protected snapshot exactly matched the pre-fixture snapshot across posts, statuses, users, callback graphs, cron graphs, Action Scheduler and ID32733.
- No real/historical business record was used as a fixture or modified.

## 22. Execution warnings and resilience
- The first later sandbox command encountered the known Hostinger namespace `ENOSPC` signature. It was classified immediately as `HOST_NAMESPACE_PRESSURE`; the sandbox attempt was not looped. Subsequent required commands used the documented namespace-free path.
- Earlier patch-tool validation showed that the repository Git helper rejects standard new-file patches containing `/dev/null`; no source mutation occurred from those rejected validations. The focused implementation was therefore kept in the existing owned communications plugin file and applied as one exact inspected patch.
- Two preliminary fixture-setup trials were stopped at their first unexpected WordPress-lifecycle mail interception, before PHPMailer/SMTP, and fully cleaned. Their aggregate intercepted setup-attempt count was not retained by those aborted harness runs, so it is reported conservatively as at least `2`, not invented as an exact number. They are separate from the final acceptance fixture's exact `7` Transport-path attempts. External email, PHPMailer/SMTP and external-network counts remained `0`; final protected-state cleanup was independently verified after each abort and after the passing fixture.

## 23. Final invariants
- Owned job-status expiry evaluator: `1`, hourly.
- Owned employer expiry-notification evaluator: `1`, hourly.
- Canonical notification source/window: `_job_expiry_date`, exactly one WordPress site-calendar day before expiry.
- Successful delivery is idempotent per job plus exact expiry date; failures have bounded retry; overlapping workers cannot normally duplicate the revision.
- Candidate/admin legacy expiry notices remain retired; legacy employer expiry sender remains absent.
- Runtime source, deploy marker, local staging and `origin/staging` all identify the same final commit.

## 24. Production safety
- Production accessed: NO.
- Production touched: NO.
- Production deployed: NO.
- Production database accessed: NO.

## 25. Result meaning
- PASS refers to the completed implementation and the final corrected, deterministic acceptance matrix. The preliminary guarded harness aborts are retained above as execution-history warnings; they caused no transport or persistent protected-state drift and do not weaken the final acceptance evidence.

## 26. Proposed next task — not started
- **Zadatak 2.03 — Retire the orphan legacy daily expiry schedule after final owned-scheduler regression.** Scope should be only the now-callback-free legacy daily event, with before/after cron fingerprints and preservation of both owned hourly evaluators. This is a proposal only; 2.03 was not created or started.
