# Codex Execution Report

- Task: Zadatak 2.01 — Establish an owned job-only expiry boundary and retire legacy candidate expiry paths
- Task ID: 2.01
- Result: PASS
- Recorded at (UTC): 2026-09-04T09:46:32Z
- Source branch: staging
- Source HEAD: 862c5e5c47f4172807bb898ee6253c15b178a32d
- Source working tree clean: YES
- Staging deploy marker: 862c5e5c47f4172807bb898ee6253c15b178a32d
- Staging environment: staging

## Task report

## 1. Result and exact meaning

- **PASS.** Zadatak 2.01 established a Raspitajse-owned, job-only expiry boundary on staging and retired every legacy candidate/job expiry callback named by the task.
- Candidate profiles can no longer be auto-expired by the legacy elapsed-time checker, and the automatic candidate duration calculation is forced off in the owned layer.
- A bounded disposable staging matrix proved the owned evaluator's eligibility, transition, locking, stale-claim recovery and idempotent second run. All fixtures and the claim were removed exactly.
- Employer job-expiry email remains intentionally deferred. No legacy/broad cron, mail, alert evaluator, Action Scheduler action, payment path or production system was executed.

## 2. Baseline, feature commit and final staging SHA

- Declared and verified baseline: `d9a095893b3751051135333e08dd3a0148db6d9b`.
- Read-only task control plane: `origin/codex-tasks=52e32cd3aaeb174cc51376ccf399284509cbe3c5`; both mandatory task files were read in full and the branch was not modified.
- Feature branch: `feature/z2-01-owned-job-expiry`.
- Focused feature commit: `862c5e5c47f4172807bb898ee6253c15b178a32d` (`refactor: own job listing expiry boundary`).
- Feature was pushed, deployed to staging, fixture-validated, then fast-forwarded to `staging` without a merge commit or history rewrite.
- Final local `staging`, `origin/staging`, feature commit and deploy marker: `862c5e5c47f4172807bb898ee6253c15b178a32d`.
- Final source worktree: clean and aligned with `origin/staging`.

## 3. Deploy and runtime parity

- Approved deploy: `deployment/deploy-staging.sh changed feature/z2-01-owned-job-expiry`.
- Deployment changed exactly one allowlisted staging file and completed successfully.
- Final source/runtime SHA-256 for `raspitajse-communications.php`: `b29d1add5b1413ba5aeb217e92fdf8ce359a39dfef7fbfbe1a320099734197fc` on both sides.
- Final environment: exactly `staging`; staging mail-safety: loaded.
- The validated feature commit became the final staging commit byte-for-byte, so no redundant second deploy was required after fast-forward.

## 4. Exact changed files and diffstat

Changed file only:

- `wp-content/plugins/raspitajse-communications/raspitajse-communications.php`

Diffstat:

```text
1 file changed, 322 insertions(+), 1 deletion(-)
```

Changes are limited to:

- plugin version `0.7.0 → 0.8.0`;
- one focused `Raspitajse_Communications_Job_Listing_Expiry` component;
- component boot and deactivation wiring.

No WordPress core, WPJBP, Paid Listings, WooCommerce, Superio parent/child theme, MU plugin, template, option definition or Raspitajse Commerce source changed. No debug/PII logging, secret, hard-coded user/email, production URL or second expiry-date source was introduced.

## 5. Daily callback graph and legacy event

Before:

- callback count `4`;
- fingerprint `e7ee490fc572e73d161acdb3780697715f38606389f39acfc7d4575a67f11fa5`;
- candidate admin, candidate self, job admin and employer job expiry callbacks each once at priority `10`.

Final:

- callback count `0`;
- empty fingerprint `4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945`;
- each of the four exact legacy callbacks registration count `0`;
- no replacement notice sender was registered.

The legacy daily event remained byte/logically unchanged and was not executed:

- count `1`;
- timestamp `1778854026`;
- schedule `wpie_daily`;
- interval `86400`;
- args `[]`;
- canonical-row fingerprint `860009d2da43c86850e746d89640f16edb62527d18dc4c992012d9cccdfc92be`.

## 6. Shared legacy expiry graph and event

Before:

- callback count `2`;
- fingerprint `8f081a7e4cbef4cc27ae947caed9031216a6130da6842ac5a9ffcbabd16190f4`;
- vendor candidate checker and vendor job checker each once at priority `10`;
- scheduled event count `0`.

Final:

- callback count `0`;
- candidate checker `0`; legacy job checker `0`;
- scheduled event count remains `0`;
- callback/event empty fingerprint `4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945`.

Repository search confirmed there is no alternate owned/manual caller of either removed vendor method. The broad shared hook was not executed or scheduled.

## 7. Candidate automatic-expiry suppression

- Owned filter: `wp-job-board-pro-calculate-candidate-expiry`.
- Callback: `Raspitajse_Communications_Job_Listing_Expiry::disable_candidate_expiry`.
- Priority: `PHP_INT_MAX` (`9223372036854775807` on this host); accepted args: `2`.
- Paid Listings' package-duration filter remains present at priority `10`; the owned late filter deterministically returns integer `0` after it, regardless of global/package/vendor input.
- Direct effective filter test with a non-zero input returned `0`.
- `WP_Job_Board_Pro_Candidate::calculate_candidate_expiry(0)` returned the canonical disabled result `''`.
- No candidate meta/status was rewritten. Historical/manual meta cannot trigger automatic mutation because the legacy candidate checker is absent.

Candidate preservation before/final:

- total `2`; statuses `publish=1`, `pending=1`;
- candidate expiry meta count `0`;
- sanitized expiry fingerprint `3a9914b6a4bd5f36d20e5c2edb0d92b9c404f03236e5b3bd5cbf7e7f51d706d2`, unchanged.

The known defective vendor candidate due helper was not called.

## 8. Owned evaluator and schedule

- Hook: `raspitajse_job_listing_expiry_evaluator`.
- Runtime callback: `Raspitajse_Communications_Job_Listing_Expiry::run`, exactly once at priority `10`.
- Callback fingerprint: `dd3817786200c96e8ceb67f11a12517c1bcaf27b867bb2bf88730a901365abce`.
- `init` priority `20` calls `ensure_schedule()`; it uses `wp_next_scheduled()` and creates the event only when missing.
- First post-deploy bootstrap created the one expected technical scheduler record. A second bootstrap kept the exact same single event, proving no duplicate creation.
- Final event: count `1`, timestamp `1788514786`, schedule `hourly`, interval `3600`, args `[]`; fingerprint `bb39538e751e131bd977f3a54ad8a9c6bd12eabcd59f4d5318b2b4c992d1e9aa`.
- Deactivation wiring clears only this component's hook.
- The scheduled event itself was never executed; only the public owned callback was directly invoked inside the bounded fixture.

## 9. Eligibility, bounding, ordering and timezone

The evaluator consumes only existing `_job_expiry_date` and requires:

- `post_type=job_listing`;
- current `post_status=publish`;
- canonical, real `Y-m-d` expiry value;
- expiry strictly before today's WordPress site-calendar date.

It excludes missing, empty, malformed, today, future and non-published rows. `current_datetime()`/`wp_timezone()` define calendar semantics; raw PHP `date()` and adjusted legacy timestamps are not used. Reflection-backed boundary checks accepted a canonical date and rejected both an impossible calendar day and a wrong shape.

The SQL query is capped at `50`, ordered by oldest expiry value then stable post ID. Every selected ID is re-read immediately before transition, including type, status and exact current meta validation. Transition uses `wp_update_post(..., post_status=expired, true)`, preserving ordinary WordPress/WPJBP lifecycle hooks. Failures are isolated per job; no posts-table bypass is used.

## 10. Claim, concurrency and idempotency

- Claim key: `raspitajse_job_listing_expiry_claim`.
- Mechanism: atomic unique `add_option()` for a fresh global evaluator claim.
- TTL: `600` seconds.
- Ownership: collision-resistant token stored with expiry timestamp.
- Active claim: a second worker exits with `claim_acquired=false`, reason `active_claim`, and no mutation.
- Stale recovery: exact compare-and-set update against the old option value; concurrent replacement cannot be overwritten blindly.
- Release: exact token validation plus compare-and-delete of the exact stored value; cache invalidated after direct atomic SQL.
- Crash recovery: a stale claim becomes replaceable after TTL.
- Normal idempotency: once an eligible job transitions out of `publish`, it is no longer selectable; a second run cannot re-transition it.
- Final claim option: absent.

## 11. Disposable fixture matrix

Fixture state class: explicitly authorized disposable staging business state plus a bounded owned technical claim.

Six collision-resistant `job_listing` fixtures were created with a unique private marker:

| Case | Initial state | Expected / observed after first run |
|---|---|---|
| Past valid expiry | `publish` | `expired` |
| Expiry today | `publish` | remained `publish` |
| Future expiry | `publish` | remained `publish` |
| Empty expiry | `publish` | remained `publish` |
| Missing expiry | `publish` | remained `publish` |
| Already non-published | `expired` | remained `expired` |

Concurrency and repeat evidence:

- overlap attempt: blocked; past fixture remained `publish` while the active claim existed;
- stale claim: recovered and exact replacement released;
- first normal run: claim acquired/released YES, selected `1`, expired `1`, skipped `0`, failed `0`;
- observed `publish → expired` transition count: exactly `1`;
- second normal run: claim acquired/released YES, selected `0`, expired `0`, failed `0`; transition count remained `1`.

No evaluator other than the owned job-expiry callback was executed.

## 12. Cleanup and restored business fingerprints

- Created fixture posts: `6`; exact marked cleanup: `6`; remaining fixture meta/posts: `0`.
- Owned/stale claim artifacts: `0` remaining.
- Real job footprint before/after cleanup: count `0`, fingerprint `4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945`.
- Candidate fixture-process fingerprint before/after: `7fdaf37202fe2422b2cca0ce6433d4dff87361ecf9cef8d2656c0988397e90d8`, unchanged.
- Final independently canonicalized candidate expiry fingerprint also matches the pre-task value `3a9914b6a4bd5f36d20e5c2edb0d92b9c404f03236e5b3bd5cbf7e7f51d706d2`.
- Cleanup completed inside `finally`; no rollback was needed beyond the planned exact fixture lifecycle.

## 13. Listing-expiry writer preservation

The owned evaluator only reads `_job_expiry_date`; it does not calculate or write a second date.

Unchanged source/runtime SHA-256 evidence:

- WPJBP job post-type transition/writer file: `d86890aff3120b1a8bb08921cad00c3c31e7561a664a53234e16eca290c6d295`.
- Paid Listings job package duration filter: `85f87d1ef0f3f2375c69095268fbc4b557e1eabf097a4638cd88b8666ff8a124`.
- Paid Listings submit/package-duration copy path: `a224aacb4c23f7d136adeb6f52fbd2c5b364c5e6f82acb2c43babafc5aca4af6`.

Therefore WPJBP publish-like transitions, package `_job_package_duration` override and intentional no-expiry listing subscriptions remain authoritative and unchanged.

## 14. Entitlement validity remains separate

- Raspitajse Commerce package-policy file was unchanged; source/runtime SHA-256 `d4f0f9463a26c30831980a8e86cdcc1923491e99a4dba5c8f7c52d7bdf988da5`.
- Final all-status `job_package` count: `10`; full selected post/meta fingerprint `96bf340c4f25c5bd77bc801f61cddd6e93c0b934c36f7887770b06a4f69f94f7`.
- No entitlement marker, validity timestamp, count, order, product, package duration or ownership value was migrated/recalculated/mutated.
- The evaluator has no entitlement query or dependency; entitlement consumption validity and an already-published listing's lifetime remain independent clocks.

## 15. Employer expiry email remains deferred

- Legacy employer job-expiry callback final registration: `0`.
- No owned employer expiry sender/evaluator/template was added.
- No call to `SenderPolicy` or `CHANNEL_JOB_EXPIRY` was introduced.
- No expiry email option/template/content was modified.
- Final employer-notification cadence was not decided.
- Fixture counters: WP mail attempts `0`, PHPMailer calls `0`, SMTP `0`.

## 16. Candidate-alert and candidate→job invariants

Final protected state:

- employer→candidate candidate-alert vendor sender: `0`;
- all three candidate-alert add routes remain owned once at priority `10` by `Raspitajse_Communications_Alert_Security::process_add_candidate_alert`;
- the unchanged owned service still fails closed for new `candidate_alert` creation;
- historical published `candidate_alert`: `4`; SQL-order full-meta fingerprint `a00db2c52873b718f4253e1d34352dcdaec2eec35642454a69eaf48aa40f8623`, unchanged;
- candidate→job vendor sender: `0`;
- owned candidate→job evaluator callback: `1`; fingerprint `e9882904d6e8fad28bd0fe124b033d5f7c013a98f01e186a6d0e01a38230b9ae`;
- owned candidate→job event: one hourly event, timestamp `1788258663`, fingerprint `d636587702cde244ee2d1bbcd3bb65c3c4b2e9a179081718e11594a6c22d6a74`;
- candidate→job continuation event: `0`;
- published `job_alert`: `0`; fingerprint `4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945`;
- new-create frequency policy remains `daily`, `weekly`, `fortnightly`, `monthly`.

Neither alert delivery subsystem was executed.

## 17. Action Scheduler and protected scheduler state

Before/final:

- pending Action Scheduler count: `7 → 7`;
- independent full selected-row fingerprint: `232ca1facb4a38bcfa5214a6e3f3a72309baeba094cc1f95d2808b40c572e3e6 → identical`;
- ID32733: `pending/0 → pending/0`.

No Action Scheduler action, runner or due queue was executed or mutated. The new evaluator uses WP-Cron only.

## 18. Static and integration validation

- PHP lint on changed plugin file: PASS before commit, before deploy and final.
- PHP lint on the complete task-private fixture harness: PASS before load.
- `git diff --check`: PASS before commit, pre-deploy and final commit range.
- Namespace-free patch preflight/application: `patch_check=PASS`, `patch_apply=PASS`.
- Feature-vs-staging diff before integration: one intended owned file only.
- Fast-forward ancestry/integration: PASS; no shared-history rewrite.
- Runtime callback, candidate filter, date validation, schedule idempotency, fixture, cleanup and protected-invariant checks: PASS.
- Source/runtime parity and deploy marker: PASS.

## 19. Effects and safety counters

- Application source commits: `1` focused feature commit; staging integration: one fast-forward.
- Staging deploys: `1` changed-file deploy; production deploys: `0`.
- Non-fixture candidate/job/user/alert/entitlement/business mutations: `0`.
- Disposable fixtures created/transitioned/deleted: `6 / 1 / 6`; all exact and fully cleaned.
- Expected technical schedule creation: `1` owned hourly event; duplicate events: `0`.
- Legacy daily/shared expiry hook executions: `0 / 0`.
- Scheduled owned job-expiry event executions: `0`; direct bounded fixture calls: `3` (one overlap/no-op and two normal runs).
- Candidate alert sender / candidate→job evaluator / continuation executions: `0 / 0 / 0`.
- WP mail attempts / PHPMailer / SMTP: `0 / 0 / 0`.
- WP HTTP API attempts / guard-intercepted / approved transport / actual external application requests / unexpected requests: `0 / 0 / 0 / 0 / 0`.
- Payment calls: `0`.
- Action Scheduler mutations / runner / due actions / ID32733 executions: `0 / 0 / 0 / 0`.
- Authorized Git fetch/push and report publication are control-plane operations, not application/vendor HTTP.
- Production filesystem/database/backups/network/application accessed: **NO**.
- Production touched: **NO**.

## 20. HOST_NAMESPACE_PRESSURE

- One initial repository `apply_patch` attempt failed before mutation with the known `bwrap ... ENOSPC` namespace signature. It was not retried.
- Disk/inode diagnostics were not needed; the signature was already confirmed and the mandatory circuit-breaker was applied.
- The first temporary rewrite preflight was rejected because its target was directly under `/tmp`, outside the helper's required Raspitajse-owned task directory; it made no repository/runtime change. A fresh compliant `/tmp/raspitajse-z2-01.*` directory was then used.
- Repository fallback: one generated/inspected single-file patch through `tools/codex-git-apply.sh`; check and apply passed.
- Exact repository file affected by fallback: only `wp-content/plugins/raspitajse-communications/raspitajse-communications.php`.
- Complete PHP/diff/runtime validation passed after fallback. No WordPress mutation had started before the infrastructure failure.

## 21. Exactly one proposed next task

**Zadatak 2.02 — Implement owned employer job-expiry notification on the job-only lifecycle**

Proposed narrow scope: use the canonical `_job_expiry_date` and the existing owned job-only schedule boundary; implement one deterministic pre-expiry window, per-job/expiry-date idempotency and bounded retry/claim behavior; resolve the canonical employer recipient; send only through `Raspitajse_Communications_Transport` with `job_expiry`; own and validate the template; prove with isolated no-real-mail fixtures. It must not restore admin/candidate notices, use package-entitlement expiry as listing expiry, or alter the job status evaluator.

This is a proposal only. Zadatak 2.02 was not started.
