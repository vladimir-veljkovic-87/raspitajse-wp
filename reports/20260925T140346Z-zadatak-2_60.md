# Codex Execution Report

- Task: Zadatak 2.60.4 — Remove the staging robots bypass and refresh exact isolation URLs
- Task ID: 2.60
- Result: PARTIAL
- Recorded at (UTC): 2026-09-25T14:03:46Z
- Source branch: staging
- Source HEAD: f6093c11d71ddc175ebf83fc69453c20aaca5239
- Source working tree clean: YES
- Staging deploy marker: f6093c11d71ddc175ebf83fc69453c20aaca5239
- Staging environment: staging

## Task report

## Outcome
- PARTIAL: the intended isolation integration did not complete.
- Failed checkpoint: the single normal public `/robots.txt` acceptance response remained a cached empty response after the exact URL-scoped LiteSpeed invalidation. It lacked the required `X-Robots-Tag`, global user agent, and root disallow directives.
- Safety/business invariants held and the mandatory rollback completed exactly.
- `PASS: STAGING_SEARCH_ENGINE_ISOLATION_READY` is not claimed.

## Source and preconditions
- Declared baseline, initial local `staging`, `origin/staging`, and initial live marker: `f6093c11d71ddc175ebf83fc69453c20aaca5239`.
- Exact feature/deploy SHA: `783cc6b3425985c1e27de8e823fca020d0d7b869`.
- The feature was a direct child of the baseline and changed only `wp-content/mu-plugins/raspitajse-staging-search-isolation.php`.
- Working tree was clean; no competing Git, rsync, or deployment process was active.
- Initial runtime was exactly `staging`, `blog_public=1`, isolation plugin absent, and all mail-safety, cron-recovery, and Action Scheduler guards active.
- LiteSpeed Cache was active with exactly one registered `litespeed_purge_url` callback, `LiteSpeed\\API::purge_url`.
- Initial staging-root `robots.txt` was a regular non-symlink empty file: mode `0644`, size `0`, and SHA-256 `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.

## Bounded mutation
- Acquired the owned-runner lock on fd 9 and runtime lock on fd 8 nonblocking; all mutation preconditions were re-proven while both were held.
- Preserved the exact empty robots file and metadata in a fresh task-private rollback directory and verified the backup hash before removal.
- Deployed the exact feature through `deployment/deploy-staging.sh changed feature/task-2.60.2-staging-search-isolation`.
- Exactly one feature file was synced. Runtime/source plugin SHA-256 matched: `3eaf20ec82a0983ee9b967953446c0c428364034e75f8ea01124f9ec56da6438`.
- Changed only `blog_public` from `1` to `0`.
- Reverified the original robots file type, size, and hash; removed only that file and proved the path absent.
- Called `litespeed_purge_url` exactly once for the canonical staging home URL and exactly once for the canonical staging robots URL.
- Observed URL action calls: `2`. Observed broad purge/flush calls: `0`, including purge-all, object purge-all, generic purge, post, post-type, tag, and front actions.
- Environment and all three guards remained valid. Locks were released fd 8 then fd 9.

## Public acceptance
- Exactly two unauthenticated normal GETs were made to one allowed host: staging home and staging `/robots.txt`.
- Query strings: `0`; request no-cache overrides: `0`; redirects followed: `0`; redirect responses: `0`; cookies/auth/user data: `0`.
- Home: HTTP `200`; required noindex/nofollow response header present; rendered robots meta contained noindex; LiteSpeed result was `miss`. PASS for this request.
- Robots: HTTP `200`, but no noindex/nofollow response header, no global `User-agent: *`, and no `Disallow: /`; no sitemap was present. Response retained empty-file cache indicators including a nonzero `Age`, prior `Last-Modified`, and empty-response ETag. FAIL for this request.
- No third request was made. Acceptance was not weakened and no broader cache operation was attempted.

## Integration
- Integration gate failed, so `staging` was not fast-forwarded or pushed to the feature commit.
- No source edit, new application commit, merge, force push, or fix-forward occurred.

## Mandatory rollback
- Reacquired both locks in the required order.
- Atomically restored the exact empty robots file from the verified backup with its original mode, owner/group identifiers, mtime, size, and SHA-256.
- Restored `blog_public` from `0` to `1`.
- Ran `deployment/deploy-staging.sh changed staging`; the isolation plugin was removed and the marker returned to the baseline.
- Called the same two exact URL-scoped LiteSpeed invalidations once each; observed rollback URL calls: `2`, broad purge/flush calls: `0`.
- Final local branch and `origin/staging`: `f6093c11d71ddc175ebf83fc69453c20aaca5239`; worktree clean.
- Final deploy marker: `f6093c11d71ddc175ebf83fc69453c20aaca5239`.
- Final runtime: environment `staging`, `blog_public=1`, isolation plugin absent, physical empty robots file restored with exact metadata/hash, and all three guards active.
- Rollback: PASS.
- The task-private verified empty-file backup is retained because the task permitted its removal only after successful remote integration and final proof; no staging state depends on it.

## Prohibited side effects
- Broad cache purge/flush/object-cache flush/crawler/warm/optimization: `0`.
- Business fixtures or protected business-state mutation: `0`.
- Mail, cron callback, Action Scheduler, payment, control-panel, web-server, `.htaccess`, or `wp-config.php` execution/change: `0`.
- Production touched: NO.

## Required follow-up boundary
- The permitted WordPress `litespeed_purge_url` action, when invoked through this WP-CLI execution path, did not evict the publicly served cached empty robots response.
- Any next attempt requires a new task with an explicitly classified, narrowly scoped cache-owner invalidation mechanism. This task does not authorize retrying GETs or broadening cache operations.
