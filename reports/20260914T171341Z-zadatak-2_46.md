# Codex Execution Report

- Task: Zadatak 2.46 — Read-only free-access and three-job-quota impact map
- Task ID: 2.46
- Result: PASS
- Recorded at (UTC): 2026-09-14T17:13:41Z
- Source branch: staging
- Source HEAD: ec97a6f76a3d393bac5e2629b977cf6a1fbe9bf4
- Source working tree clean: YES
- Staging deploy marker: ec97a6f76a3d393bac5e2629b977cf6a1fbe9bf4
- Staging environment: staging

## Task report

## Outcome
- PASS classification: `FREE_ACCESS_IMPACT_MAP_COMPLETE`.
- This read-only impact map is precise enough to implement Tasks 2.47 and 2.48 without rediscovering package, submission, quota, role, UI, order, email, or scheduler scope.
- Superseded Task 2.45 was read only to confirm its superseded status and was not executed.

## Source, deployment, and runtime baseline
- Fresh `origin/codex-tasks`: `09fe64fa40450d33c82c50df0dcb91dbbdc5770e`; authoritative task/roadmap: Task 2.46 / free-launch architecture.
- Source branch/HEAD and `origin/staging`: `staging@ec97a6f76a3d393bac5e2629b977cf6a1fbe9bf4`; worktree clean.
- Deploy marker: `ec97a6f76a3d393bac5e2629b977cf6a1fbe9bf4`.
- Live/Git SHA-256 equality was proven for the commerce bootstrap, owned job-package policy, child `functions.php`, child `style.css`, async-dispatch guard, and DROP scheduling guard.
- Active theme: Superio `1.3.37` with `superio-child`. WordPress live `7.1`; WooCommerce live `11.1.0`; WP Job Board Pro `1.2.86`; Paid Listings `1.0.19`; Raspitajse Commerce `0.4.0`; Raspitajse Communications `0.9.1`; Elementor live `4.2.4`; WPForms Lite live `2.0.1.1`; AIOSEO live `5.0.1.1`.
- Repository vendor headers remain WooCommerce `9.5.4`, Elementor `3.25.11`, WPForms Lite `1.10.0.4`, and AIOSEO `4.8.6.1`; the WP Job Board Pro and Paid Listings files relevant to this map byte-match live. The newer live vendor versions are existing deployment state, not a Task 2.46 mutation.
- Relevant active dependencies include WooCommerce, Stripe, multi-bank-transfer gateways, PDF invoices, WP Job Board Pro, Paid Listings, CMB2, Elementor, Raspitajse Commerce, and Raspitajse Communications. WooCommerce Subscriptions is not active.

## Locked data baseline (sanitized)
- Both canonical staging locks were held by one process for each authoritative SQL-only snapshot. No WordPress bootstrap was used. T0/T1 query outputs were byte-identical and both locks were released.
- Registration: `users_can_register=1`; WordPress `default_role=subscriber`. Actual WP Job Board Pro AJAX registration explicitly assigns `wp_job_board_pro_employer` or `wp_job_board_pro_candidate`.
- Role counts: administrators 3, employer users 1, candidate users 1; exactly one employer-profile relation and one candidate-profile relation.
- Jobs: zero `job_listing` rows, therefore zero active jobs and no current employer exceeds quota.
- Preserved products: three published products, IDs `1728`, `1729`, `1730`, each with `job_package` product type; configuration values were captured only as salted-free row hashes.
- Preserved orders: three legacy post-backed orders (`wc-on-hold` 2, `wc-processing` 1); HPOS order table currently contains zero rows.
- Preserved entitlements: 10 `job_package` rows, all `cancelled`: IDs `8900`, `9826`, `9829`, `9832`, `9835`, `9838`, `9841`, `9844`, `9847`, `9850`. Their owner/product/order/count/limit relations are retained in restricted evidence; no row is active.
- Public paid surfaces: Elementor pricing page ID `36` (`cenovnik`), packages dashboard page ID `1699`, cart `8606`, checkout `8607`; menu item `1712` links to packages. Submit page `1694` and user dashboard `1653` are `page-dashboard.php` pages. Cart/checkout option IDs remain `8606/8607`.
- `wp_job_board_pro_settings` is absent, so current code defaults apply: direct publish unless a later filter changes it; candidate application defaults free (`on`); candidate detail/list/contact restrictions default unrestricted; no configured submission duration.
- Action Scheduler rows `32867` and `32868` were recorded only as observational pending vendor rows, attempts/claims `0`, exactly as the roadmap requires. They were not investigated, claimed, changed, or executed. Claims table count was `0`.

## Complete impact inventory

`DROP` below means disable the launch-time runtime/public path, never delete its vendor code or historical data.

| # | Exact owner / function / hook | Current purpose, caller, data and side effects | Surface | Decision | Owned target, risk and required test |
|---:|---|---|---|---|---|
| 1 | `wp-job-board-pro-wc-paid-listings/inc/class-submit-form.php`; `init()` → `wp_job_board_pro_submit_job_steps` priority 5; `get_products()` | Discovers published `job_package` products and inserts choose/process package steps. Method invocation also registers `wp_job_board_pro_submit_job_post_status`, converting preview/expired to `pending_payment`. | Employer submit/relist | REDESIGN | 2.47 owned free-access policy removes these two steps and overrides any remaining `pending_payment` result. Test new/relist with no product, cookie, cart, order, or entitlement. |
| 2 | Same file; `choose_package_handler()`, `validate_package()`, `before_preview_job()`, `process_package_handler()` | Requires product or user package, writes `_job_package_id`, `_job_user_package_id`, duration/featured flags, consumes package, mutates WC cart, and redirects to checkout. | Submit wizard | DROP | Disable callbacks in owned layer, not vendor edits. Assert neither handler, cart API, nor checkout redirect is called. |
| 3 | `wp-job-board-pro-wc-paid-listings/inc/class-mixes.php`; `package_is_valid()`, `increase_package_count()`, `approve_job_with_package()`, `get_packages_by_user()` | Entitlement authorization/count and publication after purchase; stores `job_package` posts and package/job links. | Submission and entitlement admin | DROP as authorization; KEEP historical reads | 2.47 quota replaces authorization; history stays queryable. Assert no new `job_package` or package meta and existing 10 rows/hash unchanged. |
| 4 | `raspitajse-commerce/includes/class-raspitajse-commerce-job-package-policy.php`; `boot()`, `protect_submit_handlers()`, selection/process wrappers | Adds 30-day activation window, HPOS marker bridge, order failure revocation, and fail-closed package handlers. Reads/writes entitlement/order markers. | Package submission/admin history | REDESIGN | Retain only compatibility/read needs while free mode bypasses package authorization and activation. Test old order/entitlement readback unchanged and no new marker writes. |
| 5 | `wp-job-board-pro-wc-paid-listings/inc/class-wc-cart.php`; add-to-cart hooks, `woocommerce_add_to_cart_redirect`, checkout-signup/guest filters, line-item persistence | Sends package products to cart/checkout and carries `job_id` into order items. | Cart/checkout | DROP | 2.48 removes public entry and owned route guards prevent package/cart/checkout journey. Assert zero cart mutation and no `_job_id` line item. |
| 6 | `raspitajse-commerce/raspitajse-commerce.php`; `enqueue_package_purchase_transport()`/`package-purchase.js` | Dashboard AJAX add-to-cart then same-origin checkout redirect. | Packages dashboard widget | DROP | Do not enqueue/localize in free mode. Browser network test: no add-to-cart/checkout request. |
| 7 | Same file; checkout prefill, fields, order-company meta, currency preview, NBS HTTP rate, RSD conversion, thank-you payment instructions, invoice attachment | Launch-time package checkout/payment presentation and order mutations; may perform external HTTP and payment/email-adjacent behavior. HPOS admin renderer also reads historical order meta. | Checkout/thank-you/order admin | DROP public write/transport; KEEP historical admin renderer | 2.48 conditionally boots only read-only HPOS admin rendering in free mode. Assert checkout unreachable from free UI, NBS/payment/mail counters zero, old orders still render. |
| 8 | Paid Listings `class-wc-order.php`; `woocommerce_order_status_processing/completed` → `order_paid()` | Creates entitlements, publishes pending-payment jobs, marks order processed, may send admin mail. | Order lifecycle | DROP launch runtime | Owned free-mode boot removes these status callbacks. Test synthetic hook-registration only; never process real orders. |
| 9 | Same file; cancelled/refunded → `order_cancelled()`, `delete_user` | Cancels entitlements/jobs and deletes package posts. | Order/user lifecycle | DROP launch coupling | Disable purchase-coupled callbacks in free mode while preserving rows. Regression test that status changes in free fixtures do not touch jobs/packages. |
| 10 | `class-job-package-subscription.php`; subscription lifecycle/status/trash/expiry hooks | Renews/deletes packages and expires/reactivates jobs/candidates. Woo Subscriptions is inactive, but hooks remain conditional code. | Subscription lifecycle | DROP | Do not activate; detach free-mode callbacks where registered. Assert no subscription dependency/class required. |
| 11 | `class-product-type-package.php` and `class-post-type-job_package.php` | Registers product types, product fields, and entitlement post type/admin columns. | Woo/admin history | KEEP temporarily | Keep active for historical readability in 2.47/2.48; no public discovery. Test three products and 10 entitlements remain readable/hash-identical. |
| 12 | `class-candidate-package.php`; `wp-job-board-pro-check-candidate-can-apply` and applicant count hooks | Package-gates applications only when `candidate_free_job_apply=off`; increments package usage. Current default is free/on. | Candidate apply | REDESIGN to explicit free | Owned max-priority policy returns free eligibility and disables package counters. Test apply with no package/order. |
| 13 | `class-cv-package.php` and `class-contact-package.php`; candidate listing/detail/contact filters | Can gate CV/profile/contact visibility by employer package and increment view/contact counters. Current settings default unrestricted. | Candidate search/profile/CV/contact | REDESIGN to explicit free | Owned filters force free-launch values and prevent package counter mutation. Authorization/privacy rules remain separate. Test employer access without entitlement and cross-role privacy. |
| 14 | `class-resume-package.php`; candidate creation/approval/status filters | Presence of resume products can force candidate profiles to `pending_payment` and show package purchase. | Candidate registration/profile/CV | DROP payment gate | Remove these filters/actions in free mode. Test new candidate remains in configured moderation flow, never `pending_payment`. |
| 15 | `wp-job-board-pro/includes/class-shortcodes.php`; submission/edit/my-jobs/dashboard shortcodes | Requires logged-in employer (or configured employee), resolves canonical parent employer, renders submit/edit/dashboard. | Employer pages | KEEP + quota UI | Continue role/ownership checks; inject owned quota state/message. Test employer, employee policy, candidate denial, admin. |
| 16 | `wp-job-board-pro/includes/class-submit-form.php`; `submit_process()`, `preview_process()`; filters `wp-job-board-pro-process-submission-data`, `wp-job-board-pro-submission-validate`, `wp_job_board_pro_submit_job_post_status` | Saves content first as `preview`, then publishes or sends to pending review. Uses canonical employer user as author. | Direct form | KEEP + REDESIGN | 2.47 removes paid steps and enforces quota both before display/submit and at universal persistence boundary. Test content survives fourth attempt. |
| 17 | `wp-job-board-pro/includes/class-edit-form.php`; edit process and ownership check | Edits existing listing; normally keeps publish, optionally returns to moderation; may send update mail. | My Jobs edit | KEEP | Quota excludes same post ID so active edits allocate no slot. Mail remains guarded and is separately accepted in 2.51. |
| 18 | `WP_Job_Board_Pro_User`; `employer_id`, employee→employer mapping, `is_employer_can_add_submission()`, `is_employer_can_edit_job()` | Canonical identity/ownership. Job helper `WP_Job_Board_Pro_Job_Listing::get_author_id()` prefers `_job_employer_posted_by` → employer owner user, falling back to `post_author`. | All employer job operations | KEEP | Quota key is canonical employer owner user ID; employees map to the same owner. Test cross-employer isolation and spoofed author/meta rejection. |
| 19 | `WP_Job_Board_Pro_Post_Type_Job_Listing`; `wp_insert_post_data`, publish transitions, `save_post`, expiry meta | Registers statuses and sets expiry on transitions. No current active-job quota exists. | Admin/direct/REST/form persistence | REDESIGN | Owned universal `wp_insert_post_data` filter plus held employer lock enforces quota; `transition_post_status` audits/releases state. Test every transition and direct REST/AJAX/form path. |
| 20 | `Raspitajse_Communications_Job_Listing_Expiry`; `raspitajse_job_listing_expiry_evaluator` | Owned hourly bounded expiry changes due `publish` jobs to `expired`; legacy expiry/email callbacks are suppressed. | Background lifecycle | KEEP | Its normal `wp_update_post` transition releases quota automatically. Unit probe invokes only explicit fixture IDs; never broad runner. |
| 21 | `templates/submission/my-jobs.php`; publish/expired/pending/draft/preview actions | Lists employer-owned jobs and exposes edit/relist/continue/remove. Current query keys by canonical author. | Employer dashboard | KEEP + quota status | Add owned remaining-slot message and clear fourth-job reason; relist/restore passes same quota gate. Browser test all statuses. |
| 22 | `WP_Job_Board_Pro_User::process_register()` and `process_register_new()`; AJAX actions; `user_register` profile linkage | `users_can_register` gate, explicit employer/candidate role, profile relation, login session/approval/email behavior. | Registration | KEEP | No entitlement migration or creation. Explicitly assert both new/existing roles gain free access; mail/CAPTCHA tested later with guards. |
| 23 | Child `restrict_wp_admin_access()` on `init` | Allows AJAX and users with `manage_options`; redirects other wp-admin users. | Administration | KEEP | Quota has the same capability-based admin exemption; never hardcode user ID. Test admin moderation and non-admin redirect/AJAX exception. |
| 24 | Parent/mobile headers, Elementor add-job widget, nav menus; pricing page 36, packages 1699/menu item 1712, cart 8606, checkout 8607 | Exposes Post Job plus package/pricing/cart/checkout UI. Header cart depends on `show_cartbtn`; package widgets call Paid Listings product discovery. | Public navigation/dashboard | REDESIGN/DROP paid targets | 2.48 preserves direct Post Job but removes pricing/packages/cart/checkout items/buttons and redirects paid-only routes to submit/dashboard. Test desktop/mobile menus and rendered Elementor pages. |
| 25 | Paid templates and child overrides: `template-paid-listings/{choose-package-form,user-packages,packages}.php`; child Elementor user-packages widget | Renders package radio buttons, status/quota and purchase forms. | Submit/dashboard/packages | DROP paid rendering | Free submit no longer invokes choose-package template; replace dashboard package widget with active-job quota summary or remove it. Snapshot test contains no package/price/checkout CTA. |
| 26 | Job submission/edit emails; Paid order email; owned expiry/job-alert schedules | Submission/update/expiry can mail; purchase activation can mail; owned cron can expire jobs or send notices. | Email/background | KEEP business email for 2.51; DROP purchase email | 2.47/2.48 tests intercept `wp_mail`, PHPMailer/SMTP, HTTP, payment, WP-Cron and AS; no broad runner. Expiry transition must release quota once. |
| 27 | Existing products/orders/entitlements and Woo admin views | Historical commercial records and audit trail. | Admin/history | KEEP | No deletion, rewrite, status migration, or retroactive entitlement creation. Hash/count assertions before/after every implementation task. |

## Exact three-active-job implementation contract (Task 2.47)

### Owned component and boot order
- Add `wp-content/plugins/raspitajse-commerce/includes/class-raspitajse-free-job-access-policy.php` and require/boot it from the already-active `raspitajse-commerce.php`. This is the smallest update-safe Raspitajse-owned target and avoids a new plugin activation/options mutation.
- Use an explicit, filterable `FREE_LAUNCH_ENABLED=true` policy seam. Vendor files remain untouched.
- On `plugins_loaded` after Paid Listings registration, detach only the public package/candidate gate callbacks listed above. Also filter `wp_job_board_pro_submit_job_steps` at maximum priority to remove `wjbp-choose-packages` and `wjbp-process-packages`, and force `wp_job_board_pro_submit_job_post_status` away from `pending_payment` to the normal moderation result.

### Identity and count
- Post type: exactly `job_listing`.
- Quota key: canonical owning WordPress employer user ID. Resolve `_job_employer_posted_by` (employer post ID) through `WP_Job_Board_Pro_User::get_user_by_employer_id()`; for new direct-form jobs use `WP_Job_Board_Pro_User::get_user_id()`; only fall back to `post_author` when it resolves to a valid employer profile. Employees resolve to their parent employer owner and share that employer’s three slots.
- Count only rows with `post_status='publish'` and no valid past `_job_expiry_date`. Missing/blank expiry means not expired. A canonical `Y-m-d` earlier than the site-local current date does not count. Invalid non-empty expiry fails closed for publication and is surfaced for admin repair.
- Never count `draft`, `preview`, `pending`, `pending_approve`, `pending_payment`, `expired`, `cancelled`, `denied`/rejected variants, `trash`, revisions, or autosaves.
- Existing active edit excludes its own post ID. Employer B can neither consume nor release employer A’s slots.

### Universal enforcement and races
- Early UX filters report `used`, `remaining`, and localized quota error, but are not the security boundary.
- The authoritative boundary is an owned `wp_insert_post_data` filter for every `job_listing` insert/update. It covers frontend form, wp-admin, REST, AJAX handlers and ordinary direct `wp_insert_post/wp_update_post` calls. Raw SQL is unsupported and forbidden operationally.
- For every non-admin transition whose requested target is `publish`, acquire a MySQL advisory lock named from a SHA-256 of blog ID plus canonical employer ID. Use the existing `$wpdb` connection; a bounded lock timeout that cannot acquire must fail closed.
- Hold the lock from `wp_insert_post_data` through `wp_after_insert_post`; release in `finally`-equivalent post hook and a shutdown fallback. Track held lock names request-locally to avoid recursive double-acquisition. While held, recount active rows excluding the current ID.
- If count is 0–2, allow publication. At 3, replace requested status with `draft`, preserve all submitted content and meta-safe data, set an owned non-PII reason code, and return a localized message through the form/admin/REST response adapter. Do not create an order, entitlement, cart item, mail, HTTP request, or scheduler row.
- `transition_post_status` is an invariant/audit hook, not the primary allocator: publish consumes one implicitly through the authoritative query; publish→non-counted or expiry releases one; restore/republish re-enters the same locked gate. No mutable numeric counter is authoritative, so retries cannot double-increment/decrement.
- `manage_options` is the roadmap-approved administrative moderation bypass. It may edit/change status without UI deadlock; every bypass is explicit and observable. Non-admin employer publication remains blocked while canonical count is 3 or greater.

### Existing and new users
- No bulk user/role/profile migration is needed. Eligibility derives from existing valid employer/candidate profile relations, making deployment deterministic and idempotent.
- Existing and new employers receive access without product/order/entitlement creation. Existing job-package metadata remains historical and is ignored for authorization.
- Candidates remain free for profile/CV/search/application. Force explicit free policy values so later option drift cannot silently restore payment gates.

## Free-journey migration contract (Task 2.48)
- In the same owned free-launch seam, stop booting public checkout prefill/field/currency/conversion/payment/invoice-send callbacks and package-purchase JS. Keep only read-only Woo/HPOS order-admin rendering required for history.
- Remove exact Paid Listings package/cart/order/candidate-gate callbacks at controlled boot priority; do not deactivate/delete vendor code until a later dependency proof confirms historical admin views survive.
- Remove pricing/packages/cart/checkout menu items by exact configured page IDs and remove header mini-cart in free mode. Keep Post Job links and point employer CTAs directly to submit page `1694` or dashboard `1653`.
- Replace/remove package Elementor widgets and child paid templates from rendered free journeys. The submit wizard must start at the normal submit step, not at package selection.
- Guard direct public requests to pricing/packages/cart/checkout/order-pay endpoints with same-origin safe redirects to submit/dashboard, while wp-admin historical order/product access remains available. No gateway callback is invoked by the redirect.
- Preserve all three products, three orders, and 10 cancelled entitlements byte-for-byte/logically unchanged. Never delete product, order, order meta, entitlement, or package meta.
- Keep WooCommerce installed because history/admin rendering and current owned order renderer still depend on it; disable only public purchase execution. Future monetization can re-enable a separate policy adapter behind the explicit feature seam, not by reusing hidden launch friction.
- Rollback is code/feature-flag only: restore prior callbacks/UI; never reverse-migrate data. Free-created jobs remain intact and visible; a rollback must grandfather already-created jobs and may require packages only for future submissions after an explicit owner decision.

## Deterministic acceptance matrix for Tasks 2.47/2.48

| # | Fixture/action | Required assertion |
|---:|---|---|
| 1 | Separate employers with 0, 1, 2 active unexpired jobs publish one | Resulting counts 1, 2, 3; no package/order/cart/meta side effect. |
| 2 | Employer with exactly 3 attempts fourth publish | Fourth remains draft with content/meta intact and localized reason; public count stays 3. |
| 3 | One row per excluded status plus past/blank/future expiry | Only publish + blank/future expiry counts; invalid expiry fails closed. |
| 4 | Edit one active job repeatedly | Count unchanged; same ID excluded from allocation. |
| 5 | Publish→draft, publish→pending, owned expiry→expired, trash | Each makes one slot available; retries are idempotent. |
| 6 | Restore/republish a non-counted job | One locked gate is applied; succeeds only when a slot exists. |
| 7 | Two synchronized publish workers against count 2 | Exactly one may create count 3; the other remains draft; never 4. Evidence includes advisory-lock ownership/release. |
| 8 | Administrator user with `manage_options` moderates an employer job | Admin operation is not trapped by employer UI quota; bypass is recorded without changing another employer’s ownership. |
| 9 | Employer A attempts IDs/meta/author belonging to B | Denied; both counts and all B content remain unchanged. |
| 10 | Existing employer and newly registered employer submit with no package/order | Both reach normal preview/moderation/publication path; zero entitlement rows created. |
| 11 | Existing/new candidate creates profile/CV, searches and applies once with no package | Allowed under normal auth/privacy/duplicate rules; zero package counters or `pending_payment`. |
| 12 | Desktop/mobile menus, pricing/packages pages, dashboard widget, submit form, cart/checkout direct routes | No paid CTA, package selector, cart, checkout, order-pay or payment dependency; submit CTA remains. |
| 13 | Before/after hashes/counts for product IDs 1728/1729/1730, three orders, and 10 entitlement IDs | Exact equality; admin historical renderer still works. |
| 14 | Every focused probe under ledgered fixtures and guards | Fixtures cleaned by exact IDs; business/protected fingerprints return to T0; mail, PHPMailer, SMTP, payment, external HTTP, broad runner, protected action and unexpected scheduler counters all 0. |

## Migration and rollback safety gates
- Preflight every implementation task at `origin/staging@ec97a6f…`, clean worktree, truthful marker and matching relevant live hashes; recapture business, product/order/entitlement, cron, Action Scheduler and role/profile fingerprints under both locks.
- Create fixtures only after a complete exact-ID ledger exists. Never use broad SQL/status/date cleanup, broad cron, or Action Scheduler runner.
- Before enabling free mode, read-only assert no employer already has more than three active jobs. Current count is zero. If future count exceeds three, do not unpublish automatically; block non-admin new publications and request an explicit business decision.
- Rollback restores code and the truthful marker only. It must not delete free-created jobs or alter preserved commercial history. All callbacks/filters and route changes must be feature-seam reversible.

## Unresolved product decisions
- None blocks 2.47/2.48. Later roadmap tasks still own CAPTCHA/browser auth, transactional email policy, privacy/retention, gateway/plugin decommissioning, and whether historical unpaid orders remain payable; free launch must not expose payment meanwhile.
- Vendor AS rows `32867/32868` remain separate observational roadmap items and do not block this map.

## Evidence and safety
- Evidence directory: `/tmp/raspitajse-task-2.46.4D3n6J`; restricted raw option/config snapshots are mode `0600` and contain no report-exposed secrets or PII.
- Manifest: `/tmp/raspitajse-task-2.46.4D3n6J/evidence.sha256`.
- Static source inspection and SQL `SELECT` only. Code/Git history/source branch/database/options/users/roles/live files/deploy marker/scheduler rows: unchanged.
- Cron/Action Scheduler callbacks, queue runner, payment, mail, SMTP, external HTTP/vendor requests: `0` executed.
- Locks released: YES.
- Production touched: NO.
