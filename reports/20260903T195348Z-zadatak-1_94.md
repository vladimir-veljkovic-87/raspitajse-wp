# Codex Execution Report

- Task: Zadatak 1.94 — Establish trustworthy WPJBP 1.2.73 vendor baseline provenance
- Task ID: 1.94
- Result: PARTIAL
- Recorded at (UTC): 2026-09-03T19:53:48Z
- Source branch: staging
- Source HEAD: 642c8c8efb51a56449fd7048c71d3216590d52bf
- Source working tree clean: YES
- Staging deploy marker: 642c8c8efb51a56449fd7048c71d3216590d52bf
- Staging environment: staging

## Task report

## 1. Result

- **PARTIAL / blocked at provenance boundary.** The complete bounded local search found no independently trustworthy WP Job Board Pro 1.2.73 artifact.
- No clean vendor baseline was accepted, no clean-vendor diff or per-hunk vendor classification was fabricated, and no cleanup is recommended.

## 2. Baseline/final application SHA and unchanged proof

- Declared baseline: `642c8c8efb51a56449fd7048c71d3216590d52bf`.
- Initial/final local `staging`, initial/final `origin/staging`, and staging deploy marker: exact same SHA.
- Final source branch: `staging`; worktree clean and aligned with `origin/staging`.
- Application source writes/commits/feature branches/pushes/deploys: `0/0/0/0/0`.
- `origin/codex-tasks` was read directly at `005a36e3289f6b84e6a476c93102889e2493b3e1`; it was never checked out, modified, committed, merged, rebased or pushed.

## 3. Authorized local artifact locations searched

Only the following bounded, non-production locations/evidence stores were inspected:

1. repository tree `/home/u601262303/repo/raspitajse-wp`, including filename-level archive/manifest discovery;
2. repository parent `/home/u601262303/repo` to maximum depth 3;
3. repository refs, tags, history, reflog, reachable and unreachable Git objects;
4. mounted task attachments under `/home/u601262303/.codex/attachments` to maximum depth 3, with file-type inspection only;
5. `/tmp` to maximum depth 3 for WPJBP/archive filenames;
6. exact staging subtree `/home/u601262303/domains/raspitajse.com/public_html/public_html_stage`, including exact staging `wp-content/upgrade` and `upgrade-temp-backup` locations when present;
7. `/home/u601262303/deploy-state` filenames at depth 1.

No unrelated home/system directory, secret store, production path, production backup or production database was searched.

## 4. Candidate artifacts found

No ZIP, tar, tgz, 7z, rar, package checksum or source manifest matching WPJBP was found. The only plausible-looking local candidates were directories/Git objects:

- current repository plugin tree;
- exact deployed staging plugin tree;
- earliest repository plugin snapshot from `3227269c2f4ead2abeb1ab839e3b05fe3e326bbf`;
- Superio theme adapter directory `wp-content/themes/superio/inc/vendors/wp-job-board-pro`;
- 11 unreachable WIP Git commits and 7 orphan blobs;
- mounted attachments, which consist of pasted text/JSON metadata and one PNG, not a plugin archive.

The repo parent contains only this repository. Deploy-state contains only the staging commit marker. No archive candidate existed in `/tmp` or the exact staging upgrade locations.

## 5. Provenance decision for each candidate

| Candidate | Coherence/version evidence | Independence/cleanliness | Decision |
|---|---|---|---|
| Current repository plugin tree | Git tree `cc8c06ec05efe64fb3ea714d0401d59c6e406cd6`; 1,351 files; 108,141,653 Git blob bytes; plugin header/version `WP Job Board Pro 1.2.73` | It is the current customized application payload and contains known Raspitajse signatures | REJECT: not independent and not clean. |
| Deployed staging plugin tree | Version 1.2.73; all three target hashes exactly match `origin/staging` | It is a deployment of the current customized repository tree | REJECT: duplicate runtime copy, not independent provenance. |
| Earliest Git plugin tree at `3227269c` | Git tree `c3cfe2afb8b66a270df962356978cd035fd7027f`; 1,343 files; 108,027,666 Git blob bytes; version 1.2.73 | Initial commit is explicitly “custom job board plugins” and target files already contain Raspitajse sender/template/test/debug signatures | REJECT: coherent but already customized. |
| Superio `inc/vendors/wp-job-board-pro` | Git tree `b8a21c8645801ab87dbae2112458f74dd65eaa03`; 7 PHP integration files | No plugin header, no package version, and none of the three target files; it is a theme adapter | REJECT: not the plugin/package. |
| 11 unreachable commits | All are dated WIP/task descendants of known customized commits; target blobs are known customized variants (`bcf4dc78…`/`670435d6…`, `f234319a…`, `9c44942a…`) | No independent import or clean package origin | REJECT. |
| 7 orphan blobs | Size/SHA/signature-only inspection found zero WPJBP class/plugin-header and zero target-template signatures | Not a coherent WPJBP package or any target file | REJECT. |
| Mounted attachments | Text/JSON plus one PNG; no archive/container MIME type | No package | REJECT/not plausible. |

No candidates disagreed as competing clean 1.2.73 packages because no clean package was found.

## 6. Accepted artifact filename/coarse location

- Accepted artifact: **NONE**.
- Filename/location: not applicable.

## 7. Accepted artifact SHA-256

- Not applicable. No archive/package passed provenance.
- Directory Git tree IDs above are reported only to make rejection evidence reproducible; they are not accepted archive checksums.

## 8. Exact WPJBP version proof

- Current repository and deployed staging plugin header declare `Plugin Name: WP Job Board Pro` and `Version: 1.2.73`; source constant also declares `WP_JOB_BOARD_PRO_PLUGIN_VERSION = 1.2.73`.
- The earliest repository snapshot carries the same plugin line/version, but version identity does not prove vendor cleanliness.
- The Superio adapter has only compatibility checks against plugin constants and is not a standalone versioned plugin.
- No independent artifact existed from which exact 1.2.73 vendor provenance could be proven.

## 9. Archive safety/extraction proof

- Archives found: `0`.
- Archive SHA-256 operations: `0`; content listings: `0`; traversal checks: `0`; extractions: `0`; PHP executions: `0`.
- No temporary extraction directory was created because there was no local archive to inspect.

## 10. Exact clean hashes for the three target files

Not available: no accepted clean artifact exists. Reporting earliest customized Git hashes as “clean” would repeat the exact 1.93 provenance error this task was designed to avoid.

## 11. Current staging hashes for the three target files

| File | `origin/staging` SHA-256 | deployed staging SHA-256 | identical |
|---|---|---|---|
| `includes/class-job-alert.php` | `6932b0819118fb8a02c5a12f548b52c992323deaa228bd1dd368f62867a9d6f8` | same | YES |
| `includes/email-templates-default/html-job-alert-notice.php` | `a61bbb1d754aabc8fcfa2fa06eda01a57a5765a04c80ec714d602665c13786f9` | same | YES |
| `templates/misc/my-jobs-alerts.php` | `53c746684ed4eef5d08c064541e55d4f79bee553da453af905676e3ee43cbe52` | same | YES |

## 12. Exact clean→current diff summary

- Not available and not claimed because the clean side is absent.
- The 1.93 later-history inventory (36 + 15 + 3 hunks versus already-customized pre-later-change snapshots) remains valid as repository history, but it is not a vendor-clean diff and was not relabeled as one.

## 13. Reconciliation of 1.93 findings

Without an accepted independent package, vendor-original status remains unresolved for:

- presence/absence of minute frequency;
- original `biannually` label;
- original From and Reply-To behavior;
- origin of HTML Content-Type;
- original debug/error logging;
- original `_job_alert_send_email_time` write semantics;
- whether the default email template was generic/vendor-branded;
- vendor-original management UI labels.

Protected current conclusions remain unchanged: do not reintroduce minute/test mode, hard-coded sender identities, PII/debug leakage, production URLs on staging, malformed targets, or regress localized/current UI behavior. Mechanical revert of 1.86 is still forbidden because it would restore known hard-coded identities, not prove a vendor restoration.

## 14. Per-hunk KEEP / REDESIGN / DROP decisions

Not produced. The task permits per-hunk vendor-delta classification only after accepting a clean artifact. Existing 1.93 safety classifications remain conservative current-state guidance, not proof of upstream authorship.

## 15. Active runtime reachability and suppression proof

- Vendor source still self-registers `WP_Job_Board_Pro_Job_Alert::send_job_alert_notice` on the daily hook.
- Owned evaluator still installs `cutover_daily_hook()` at `plugins_loaded` priority 101 and removes that exact priority-10 vendor callback.
- Final staging vendor candidate-job sender registration: `0`.
- Final daily graph: exactly five unrelated callbacks; neither daily hook nor vendor sender was executed.
- All checked owned evaluator/delivery/query/mailer classes load.
- Six candidate-job add/remove routes each contain exactly one `Raspitajse_Communications_Alert_Security` callback and zero vendor mutation callbacks.
- Global getter keys remain daily, weekly, fortnightly, monthly, biannually, annually; the owned new-alert allowlist remains exactly daily, weekly, fortnightly, monthly.
- Owned delivery only uses `metadata_exists()`/`get_post_meta()` for `_job_alert_send_email_time` during lazy initialization. Static search found zero owned update/add/delete writes to that key; the old write remains only in the suppressed vendor sender.

## 16. Owned WPJBP infrastructure dependencies that must remain

- Query adapter deliberately calls `WP_Job_Board_Pro_Query::get_posts()` and WPJBP query filters while adding owned bounds/order/eligibility.
- Owned mailer deliberately calls `WP_Job_Board_Pro_Email::render_email_vars()` and `WP_Job_Board_Pro_Email::wp_mail()` inside owned `candidate_alerts` SenderPolicy/Transport context.
- It reuses WPJBP email options/default fallback, `job_entry_template`, job/user/meta helpers, and `WP_Job_Board_Pro_Mixes::get_jobs_page_url()`.
- Owned mailer and multiple WPJBP/Superio UI surfaces use `WP_Job_Board_Pro_Job_Alert::get_email_frequency()`.
- Owned security uses WPJBP class existence, prefixes, user/profile helpers, nonces and lifecycle hooks.
- These dependencies prevent treating the vendor class/plugin as removable; only proven custom deltas may be considered in a future authorized task.

## 17. Current daily-hook graph and cron state

Environment: `staging`; mail-safety MU plugin loaded.

Final `wp_job_board_pro_email_daily_notices` graph, all priority 10:

1. `WP_Job_Board_Pro_Candidate::send_admin_expiring_notice`;
2. `WP_Job_Board_Pro_Candidate::send_candidate_expiring_notice`;
3. `WP_Job_Board_Pro_Candidate_Alert::send_candidate_alert_notice`;
4. `WP_Job_Board_Pro_Job_Listing::send_admin_expiring_notice`;
5. `WP_Job_Board_Pro_Job_Listing::send_employer_expiring_notice`.

- Callback count initial/final: `5/5`; sanitized fingerprint `99b7c17d642a054dec01410cdf4eea6e6b1ba49ee9aae4da104cec2317de3ff8` / identical.
- Direct vendor sender initial/final: `0/0`.
- Daily event count initial/final: `1/1`; timestamp `1778854026`, schedule `wpie_daily`, interval `86400`, empty-args hash `4f53cda1…b945`.
- Daily event fingerprint initial/final: `e37b37c804749fba0fe24ef4c39ab0c50f6552dff63e4e6ada90a739d785d9a1` / identical.

## 18. Owned hourly/continuation state

- Hourly evaluator count initial/final: `1/1`; timestamp `1788258663`, schedule `hourly`, interval `3600`, empty-args hash `4f53cda1…b945`.
- Hourly fingerprint initial/final: `0a24ef24f6c6ee7003c970f9e9eb250e7d947b7a0428ddc2c76515723f3e0bf5` / identical.
- Continuation count initial/final: `0/0`; empty fingerprint `4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945` / identical.
- Evaluator/continuation executions: `0/0`.

## 19. Protected alert fingerprints

- Published `job_alert`: count initial/final `0/0`; sanitized fingerprint `4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945` / identical.
- Published legacy `candidate_alert`: count initial/final `4/4`; sanitized fingerprint `ea9d38cbfa312ce7ecf659af76e0f7feccc9c60aad797fc8b2bfd4057b0e15b6` / identical.
- No PII, recipient or saved-query values were emitted.

## 20. Action Scheduler and ID32733

- Pending AS count initial/final: `7/7`.
- Sanitized fingerprint initial/final: `7765176c24f5524b29ea472f1f17932ebfe2e92f9e6a14f076283811162d119d` / identical.
- ID32733 final state: `pending`; attempts `0`.
- AS runner/due actions/ID32733 executions or mutations: `0/0/0/0`.

## 21. Zero-side-effect counters

- Application source writes/commits/feature branches/pushes/deploys: `0/0/0/0/0`.
- WordPress post/meta/option/user/business-state mutations: `0`.
- Cron schedule mutations; daily/vendor/evaluator/continuation executions: `0; 0/0/0/0`.
- Fixtures created/cleaned: `0/0`.
- `wp_mail`/PHPMailer/SMTP: `0/0/0`.
- Application/vendor HTTP requests: `0`; package-manager/download requests: `0`; payment calls: `0`.
- Action Scheduler mutations/executions: `0/0`.
- Authorized Git fetches are control-plane network operations and are reported separately from application/vendor HTTP.

## 22. Temporary extraction cleanup proof

- Extraction directories created: `0`.
- Extracted files: `0`.
- Cleanup required: none.
- The task-private report input file is removed after report publication; the report helper owns and removes its temporary worktree.

## 23. Production accessed/touched

- Production filesystem: NOT ACCESSED.
- Production database/backups/network/application: NOT ACCESSED.
- Production touched: **NO**.

## 24. Exact blocker and required artifact next step

Precise blocker: no already-authorized local artifact independently proves the original WP Job Board Pro 1.2.73 payload. Every coherent local plugin tree is derived from the customized Raspitajse repository/runtime; the theme adapter and orphan objects are not plugin packages.

Required next step—not a cleanup task and not started: the owner must provide or mount an authorized original/licensed WP Job Board Pro 1.2.73 ZIP/source package, or an independently verified checksum/source artifact with credible origin. After that evidence exists, a future READY read-only task may hash/list/extract it safely and perform the exact three-file clean comparison. No 1.95 implementation task is proposed without that evidence.

## Warnings / errors

- Known `HOST_NAMESPACE_PRESSURE` was respected; no sandbox patch helper was retried. Namespace-free read/hash commands were sufficient.
- No operational errors affected evidence collection.

## Stop

Zadatak 1.94 is stopped at the mandatory provenance boundary. No cleanup was implemented and Zadatak 1.95 was not begun.
