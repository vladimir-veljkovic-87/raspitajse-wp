# Codex Execution Report

- Task: Zadatak 2.60.2 — Enforce and verify staging search-engine isolation
- Task ID: 2.60
- Result: PARTIAL
- Recorded at (UTC): 2026-09-25T13:19:01Z
- Source branch: staging
- Source HEAD: f6093c11d71ddc175ebf83fc69453c20aaca5239
- Source working tree clean: YES
- Staging deploy marker: f6093c11d71ddc175ebf83fc69453c20aaca5239
- Staging environment: staging

## Task report

## Result

`PARTIAL: STAGING_SEARCH_ISOLATION_HTTP_CONTRACT_NOT_OBSERVED`

The scoped source change, deployment and WordPress option mutation succeeded, but the two required public HTTP responses did not expose the new isolation contract. Per task rules, no request was retried, no cache operation was attempted, the feature was not integrated, and staging was rolled back exactly.

## Source and static validation

- Baseline: `f6093c11d71ddc175ebf83fc69453c20aaca5239`.
- Expected initial live marker lag: `ff42719f218e440b168b57690b70a2402ee66809`; ancestor relationship proven; complete lag diff was only `tools/codex-git-apply.sh`; deploy-allowlisted lag diff was empty.
- Feature branch: `feature/task-2.60.2-staging-search-isolation`.
- Feature commit: `783cc6b3425985c1e27de8e823fca020d0d7b869` (pushed, not integrated).
- Exact feature change: new `wp-content/mu-plugins/raspitajse-staging-search-isolation.php`, 76 lines; no other tracked changes.
- Edit mechanism: corrected namespace-free helper; canonical new-file preflight and application passed. No normal namespace patch retry was attempted because the circuit breaker was already active.
- `php -l`: PASS.
- `git diff --check` and complete one-file diff/scope inspection: PASS.
- Offline non-staging check: PASS, zero isolation hooks registered.
- Offline staging check: PASS for `wp_headers`, core `wp_robots`, `aioseo_robots_meta`, and `robots_txt`; expected `noindex,nofollow` and exact crawl-deny body produced; sitemap absent.

## Locked staging mutation

- Locks acquired nonblocking in required order: owned-runner fd 9, runtime fd 8; released fd 8 then fd 9.
- Re-preflight environment/marker/guards: PASS.
- Pre-state `blog_public`: `1`.
- Changed deploy from feature commit: PASS.
- Exactly one changed file was synced: `wp-content/mu-plugins/raspitajse-staging-search-isolation.php`.
- `tools/codex-git-apply.sh` was not copied to WordPress.
- Temporary deployed marker: `783cc6b3425985c1e27de8e823fca020d0d7b869`.
- Runtime plugin/source hash parity: PASS.
- Only allowed option mutation: `blog_public` changed `1` to `0`; effective value and staging environment verified.
- Mail-safety, cron-recovery and Action Scheduler DROP guards remained active.

## Required HTTP verification

Exactly two unauthenticated GETs were attempted; redirects followed: 0; contacted host: `stage.raspitajse.com` only.

1. Home `/`: HTTP 200, no `Location`; required `X-Robots-Tag` absent; rendered robots meta remained `max-image-preview:large` and lacked `noindex`.
2. `/robots.txt`: HTTP 200, no `Location`; required `X-Robots-Tag` absent; global `User-agent: *` plus `Disallow: /` absent; no sitemap advertisement observed.

Acceptance result: FAIL at the public HTTP contract checkpoint. The runtime source/option state had been verified before these requests, but the served responses did not reflect it. No unproven cache/SEO cause is asserted.

## Rollback

- Reacquired both locks in the required order.
- Restored exact prior `blog_public=1`.
- Ran `deployment/deploy-staging.sh changed staging` from the still-unmodified baseline.
- Exactly the new MU plugin was removed from live staging.
- Final live marker: `f6093c11d71ddc175ebf83fc69453c20aaca5239`.
- Final local `staging` and `origin/staging`: `f6093c11d71ddc175ebf83fc69453c20aaca5239`.
- Final worktree: clean.
- Final environment: `staging`; `blog_public=1`; all required mail/scheduler guards active.
- Rollback state: COMPLETE.

## Next smallest corrective fact

A future bounded task must determine which public response layer served the old home and robots payload despite verified origin runtime state, without assuming a cache cause or authorizing purge/flush/warm. It must define any cache/CDN/server evidence and mutation contract before repeating the two-request acceptance test.

## Safety

- HTTP requests attempted: 2.
- Redirects followed: 0.
- Hosts contacted: `stage.raspitajse.com` only.
- Cache purge/flush/warm/crawler/optimization: 0.
- Fixtures: 0.
- Mail/cron/Action Scheduler/payment execution: 0.
- Protected business-state mutation: 0.
- Database mutations: one allowed option update and its exact rollback only.
- Production touched: NO.
